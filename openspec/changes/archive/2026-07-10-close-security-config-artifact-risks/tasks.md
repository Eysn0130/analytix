## 1. Policy contracts and migration

- [x] 1.1 Add execution policy version 2 to shared runtime settings and durable Go thread policy contracts, with fresh `on-request` plus `workspace-write` defaults.
- [x] 1.2 Implement narrow one-time migration for the exact unversioned legacy default, invalid-value fallback, explicit version-2 opt-in preservation, and override-aware idle-thread migration.
- [x] 1.3 Set Connect Phone and scheduled-task entry points to unattended `never` plus `workspace-write` defaults and add focused TypeScript tests.
- [x] 1.4 Enforce foreground/background shell rejection outside `danger-full-access`, harden external-read alias real-path containment, and add focused Go tests.
- [x] 1.5 Update permission copy to describe workspace-write as an application tool/path policy rather than OS isolation.

## 2. Production configuration truthfulness

- [x] 2.1 Replace whole-settings runtime synchronization/restart hashing with one explicit production startup projection and remove inactive/media credential nodes from Go config.
- [x] 2.2 Extend atomic writes and settings/runtime/MCP config writers to create and retighten secret-bearing files to POSIX mode `0600`, with platform-aware tests.
- [x] 2.3 Hide inactive storage, hook, token-economy, auto-compaction, tool-storm, argument-repair, and unsupported media controls while preserving compatibility parsing.
- [x] 2.4 Reframe memory as a manual persistent-record/privacy surface, remove ineffective enablement synchronization, and report manual/store-only capability semantics.
- [x] 2.5 Remove prompt instructions for unavailable Go `generate_*` media tools while retaining Electron Write image and speech-to-text behavior in their real scopes.
- [x] 2.6 Wire `streamIdleTimeoutMs` and provider `contextWindowTokens` through the Go config/runtime-info path with positive, zero, and absent-value tests.
- [x] 2.7 Replace the stale runtime config example with the safe production-consumed surface and add a schema/truthfulness regression test.

## 3. Repository and evidence hygiene

- [x] 3.1 Expand ESLint ignores for generated/tool payloads while proving maintained source remains linted.
- [x] 3.2 Ignore and untrack generated `output/`, `dist-standard-win*`, `validation-evidence/`, and runtime-output trees without deleting local files; retain required vendor and managed-browser inputs.
- [x] 3.3 Update the document map and evidence guidance so archived changes, current specs, generated snapshots, and rerun requirements are classified correctly.
- [x] 3.4 Strictly validate and archive the completed Hub startup-auth and Vision Bridge changes with main-spec synchronization.
- [x] 3.5 Preserve classified Analytix-local commit provenance after history rewrite, update current manifests, and leave external upstream identifiers unchanged.
- [x] 3.6 Purge the retired privacy-sensitive financial report from every retained local ref, verify exact path absence without reading identifiers into output, prune unreachable objects, and remove the temporary recovery bundle.

## 4. Windows QA access and incident cleanup

- [x] 4.1 Replace the old Windows QA path with a tracked secret-free runbook containing approved non-secret host/account/port metadata, SSH alias/key enrollment, RDP, secret-manager, and fallback procedures.
- [x] 4.2 Add redacted secret/path verification checks and confirm current tracked content contains no live Windows password, remote-control identifier, or access code.
- [x] 4.3 Rotate or disable every exposed Windows/RDP/remote-control credential and enroll the approved SSH public key through an authenticated operator session.
- [x] 4.4 Back up refs, purge the compromised QA path from every retained branch/tag, verify path and redacted secret scans, then expire unreachable local objects.
- [x] 4.5 Correct the Windows administrator OpenSSH key path/ACL procedure and harden retained-history scans for cross-line credentials and tree-valued refs.

## 5. Validation and closeout

- [x] 5.1 Run focused TypeScript and Go tests for settings migration, runtime config, file modes, policy enforcement, filesystem containment, stream timeout, and runtime model info.
- [x] 5.2 Run `npm run typecheck`, `npm run lint`, relevant builds, both Go test suites, strict OpenSpec validation, link/path checks, and `git diff --check`; distinguish new failures from baseline warnings.
- [x] 5.3 Review the final diff for unrelated/generated content, record the remaining external credential-rotation or remote-publication gate if any, and keep the change active until every closure requirement is met.
