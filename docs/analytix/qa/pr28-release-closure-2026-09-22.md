# PR28 Core qualification and multi-snapshot closure — 2026-09-22

Status: Historical execution evidence for the local candidates named below. The
[current matrix](../product-completion.md) owns current status; the
[execution plan](../delivery-execution-plan.md) owns the single delivery order.
This report is not a publication receipt or independent review approval.

## Input and preserved identity

Canonical branch: `codex/workbench-product-delivery-20260914`, PR28. The initial
checkout was clean at DELIVERY `9c21c0ec911571ddba1c733ac4a828737654e02b`
(tree `ef13b98fec01bd11048399aefb399c7cc1fe948b`). Retained SOURCE
`0708620e21fc2bd706ba18a377a60824cdb78421`
(tree `bc4f4459ca7a520cf74cd5b0f850f176bacc8130`) is an ancestor. No reset,
rebase, alternate checkout, parallel writer, Goal or automation was used.

The supplied archive SHA256 is
`5b66bb220c0c604a91058a637fbafd3fe98f17eda558eeca59023067e294325c`.
Seven regular entries passed path/type/CRC and SHA256SUMS checks before isolated
extraction. Document instructions were applied within the user's explicit scope.

## Plan and Core source delivery

- `b485a0357`: integrated the accepted long-term plan at
  `docs/analytix/delivery-execution-plan.md`; handover and matrix route to it.
  The two dated report headings were marked historical, preserving their anchors
  and original failures. No second workflow Skill or knowledge store was created.
- `69e74fdb6`: authenticated `core_controlled_release`, exact signing policy/team,
  compiled Core qualification, signer/after-sign/Go reader, milestone/GUI smoke
  consumers, Core publisher/updater target binding and post-builder metadata.
- `c2ef61c4b`: legal inventory recognizes retained `LICENSE.BSD`,
  `LICENSE.markdown`, `LICENSE.MIT` and `LICENSE.APACHE2`; missing files still fail.
- `0ce2e92a0`: the original formal-evidence owner and release gate now have a
  read-only Core-stage qualification route. It retains A0, source/artifact,
  shared signature and exact legal requirements; B1 is `not_applicable_core`,
  never fabricated as passed. Full finalization retains A0+B1 and its own ledger.

- `5a7d2cebc`: the existing whole-artifact legal reader now includes physical
  after-pack files outside the ASAR index without shadowing indexed entries.
  The exact owner self-test and all455 formal-evidence tests pass.
- `ac00f1602`: unindexed runtime metadata must belong to the actual ASAR
  unpacked directory, not an unrelated suffix match. All456 formal-evidence
  tests and the owner self-test pass; re-audit retains31 dependency blockers
  and no runtime metadata blocker.

Core content and assurance are distinct. A controlled Core candidate still has
`publishable=false`, `releaseEligible=false`, `publicationReceiptIssued=false`.
Core absence cannot authorize Full missing resources. Normal Developer ID,
notarization, publication authority and actual acceptance remain necessary.
The Core-stage command issues neither Full admission nor publication authority.

Verification: Core/packaging/updater/milestone focused suite 258/258; renderer
history suite 75/75; formal evidence456/456; Full finalization 36/36; Node and
web typechecks passed at their recorded source boundaries. Core protocol/metadata/
publisher Node checks and Go package-authority race checks also passed. A
synthetic OS signature boundary in the Core aggregator positive test is explicitly
not Developer ID acceptance. Earlier RED, timeout and fixture-link failures remain
in the private evidence inventory; later passing runs do not erase them.

## Update, version, compatibility and shipped resources

Darwin arm64 is the only Core stage target. Product identity remains
`com.analytix.desktop`; current source version remains `1.0.6`. Core stable/beta
metadata and DMG/ZIP byte identities are bound to platform/arch/profile/channel,
version and `core-v1` compatibility generation. Cross-profile automatic updates,
unsigned overrides and unknown compatibility generations fail closed. Version
archive uploads use create-only writes; channel pointers remain a separate step.
The pinned builder writes update YAML after its hook, so the Core metadata owner
runs after builder completion. No new feed service was created.

Read-only public checks observed no GitHub releases. Both configured Core
stable and beta metadata HEAD requests returned HTTP404. This does not establish
all historical R2 versions or authorize selecting a public version. No upload,
channel promotion, end-to-end upgrade, interrupted-download recovery or installed
old-data acceptance was run. No app identity, Keychain or real user data changed.

The retained private Core ASAR inventory contains PDF/Shiki WASM, renderer
libraries, 1,701 font-file entries and Computer Use/native dependency entries. It
contains 1,226 license/notice entries. These index counts include paths whose bytes
can live in the unpacked directory; they are not executed-code or rights admission.

| Exact old-artifact inventory | Observed license evidence / remaining limit |
| --- | --- |
| PDF.js5.4.394: openjpeg.wasm and qcms_bg.wasm | Apache-2.0 package declaration/LICENSE; four component notices enumerated; exact redistribution obligations still require admission |
| Shiki onig.wasm / engine-oniguruma3.23.0 | Engine package MIT/LICENSE present; WASM provenance and complete obligations are not established by that alone |
| mermaid11.16.1 and katex0.16.46 | MIT declarations and LICENSE files present |
| Noto Serif SC5.2.9 and other font-file entries | Noto OFL-1.1/LICENSE present;1,701 indexed font files are not1,701 separately cleared fonts |
| Computer Use libnut-darwin2.7.1 and nut-js4.2.0 | Apache-2.0 notices present in both root/runtime copies; related4.2.0 packages remain among missing-license detections |
| canvas-darwin-arm640.1.100; better-sqlite3, node-pty and other native index entries | Canvas declares MIT but lacks its required license file;25 native index entries include platform variants and are not proof all variants physically ship or execute |
| Office assets / templates | No separately named Office WASM or document-template file was found in this bounded inventory; bundled code or other template forms are not excluded by that search |


The initial filename correction reduced40 blockers to34:31 dependency instances
and three first-party metadata paths. The physical after-pack enumeration repair
removed those three metadata false positives. The corrected whole-app audit now
finds1,175 package instances/833 package-version pairs and31 mandatory dependency
blockers, with no parse errors. They include29 missing license-file detections and
two absent package license declarations (exif-parser0.1.12 and khroma2.1.0).
Type-fest4.41.0 retains separate license-mit/license-cc0 files; its complete OR
license choice still needs explicit exact-artifact accounting. The other missing
files are also absent from their matching installed source package directories. This audit is bound to the old private app, not a new signed
artifact; remaining fonts/WASM/native obligations need exact-source evidence.
No sibling license was copied speculatively and no obligation was waived.

The existing Core DMG remains unchanged: sealed source
`96e55c817b54af171c8c874d2af4ace711bc5c75`, profile `core`, version `1.0.6`,
SHA256 `ea577ad19ffc1c71204dce1ad003aaa541a7b81ea0bdda13c454741b67475f95`,
330,546,820 bytes, classification `development_clean_non_publishable`.
It was not relabeled or used as proof of the new source's package acceptance.

## Multi-snapshot public history

Delivered SOURCE: `dc059977ae0ae3b4986698eda82c25bfe9a54275`
(tree `515145582c719a4f18bf2cc0405adcaef0f9797c`). The23 source/test/UI files
match the full-chain candidate hash inventory. This commit preserves all prior
Core and delivery commits.

The original B1 test now asserts actual public GET and both original final
bindings after A1/A2 creation and durable reopen, in addition to its existing
protected sink, native-result, original-byte and Provider-count assertions.

Observed executions preserve distinct failure boundaries:

1. Baseline isolated from `b485a0357` plus only the new assertion: exit1 after
   621.02s; two committed facts, recovery candidates2/admitted0/held2; actual
   public HTTP503 `public_projection_pending`.
2. Retained dataset-owner candidate: exit1 after823.25s; admitted2/held0 and
   HTTP200, but the original A1 public binding was absent.
3. Initial public projection candidate: exit1 after892.81s; admitted2/held0,
   HTTP200, A1 public view absent. Its exact failure and original evidence remain.
4. Current-permission candidate: exit1 after412.73s; the A2 public GET exceeded
   its original45-second deadline before durable reopen.
5. Whole-hydration operation candidate: exit1 after291.45s; A2 asynchronous
   finalization exceeded its original90-second deadline. Cleanup observed both
   terminal operations completed without business failure. This alone does not
   identify the cause or prove a permanent stall. A follow-up diagnostic retains
   the90-second limit and reports only bounded owner-frame counts on timeout.
   A two-second sample of that exact test process observed current-selection/shared
   head and Private CAS file-validation frames, with no retained-history or public
   projector frames in the sample. It does not prove the whole-run bottleneck.
6. Diagnostic repeat: exit1 after302.44s at the same90-second asynchronous test
   wait; cleanup again observed both operations completed without business failure.
   Timeout frame counts for historical-witness/public-projection owners were zero.
   This is not proof of a projection stall. Following execution checklist section9,
   the harness now gives exact-native terminal validation180seconds, logs when it
   exceeds the old90-second budget, and still fails if not complete. No product
   latency threshold was changed; public HTTP stays45seconds and every status,
   content, Provider, original-byte and whole-batch assertion remains.
7. Post-witness epoch candidate: exit1 after904.59s. A2 terminal and public GET
   passed; durable recovery admitted2/held0; the newly asserted A1/A2 public GET200,
   both original final digests, A1 historical label and A2 current label passed.
   The retained missing-material fault then returned500 instead of the existing
   public-projection503 contract. The original owner now returns the existing
   typed pending error; the HTTP adapter remains the sole status/body mapper.
   A focused test checks503, no turns and no private error details before rerunning
   the complete native chain. This failing run is retained, not relabeled green.

8. Final unchanged-source full chain: exit0 after1776.55s. Actual A1/A2 GET200,
   both original final digests, retained/current labels, missing/corrupt GET503
   without partial turns, exact restoration GET200, ordinary Provider continuity,
   unchanged original stores/Provider counts and a fresh OS process all pass.
   The new process creates a fresh witness client, restores both originals and
   verifies protected local display. Log and exact source-file/binary hashes are
   retained in `b1-public-fault-result.json` and its candidate identity record.

The accepted [case-data-forensics contract](../../../openspec/specs/case-data-forensics/spec.md)
requires prior facts to retain their snapshot binding and appear as historical,
while new queries bind the current exact selection. This repair applies that
existing distinction; it does not add a second authority or authorize old queries.

The current repair preserves original snapshot/material/registry/witness/final
identities while challenging their ancestry against the fresh retained chain.
It grants no current-query selection capability. The public consumer separately
checks the current principal/case/risk authority for the current display operation.
A1 is marked `retained_snapshot` only after successful admission; A2 remains
current. Renderer labeling additionally requires its validated delivery receipt.
The whole-thread failure unit and immutable original authority are retained.
Hydration revalidates historical/current permission before and after the complete
operation, while each event keeps its primary-CAS/manifest checks. The private
operation copy is bound to the exact input thread, never cached across requests;
a late revocation or cancellation returns no partial delivery. After the witness
boundary the case/context-epoch owner is challenged again; returning from B to A
cannot reactivate an earlier context handle. A fresh current A context may display
retained A history even when it selects the same original snapshot. The complete
thread race suite passes26.902s after this correction.

The first failure was at the registry witness/dataset selection boundary, before
public hydration. The previous path required each original dataset root to equal
the currently selected root. A1 therefore failed after A2 became current, and the
existing whole-thread restore transaction withheld both records. Two held records
were not evidence that both original facts were corrupt. After exact historical
admission, both recovered, exposing a separate public-epoch visibility mismatch;
that consumer repair retained original identities and added current permission.
Protected local-value display already had its own exact retained-material route;
new analytical queries continue to require current selection and capability.

A read-only comparison of the actual two committed synthetic contexts confirmed
that identity/workspace/case/binding match, while A1's snapshot, epoch and risk
policy digest differ from current A2. Risk class, disposition and binding
observation still match. Requiring the old publication policy for current viewing
therefore mixed historical provenance with current permission.

Dataset/registry/thread/current-risk owner race suites pass. A1/A2 dataset reads
are separately exercised in both orders, concurrently, and after exact material
restoration, without changing current query selection. An initial additional ABA
test incorrectly expected the risk-level owner to invalidate same-risk case
changes; that responsibility belongs to the case/context-epoch owner. The corrected
ABA test targets that owner rather than changing risk semantics to satisfy a test. The final complete
native/public/fault/new-process run passes as recorded above; its result is
independent of the owner tests. The expanded fresh-process helper reads the actual persisted facts,
recreates process-local authority from a new witness client and performs public
readback without repeating native import or model work. Only synthetic config,
paths and expected identities cross that helper boundary, not capabilities/keys.

## Verification scope for the recovery contract

| Required seam | Actual evidence boundary |
| --- | --- |
| Original A1/A2 facts and current query separation | Real native public chain plus dataset historical-selection owner; original contexts/finals are immutable and no query capability is issued by historical admission |
| A→B→A, revoked/cancelled operation, no partial batch | Current case/context-epoch and two-delivery hydration owner race tests; not a GUI permission journey |
| Material loss/tamper/restoration | Real B1 retained parsed-page fault injections and public503/200 assertions; final full-run result passes as separately recorded |
| Enumeration and concurrency | Both A1/A2 orders and concurrent reads in the actual dataset owner; existing recovered-terminal concurrency, revocation and immutable-byte checks retained |
| Repeat startup and fresh process | Durable reopen plus fresh OS process using persisted synthetic facts and a new witness client; no serialized process capability or repeated model/import work |
| Subsequent Provider and compaction | B1 ordinary Provider continuation during retained-material failure and forbidden-channel checks; affected thread suite includes existing compaction/privacy guards |
| Crash cuts | Existing dataset atomic-bundle crash/retry and terminal recovery guards are retained; accepted-final durable crash/event-slot/concurrent-winner and compaction crash matrices pass with production tags and race in124.842s |

These seams do not claim an exhaustive Cartesian product of multi-snapshot,
concurrent startup/GET, every crash cut and every compaction schedule. Installed
Funds qualification and broader case journeys remain distinct from this bounded
source repair.

## Remote checkpoint (read-only)

At the recorded2026-09-22T10:27:21Z check inventory, PR28 remained OPEN/Draft/BLOCKED,
head `dde784437dc8563e84066629dd57f4a11fd9acc9`; main/base was
`ce96cf12581acfa0e19fae7c6aa9c709371012c8`. The returned review decision was empty,
not an approval. All56 check runs belong to that old PR head:55 success from
GitHub Actions (app15368), and CodeQL failure from GitHub Advanced Security
(app57789). Commit statuses were empty. These are neither a scan of local
successors nor current-source CI approval. No dismissal or gate change occurred.

## External boundaries and minimum next actions

| Blocker | Minimum next action | Owner / acceptance needed |
| --- | --- | --- |
| Approved normal macOS test session and Provider credential entry not established | Identify the authorized test session, then use normal product Provider Settings; do not send secrets in chat | User supplies non-secret environment readiness; writer performs installed journey |
| Official signing team is unset; production publication trust anchor is unset | Configure the real authorized team/signing and publication authority through the normal route | Authorized release owner; fresh controlled Core bytes, notarization/staple receipts |
| Exact resource obligations unresolved | Supply/verify exact package-version and asset license/provenance material, propagate through the packaging owner, audit the new exact artifact | Dependency/resource owner; no blanket waiver |
| Installed Core/Provider/relaunch/120-turn GUI and update/old-data journeys unrun | Run those journeys on the qualified exact candidate in isolated authorized state | Actual Electron/Main/preload/renderer and normal Provider evidence |
| Source egress `PAYLOAD_SCOPE_UNKNOWN / NOT_CLEARED` | Resolve the original request/target/scope through its normal review/support record | Original refusal request ID/time/payload remain UNKNOWN; no alternate transport |
| Current remote CI/review/main not applicable to local successors | After egress clearance, inspect all outgoing history, normal push original branch, exact-head CI/review, expected-head merge, then real main checks | Existing mandatory gates; no old-SHA CI reuse |

The macOS test-session clarification is pending. No Keychain content, personal
profile, credentials or real case data was read/copied. No installation, live
Provider, signing/notarization, push, PR mutation/merge, tag, release or channel
write ran. Core/Funds stages, MERGED_MAIN, VERIFIED_MAIN, COMPLETE_PRODUCT,
FORMAL_RC and PUBLIC_RELEASE remain separate and unaccepted.

## Recorded verification commands

All storage-using commands sourced `./scripts/use-analytix-cache.sh` in the same
shell. The private logs retain failed as well as successful executions. Focused
checks belong to their recorded candidate; overlapping groups are not summed.

| Command / owner | Evidence |
| --- | --- |
| `node --test scripts/core-package-profile.test.cjs scripts/core-update-metadata.test.cjs scripts/publish-r2.test.mjs` | Core qualification, channel metadata and publisher positive/negative fixtures pass |
| `npx vitest run src/main/packaging-config.test.ts src/main/gui-updater.test.ts src/main/gui-update-target.test.ts src/main/runtime-go-packaged-milestone-a.test.ts --testTimeout=30000` |258/258 pass |
| `npx vitest run src/main/runtime-go-formal-evidence.test.ts --testTimeout=30000` |456/456 pass after final legal owner-path fix |
| `npx tsc --noEmit -p tsconfig.node.json` and web typecheck | Pass at recorded unchanged TypeScript candidate; initial web metadata typing failure retained |
| `go test -tags analytix_prod -race` on package-authority domain/reader, dataset, registry, evidence, turnsecurity and thread-risk owners | All seven scoped packages pass; source-bound synthetic evidence |
| `go test -race ./internal/app/thread` | Complete affected thread suite passes after post-witness epoch and pending-error changes |
| `go test -race ./internal/app/thread ./internal/adapters/inbound/httpapi -run 'Test(Retained\|CurrentCase\|Build.*Hydration\|ThreadHandlersRecordErrorMapping)'` | Focused authority and existing503 adapter mapping pass |
| `go test -race ./internal/app/datasetsnapshot -run TestHistoricalFactSelection` | Original binding, denial, order/concurrency and exact restoration pass |
| `go test -tags analytix_prod -race ./internal/server -run 'Test(AcceptedFinalDurable(CrashCutMatrix\|EventSlotCrashMatrix\|ConcurrentTerminalHasOneWinnerAndQuarantinesContender)\|CaseCompactionCrashSafeTwoPhaseFilesystemMatrix)$' -count=1 -timeout=10m` | Pass124.842s; existing crash checks, no relaxed assertions |
| Production-tag runtimeapp test-binary compilation with the existing explicit ad-hoc signing-policy fixture flags | Pass; not an installed runtime or formal package build |
| Exact-native `TestFundsAccountFlowB1ProductionPublicChain`, timeout40m, current-native source `aac3ccef06c8118661a06aa5fc3116ed7e5cd131` | Pass1776.55s, including fresh OS process; all original assertions retained |

The normal Core-stage command, installed A0, true Developer ID/notarization,
GUI120-turn journey, normal Provider, end-to-end update/data migration, main CI and
publication were not run because their stated prerequisites are not met.

## Evidence routing and final identity

Private evidence root on this host:
`/Volumes/AnalytixCache/development-v3/evidence/release-closure-wh17y_1k`.
Directories are0700; reports/logs are0600. Test binaries live in the separate
managed temporary bin directory. Initial Core binary placement in evidence was
corrected after the cache helper failed closed; blocked commands were not treated
as executed. The root contains raw logs and a bounded source-file hash inventory;
its contents are not authorized for external transmission.

Skill inventory was limited to14 known developer Skill entries. No installation,
Skill loader, third-party code migration or connector write occurred. The canonical
knowledge mirror is pending the same uncleared external-write boundary.

Final SOURCE is `dc059977ae0ae3b4986698eda82c25bfe9a54275`, tree
`515145582c719a4f18bf2cc0405adcaef0f9797c`. The subsequent docs-only delivery commit
contains this report, the current matrix and the handover index; its exact HEAD/tree
and clean status are in the writer’s final receipt. No new distributable app or
package was made after these source changes. The old artifact identities above remain unchanged.
