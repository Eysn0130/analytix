# analytix runtime P0/P1 closure matrix

Status: Historical three-source closure snapshot.
Snapshot date: 2026-07-02.
Currentness check: 2026-07-10.

This matrix records the scoped runtime closure audit against the three pinned
refs below. A `fixed` row means the cited source and test supported that scoped
requirement at those refs; it does not prove current nine-source closure.

The strict audit treats the three refs below as immutable historical snapshot
ancestors and independently requires each current local ref to match
`upstream-sources.json`. It still returns `ok:false` against the current
worktree because this historical matrix has stale analytix line references and
has not received the required row-by-row Reasonix/OpenCode delta review. Do not
update only the three historical hashes or current line numbers to make this
snapshot green. Current source authority is `upstream-sources.json`, and the
current gaps/decisions begin in `upstream-capability-audit-2026-07-10.md`; the
Goal-wide current capability matrix and benchmark remain separate unfinished
artifacts.

CodexDesktop `src/*` references in historical rows point to ignored/generated
local extracts. They are not commit-pinned source evidence and do not establish
code or asset reuse permission.

## Upstream refs

| Upstream | Ref |
| --- | --- |
| Reasonix `origin/main-v2` | `5c18d06e1e3617d02c8c8cf85e5d7eaac3e380a8` |
| OpenCode `origin/dev` | `3adfb970bf071419599ca016ebd2b08361fa28e9` |
| CodexDesktop-Rebuild `origin/master` | `fd39a0a9f450d7fd58e97d8ec35de98cd6c6ebf4` |

## P0/P1 matrix

| Category | analytix current evidence | Reasonix reference | OpenCode reference | CodexDesktop reference | Risk | Absorb? | Implementation / decision | Regression | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| runtime-go kernel architecture boundary / composition root | Phase 0/1 migration is source-enforced by `packages/runtime-go/internal/architecture/architecture_test.go:1`; pure value contracts start under `packages/runtime-go/internal/domain/model/types.go:1`, `packages/runtime-go/internal/domain/model/endpoint.go:1`, `packages/runtime-go/internal/domain/model/schema.go:1`, and job/subagent aliases remain compatible through `packages/runtime-go/internal/jobs/lineage.go:1`; app ports and composition root start at `packages/runtime-go/internal/ports/ports.go:1` and `packages/runtime-go/internal/runtimeapp/app.go:1`; runtimeapp now assembles durable stores, provider client/config, MCP manager, job manager, sandbox/web/vision/step-limit config, and hands concrete components through `packages/runtime-go/internal/server/runtime_components.go:1`; `analytix serve` enters through `packages/runtime-go/cmd/runtime-server/main.go:1`. The transitional server gravity well is split into handler-state/route/lifecycle/tool/subagent/terminal/durable anchors such as `packages/runtime-go/internal/server/runtime_handler.go:1`, `packages/runtime-go/internal/server/routes.go:1`, `packages/runtime-go/internal/server/turn_start.go:1`, `packages/runtime-go/internal/server/tool_catalog.go:1`, `packages/runtime-go/internal/server/subagent_jobs.go:1`, `packages/runtime-go/internal/server/terminal.go:1`, and `packages/runtime-go/internal/server/durable_event_log.go:1`. Current app/adapters extraction anchors include model execution/turn config/cache/history repair at `packages/runtime-go/internal/app/model/service.go:1`, `packages/runtime-go/internal/app/model/provider_config.go:1`, `packages/runtime-go/internal/app/model/provider_history.go:1`, `packages/runtime-go/internal/app/model/pending_tool.go:1`, `packages/runtime-go/internal/app/model/prefix_shape.go:1`, and `packages/runtime-go/internal/app/model/tool_pairing.go:1`, goal/todo closure helpers and service orchestration at `packages/runtime-go/internal/app/goal/closure.go:1` and `packages/runtime-go/internal/app/goal/tool_service.go:1`, plan-mode create_plan pure target/fallback/sandbox/response logic at `packages/runtime-go/internal/app/plan/create_plan.go:1`, tool contracts and builtin/goal/todo/plan/subagent-job/skill/user-input schemas at `packages/runtime-go/internal/app/toolcatalog/diagnostics.go:1`, `packages/runtime-go/internal/app/toolcatalog/builtin_tools.go:1`, `packages/runtime-go/internal/app/toolcatalog/goal_tools.go:1`, and `packages/runtime-go/internal/app/toolcatalog/plan_tools.go:1`, `packages/runtime-go/internal/app/toolcatalog/subagent_tools.go:1`, and `packages/runtime-go/internal/app/toolcatalog/user_input.go:1`, subagent request/profile/config/source/job-view/runtime-state/task-job-tool contracts at `packages/runtime-go/internal/app/subagent/requests.go:1`, `packages/runtime-go/internal/app/subagent/profile.go:1`, `packages/runtime-go/internal/app/subagent/config.go:1`, `packages/runtime-go/internal/app/subagent/job_view.go:1`, `packages/runtime-go/internal/app/subagent/output.go:1`, `packages/runtime-go/internal/app/subagent/runtime_state.go:1`, and `packages/runtime-go/internal/app/subagent/task_job_tools.go:1`, `packages/runtime-go/internal/app/subagent/task_job_service.go:1`, loop step-limit precedence/resolution, recovery/failure shaping, provider retry/delay diagnostics, provider-progress events, and loop guards at `packages/runtime-go/internal/app/loop/events.go:1`, `packages/runtime-go/internal/app/loop/prompts.go:1`, `packages/runtime-go/internal/app/loop/recovery.go:1`, `packages/runtime-go/internal/app/loop/step_limits.go:1`, and `packages/runtime-go/internal/app/loop/tool_guards.go:1`, goal/todo service orchestration, turn start plan/record construction, completion/failure/tool-call-ready/gate request, continuation, and cancellation construction at `packages/runtime-go/internal/app/turn/start.go:1`, `packages/runtime-go/internal/app/turn/start_plan.go:1`, `packages/runtime-go/internal/app/turn/completion.go:1`, `packages/runtime-go/internal/app/turn/failure.go:1`, `packages/runtime-go/internal/app/turn/gate_continuation.go:1`, `packages/runtime-go/internal/app/turn/gate_cancellation.go:1`, `packages/runtime-go/internal/app/turn/gate_requests.go:1`, and `packages/runtime-go/internal/app/turn/tool_call_ready.go:1`, terminal/runtime info/MCP search/web config/usage app services at `packages/runtime-go/internal/app/terminal/bash_tool_request.go:1`, `packages/runtime-go/internal/app/terminal/bash.go:1`, `packages/runtime-go/internal/app/terminal/background_bash.go:1`, `packages/runtime-go/internal/app/terminal/buffer.go:1`, `packages/runtime-go/internal/app/runtimeinfo/capabilities.go:1`, and `packages/runtime-go/internal/app/usage/records.go:1`, inbound HTTP/SSE shapes at `packages/runtime-go/internal/adapters/inbound/httpapi/routes.go:1` and `packages/runtime-go/internal/adapters/inbound/sse/events.go:1`, outbound provider/MCP/file/process/web boundaries at `packages/runtime-go/internal/adapters/outbound/provider/compat/request.go:1`, `packages/runtime-go/internal/adapters/outbound/provider/stream/stream.go:1`, `packages/runtime-go/internal/adapters/outbound/mcp/protocol/protocol.go:1`, `packages/runtime-go/internal/adapters/outbound/filestore/jsonl.go:1`, `packages/runtime-go/internal/adapters/outbound/filestore/json_map.go:1`, `packages/runtime-go/internal/adapters/outbound/filestore/text.go:1`, `packages/runtime-go/internal/adapters/outbound/filestore/search.go:1`, `packages/runtime-go/internal/adapters/outbound/filestore/code_index.go:1`, `packages/runtime-go/internal/adapters/outbound/filestore/path_policy.go:10`, `packages/runtime-go/internal/adapters/outbound/filestore/path_policy.go:38`, `packages/runtime-go/internal/adapters/outbound/filestore/path_policy.go:58`, `packages/runtime-go/internal/adapters/outbound/process/command_probe.go:1`, and `packages/runtime-go/internal/adapters/outbound/webfetch/client.go:1`. | Reasonix keeps boot/control/agent/event/tool/provider/plugin/serve separated under `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/boot/boot.go:1`, `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/control/controller.go:1`, `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/agent.go:1`, `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/tool/builtin/bash.go:1`, `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/provider/provider.go:1`, `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/plugin/plugin.go:1`, and `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/serve/serve.go:1`; analytix absorbs layering discipline, not Reasonix product routes. | OpenCode reinforces explicit model/task/session ownership through provider/model identity at `/Users/sun/Projects/_upstreams/opencode/packages/schema/src/model.ts:16` and task inheritance at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/tool/task.ts:160`; analytix maps that into domain/app seams instead of importing OpenCode runtime shape. | CodexDesktop-Rebuild is relevant only for plugin/package resource boundaries under `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/src/mac-arm64/plugins/openai-bundled/.agents/plugins/marketplace.json:6` and resource copy layout at `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/scripts/sync-upstream.js:5`; auth patching at `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/scripts/patch-plugin-auth.js:3` remains rejected. | P1 architecture risk: the old `internal/server/runtime_server.go` gravity well made future P0/P1 closure fixes easy to regress because HTTP routing, provider loop, tool catalog, durable store, sub-agent, terminal, MCP, and replay concerns shared one package boundary with no machine guard. | Absorb Reasonix layering and composition-root discipline; absorb OpenCode provider/model identity as pure domain contracts; absorb CodexDesktop resource-boundary lessons only. | Fixed for the current migration slice: architecture tests reject dependency backflow, app-layer OS/process imports, production upstream routes, production conformance leakage, G5 shadow/replay contracts are isolated under `packages/runtime-go/internal/conformance/g5_shadow.go:1`, and `runtime_server.go` growth above the wiring facade; domain/ports/app/adapters/runtimeapp exist as stable migration targets, runtimeapp production assembly no longer delegates to the server factory facade; high-risk server/store blocks are split in-package; endpoint/model execution, turn execution config resolution, cache prefix shape, provider history projection, pending tool-call restore/repair, step-limit precedence/resolution, recovery/failure shaping, provider retry/delay diagnostics, provider-progress event construction, loop guards, goal/todo closure/service orchestration, plan-mode create_plan target resolution/response shaping, turn start plan/record construction, completion/failure/tool-call-ready/gate request, continuation, and cancellation construction, tool contracts/builtin-goal-todo-plan-subagent-job-skill-user-input schema contracts, subagent request/profile/config/default/diagnostic/source/job-view/run-output/runtime-state/task-job-tool/service logic, MCP search config defaults/merge/nested discovery, web config defaults/domain-policy/fetch-limit parsing, runtime capabilities, foreground bash request validation/result shaping, terminal durable background-bash start/result shaping, terminal buffering, usage aggregation, HTTP/SSE shapes, provider/MCP/file/process/web boundaries, JSONL usage-index load/append/atomic rewrite, JSON map persistence, symlink-aware workspace/write-root/protected-path policy, and native read text/directory/search/code-index traversal are now outside the transitional server where safe. Full concrete route/outbound store extraction remains a follow-up and is not claimed by this row. | `packages/runtime-go/internal/architecture/architecture_test.go:1`; `packages/runtime-go/internal/app/loop/events_test.go:1`; `packages/runtime-go/internal/app/loop/prompts_test.go:1`; `packages/runtime-go/internal/app/loop/recovery_test.go:1`; `packages/runtime-go/internal/app/loop/step_limits_test.go:1`; `packages/runtime-go/internal/app/loop/tool_guards_test.go:1`; `packages/runtime-go/internal/app/goal/closure_test.go:1`; `packages/runtime-go/internal/app/goal/tool_service_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/goal_tools_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/builtin_tools_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/plan_tools_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/subagent_tools_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/user_input_test.go:1`; `packages/runtime-go/internal/app/plan/create_plan_test.go:1`; `packages/runtime-go/internal/app/model/provider_config_test.go:1`; `packages/runtime-go/internal/app/model/provider_history_test.go:1`; `packages/runtime-go/internal/app/model/pending_tool_test.go:1`; `packages/runtime-go/internal/app/runtimeinfo/capabilities_test.go:1`; `packages/runtime-go/internal/app/subagent/config_test.go:1`; `packages/runtime-go/internal/app/subagent/profile_test.go:1`; `packages/runtime-go/internal/app/subagent/job_view_test.go:1`; `packages/runtime-go/internal/app/subagent/output_test.go:1`; `packages/runtime-go/internal/app/subagent/runtime_state_test.go:1`; `packages/runtime-go/internal/app/subagent/task_job_service_test.go:1`; `packages/runtime-go/internal/app/turn/start_test.go:1`; `packages/runtime-go/internal/app/turn/completion_test.go:1`; `packages/runtime-go/internal/app/turn/failure_test.go:1`; `packages/runtime-go/internal/app/turn/gate_continuation_test.go:1`; `packages/runtime-go/internal/app/turn/gate_cancellation_test.go:1`; `packages/runtime-go/internal/app/turn/gate_requests_test.go:1`; `packages/runtime-go/internal/app/turn/tool_call_ready_test.go:1`; `packages/runtime-go/internal/app/terminal/bash_tool_request_test.go:1`; `packages/runtime-go/internal/app/terminal/bash_test.go:1`; `packages/runtime-go/internal/app/terminal/background_bash_test.go:1`; `packages/runtime-go/internal/adapters/outbound/filestore/jsonl_test.go:1`; `packages/runtime-go/internal/adapters/outbound/filestore/json_map_test.go:1`; `packages/runtime-go/internal/adapters/outbound/filestore/text_test.go:1`; `packages/runtime-go/internal/adapters/outbound/filestore/search_test.go:1`; `packages/runtime-go/internal/adapters/outbound/filestore/code_index_test.go:1`; `packages/runtime-go/internal/adapters/outbound/filestore/path_policy_test.go:1`; `cd packages/runtime-go && go test ./internal/architecture ./internal/domain/... ./internal/ports ./internal/app/... ./internal/adapters/... ./internal/runtimeapp`; `cd packages/runtime-go && go test ./internal/app/goal ./internal/app/toolcatalog ./internal/app/loop ./internal/app/runtimeinfo ./internal/app/subagent ./internal/app/terminal ./internal/server ./internal/architecture`; `cd packages/runtime-go && go test ./internal/adapters/outbound/filestore ./internal/app/subagent ./internal/app/terminal ./internal/server ./internal/architecture`; `cd packages/runtime-go && go test ./...`; `git diff --check` | fixed |
| Goal todo / complete_step / final-readiness closure | Go runtime now advertises goal/todo tools only for active-goal or explicit goal/todo-scoped prompts: schema contracts live at `packages/runtime-go/internal/app/toolcatalog/goal_tools.go:9`, active persisted goal/todo checks remain in the server adapter at `packages/runtime-go/internal/server/tool_catalog.go:85`, and prompt/tool-scope activation is pure app logic at `packages/runtime-go/internal/app/toolcatalog/routing.go:17`; this keeps ordinary non-goal prompts bounded even when they contain generic goal-language while moving the executable surface off the retired TypeScript agent runtime. `complete_step` still enters through the server wrapper at `packages/runtime-go/internal/server/goal_tools.go:20`, but executable FSM logic now lives in `packages/runtime-go/internal/app/goal/tool_service.go:76`: structured `verification` command receipts are checked through durable bash tool results at `packages/runtime-go/internal/app/goal/tool_service.go:274` and `packages/runtime-go/internal/app/goal/tool_service.go:305`, todo step identity is validated before evidence persistence at `packages/runtime-go/internal/app/goal/tool_service.go:110` and `packages/runtime-go/internal/app/goal/tool_service.go:401`, durable goal evidence is appended through `packages/runtime-go/internal/app/goal/tool_service.go:127`, `packages/runtime-go/internal/app/goal/state.go:103`, and `packages/runtime-go/internal/server/durable_goals_todos.go:59`, matching todos advance through `packages/runtime-go/internal/app/goal/tool_service.go:354`, and `goal_updated` / `todos_updated` replay remains wired by the server adapter at `packages/runtime-go/internal/server/goal_tools.go:48`. `update_goal` blocks completion without evidence, incomplete todos, or strict self-check readiness at `packages/runtime-go/internal/app/goal/tool_service.go:163`, `packages/runtime-go/internal/app/goal/tool_service.go:178`, and `packages/runtime-go/internal/app/goal/tool_service.go:184`; blocked-state repetition is bounded at `packages/runtime-go/internal/app/goal/tool_service.go:416`; the pure evidence-audit evaluator has been moved under the domain layer at `packages/runtime-go/internal/domain/goal/evidence.go:70` with the old `packages/runtime-go/internal/goal/goal_evidence.go:5` path retained as a compatibility re-export. The durable store still rejects direct terminal completion without evidence/todo/self-check readiness through `packages/runtime-go/internal/app/goal/state.go:88` and the adapter call at `packages/runtime-go/internal/server/durable_goals_todos.go:41`. `todo_write` normalization keeps one active item and rejects invalid statuses at `packages/runtime-go/internal/app/thread/todos.go:10` with the server adapter at `packages/runtime-go/internal/server/durable_goals_todos.go:124`. | Reasonix `complete_step` is the source of the evidence-backed sign-off contract: the tool name/schema and evidence kinds are at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/tool/builtin/completestep.go:44`, missing evidence is rejected at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/tool/builtin/completestep.go:109`, host evidence and todo matching run at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/tool/builtin/completestep.go:123`, and command receipt verification starts at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/tool/builtin/completestep.go:158`. Reasonix `todo_write` keeps structured progress state at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/tool/builtin/todo.go:28` and rejects newly completed todos without a matching complete_step receipt at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/tool/builtin/todo.go:95`. | OpenCode durable session error/assistant metadata remains the reference for keeping terminal tool-state failures replay-visible at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/message-v2.ts:603`; analytix keeps its product-owned goal/todo contract instead of importing OpenCode session todo UI. | Not applicable to goal FSM; CodexDesktop plugin resource layout does not define goal/todo execution semantics. | P0 before fix: after TS runtime retirement, Go had `/goal` and `/todos` HTTP state plus fixture `goal_evidence_audit`, but active agent turns did not own the executable `todo_write -> complete_step -> update_goal` readiness loop in Go, so a model could attempt completion without durable evidence, todo settlement, or Reasonix-style host receipt checks; a generic prompt containing the word `goal` could also over-advertise goal tools and erode direct-answer speed/cache behavior. | Absorb Reasonix complete_step/todo evidence discipline into Go runtime; keep OpenCode only as durable replay/error visibility reference; no CodexDesktop absorption. | Fixed with Go-native goal/todo tool schemas and app-layer executor service, durable evidence ledger append, structured evidence receipt verification for commands/paths, todo step matching before evidence persistence, todo auto-advance on sign-off, completion refusal until evidence/todos/self-check are ready, explicit-only prompt cueing for inactive goal tools, and SSE replay via existing `goal_updated` / `todos_updated` contracts. Product identity, `window.analytix`, provider/model execution refs, and normal non-goal tool-count scoping are preserved. | `packages/runtime-go/internal/app/goal/tool_service_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/goal_tools_test.go:1`; `packages/runtime-go/goal_evidence_contract_test.go:1`; `packages/runtime-go/runtime_server_test.go:499`; `packages/runtime-go/runtime_server_test.go:10762`; `packages/runtime-go/runtime_server_test.go:10813`; `packages/runtime-go/runtime_server_test.go:10899`; `go test ./internal/domain/goal ./internal/goal ./internal/app/goal ./internal/app/toolcatalog ./internal/server -run 'TestToolService(CompleteStepVerifiesHostEvidenceAndAdvancesTodo|StrictCompletionSelfCheckGate|BlockedGoalRequiresRepeatedSameReason)|TestGoalAndTodoToolSchemasExposeEvidenceClosureContract|TestRuntimeServerDirectLightweightPromptsDoNotAdvertiseToolsToProvider|TestRuntimeServer(GoalToolsRequireEvidenceBeforeCompletion|GoalTodoCompleteStepAndFinalReadiness|CompleteStepRejectsMismatchedTodoIndexBeforeEvidence)' -count=1`; `go test ./...` | fixed |
| Latest upstream ref refresh / delta triage | After the latest upstream fast-forward, Reasonix `origin/main-v2` is `2ade3cd50d94e81ffb5f77d14b7de74082d06664`, OpenCode `origin/dev` is `6697cf3fd81d44fc8c3f72d32edb0e2549d24003`, and CodexDesktop-Rebuild `origin/master` is `0cb6bc8fba872d391666d6eed1dcd73b533fb2d3`. `scripts/runtime-closure-matrix-audit.mjs:1` now parses the matrix, verifies those refs against local `origin/*` without pulling or mutating upstream worktrees, and fails if any P0/P1 row starts with `needs_fix` or `blocked`, lacks a required matrix cell, lacks a `P0`/`P1` risk marker, lacks a concrete regression cell, drops coverage for any required runtime closure domain, lacks local source evidence, lacks any upstream source bucket citation, or cites a source file/line that does not exist. `scripts/runtime-go-product-regression.mjs:294` runs the same check in non-strict mode so product regression validates both matrix shape and ref freshness where local upstream refs are available. The script uses Analytix-owned upstream-repo override names such as `ANALYTIX_UPSTREAM_REPO_REASONIX`, after the product-sovereignty scan rejected the previous product-shaped override naming. Analytix keeps the model picker restricted to configured models at `src/main/upstream-models.ts:25`, applies live model probes only to concrete image/tool capabilities at `src/shared/app-settings-provider.ts:1023`, and requires reasoning protocol through explicit preset/user model profiles such as `src/shared/model-provider-presets.ts:134` instead of inferring it from an upstream catalog name. | Mandatory Reasonix anchors still cover the specified runtime/session/cancel/SSE files: stream recovery and grace loop at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/agent.go:35`, task subagent continuation at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/task.go:516`, persisted `SubagentStore` continuation/fork at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/subagent_store.go:232`, ACP session cancel at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/acp/service.go:119`, controller cancel/session lifecycle at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/control/controller.go:1268`, and serve SSE keepalive/cancel at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/serve/serve.go:346`. The `ebd73457..2ade3cd5` delta adds desktop titlebar/session-save/topic/registry features and fixes bash cancel wait cleanup in `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/tool/builtin/bash.go`; analytix already covers shell/background cancellation through Go runtime shutdown/background job tests and keeps Reasonix desktop/registry UI as non-absorbed product surface. | Mandatory OpenCode anchors still cover the specified provider/model/task files: `Model.Ref` provider/model/variant binding at `/Users/sun/Projects/_upstreams/opencode/packages/schema/src/model.ts:16`, session prompt model inheritance and hard provider lookup at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/prompt.ts:1493`, durable assistant/tool interrupted history repair at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/message-v2.ts:605`, and task tool child session provider/model inheritance at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/tool/task.ts:160`. The `f014686..6697cf3` fast-forward changes app/session UI model dialogs, prompt input, markdown timeline spacing, and generated UI code, but does not change mandatory provider/model/task inheritance files; those UI deltas are acceptable_by_contract for analytix runtime closure. | CodexDesktop `origin/master` remains `0cb6bc8fba872d391666d6eed1dcd73b533fb2d3`; audited plugin/resource paths under the bundled plugin tree did not change. Marketplace local plugin paths/policies remain visible at `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/src/mac-arm64/plugins/openai-bundled/.agents/plugins/marketplace.json:6`, plugin manifest skills/interface layout at `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/src/mac-arm64/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json:19`, upstream resource copy layout at `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/scripts/sync-upstream.js:5`, and the rejected auth-bypass patch starts at `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/scripts/patch-plugin-auth.js:3`. Only fetch/ref inspection was used for latest refs. | P1 evidence risk: stale upstream hashes could make the absorption matrix unverifiable; blindly importing Reasonix desktop/product registry state, OpenCode app/session UI deltas, or OpenAI catalog-name reasoning inference would violate analytix product/provider contracts; env override names must not look like product identity or runtime identity; a row could drift back to open P0/P1 status, vague regression evidence, partial coverage, stale source-line evidence, incomplete matrix fields, uncited upstream buckets, regression cells without a test source citation, or a missing user-mandated upstream source file. | Refresh refs; absorb only the bash cancel cleanup lesson as already-covered analytix shell/background cancel evidence and the desktop-state/MCP-diagnostic lesson as already-covered project/workspace and MCP diagnostics evidence, not Reasonix product shell/registry state; treat latest OpenCode app/session UI deltas and OpenAI reasoning-variant catalog inference as acceptable_by_contract because analytix reasoning protocol is explicit profile data and UI shell is analytix-owned; absorb CodexDesktop resource layout/manifest/packaging boundaries while explicitly rejecting the auth-bypass patch. | Updated the upstream refs and recorded the latest delta decision; machine checks keep stale refs, open statuses, missing/vague matrix cells, missing domain coverage, missing upstream source buckets, missing user-mandated upstream source files, missing regression source citations, and broken source citations failing fast locally and product-regression catches malformed/stale matrix refs where local upstream refs are available. No runtime code change is required for the latest Reasonix desktop/registry delta, latest OpenCode UI delta, or CodexDesktop unchanged resource delta; configured provider/model/profile contracts already preserve multi-provider behavior, shell/background cancel contracts already cover the Reasonix bash cleanup class, adding hidden Reasonix product state or implicit catalog-name reasoning would be misaligned with the product baseline, and CodexDesktop auth bypass remains explicitly rejected. | `scripts/runtime-closure-matrix-audit.mjs:798`; `git -C /Users/sun/Projects/_upstreams/DeepSeek-Reasonix fetch origin main-v2`; `git -C /Users/sun/Projects/_upstreams/opencode fetch origin dev`; `git -C /Users/sun/Projects/_upstreams/CodexDesktop-Rebuild ls-remote origin refs/heads/master`; `git -C /Users/sun/Projects/_upstreams/DeepSeek-Reasonix diff --stat ebd73457da2c622f856ef370dc166c1db0badf08..origin/main-v2 -- internal/agent/agent.go internal/agent/task.go internal/agent/subagent_store.go internal/acp/service.go internal/control/controller.go internal/control/turn_orchestrator.go internal/serve/serve.go internal/provider/openai internal/provider/provider.go internal/provider/retry.go internal/plugin/lazy.go internal/plugin/cache.go internal/plugin/plugin.go internal/boot/boot.go`; `git -C /Users/sun/Projects/_upstreams/opencode diff --stat f014686b07f6f14c01cf090c3c97bcb931d57cf9..origin/dev -- packages/schema/src/model.ts packages/opencode/src/session/prompt.ts packages/opencode/src/session/message-v2.ts packages/opencode/src/tool/task.ts packages/opencode/src/tool packages/opencode/src/provider packages/opencode/src/session`; `node scripts/runtime-closure-matrix-audit.mjs --strict-local --json`; `npm run runtime:go:product-regression -- --json`; `git diff --check` | fixed |
| SSE/replay setup error contract | `packages/runtime/src/server-test-support/routes/events.ts:37` keeps a per-stream high-water seq; `packages/runtime/src/server-test-support/routes/events.ts:103` emits a structured `kind:"error"` runtime event with `threadId`, `seq`, `timestamp`, `code`, `severity`, and `terminal:true`; `packages/runtime/src/contracts/events.ts:466` declares terminal/fatal/recoverable flags for error events. Go runtime now matches that contract on both replay setup and live setup: `packages/runtime-go/internal/adapters/inbound/sse/events.go:36` converts replay `highestSeq`/load failures into SSE error events, `packages/runtime-go/internal/adapters/inbound/sse/events.go:97` opens live streams with a `: connected` comment before replay setup, `packages/runtime-go/internal/adapters/inbound/sse/events.go:107` emits terminal `sse_setup_error` SSE events instead of returning JSON 400 when live setup fails, and `packages/runtime-go/internal/server/routes.go:135` only injects durable store functions into the inbound SSE adapter. Main IPC stops reconnecting after terminal runtime error SSE frames at `src/main/runtime-sse-ipc.ts:112` / `src/main/runtime-sse-ipc.ts:367`, and renderer provider dispatch keeps setup errors visible even when their `seq` equals the subscription cursor at `src/renderer/src/agent/analytix-runtime.test.ts:1861`. | Reasonix `internal/serve/serve.go` uses early SSE connection/keepalive patterns; analytix keeps its own event schema instead of adopting Reasonix protocol. | Not the main source for SSE shape. | Not applicable; desktop connector bridge remains analytix-owned. | P0 before fix: setup failure could reach renderer as a bare `event:error` / HTTP setup failure without replay/terminal contract, be missed if a cursor-equal setup error was treated like a replayed delta, or loop as a reconnect storm when the terminal setup error closed the stream without advancing `since_seq`. | Equivalent rewrite. | Fixed in TS runtime SSE route, Go runtime `/events`, main SSE IPC, and renderer provider dispatch without changing product identity or IPC shape; setup failure stays on the SSE channel with `code:"sse_setup_error"` and `terminal:true`, terminal runtime error frames stop reconnection, and only assistant deltas are floor-deduped. | `packages/runtime/tests/events-route.test.ts:13`; `packages/runtime-go/internal/adapters/inbound/sse/events_test.go:13`; `packages/runtime-go/internal/server/durable_store_test.go:1055`; `packages/runtime-go/internal/server/durable_store_test.go:1110`; `src/main/runtime-sse-ipc.test.ts:354`; `src/renderer/src/agent/analytix-runtime.test.ts:1861`; `npx vitest run packages/runtime/tests/events-route.test.ts packages/runtime/tests/contracts.test.ts src/renderer/src/agent/analytix-mapper.test.ts src/renderer/src/agent/analytix-runtime.test.ts`; `npm run test -- --run src/main/runtime-sse-ipc.test.ts src/renderer/src/agent/analytix-runtime.test.ts`; `go test ./internal/adapters/inbound/sse ./internal/server -run 'Test(ThreadEventsHandler|RuntimeThreadEvents)(LiveSetupErrorUsesStructuredSSE|ReplaySetupErrorUsesStructuredSSE|LiveAndReplayIncludePendingBeforePersistence|WritesSetupErrorOnReplayFailure|ReplaysDurableEvents)' -count=1` | fixed |
| Renderer terminal handling for runtime error events | `src/renderer/src/agent/analytix-mapper.ts:1161` treats top-level `terminal` or `fatal` error events as terminal; `src/renderer/src/agent/analytix-mapper.ts:1831` routes `kind:"error"` to visible runtime error callbacks. | Reasonix stream recovery is a reference for fatal stream closure, but renderer UI remains analytix-specific. | Not applicable. | Not applicable. | P1: terminal event shape must align across runtime and renderer. | Already present; contract widened above. | No renderer code change needed for this slice. | `packages/runtime/tests/events-route.test.ts:13`; `npx vitest run packages/runtime/tests/events-route.test.ts packages/runtime/tests/contracts.test.ts src/renderer/src/agent/analytix-mapper.test.ts src/renderer/src/agent/analytix-runtime.test.ts` | fixed |
| Go runtime failure record durability | `packages/runtime-go/internal/app/turn/failure.go:1` now owns failed-turn error item and `turn_failed` event construction, while `packages/runtime-go/internal/server/turn_terminal.go:13` limits server work to loading replay events, terminal CAS, and durable event recording. `packages/runtime-go/internal/server/durable_turns.go:156` adds `FailTurnIfActive`, and `packages/runtime-go/internal/server/durable_turns.go:204` delegates terminal failed status plus terminal items into one thread update through `packages/runtime-go/internal/app/turn/terminal_update.go:26`, with item settlement delegated at `packages/runtime-go/internal/app/turn/settlement.go:12`. Sync/async failures log diagnostics at `packages/runtime-go/internal/server/turn_start.go:200` and `packages/runtime-go/internal/server/turn_start.go:214`. | Reasonix finalization under `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/agent.go:1` treats failure state as durable session state before stream recovery/consumers. | OpenCode durable assistant metadata at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/message-v2.ts:605` is the reference for reload-visible failure state. | Not applicable. | P0 before fix: a failure could mark the turn failed and then silently lose the visible error item / `turn_failed` event if event recording failed. | Equivalent rewrite plus app-layer extraction. | Fixed by atomically persisting failed turn + error item in `thread.json`, returning joined event-record diagnostics, logging sync/async failures, and moving failure item/event shaping into `internal/app/turn` so the terminal contract is regression-tested outside the server facade. | `packages/runtime-go/internal/app/turn/failure_test.go:1`; `packages/runtime-go/internal/server/durable_store_test.go:937`; `go test ./internal/app/turn ./internal/server -run 'Test(BuildFailureRecord|RuntimeTurnFailurePersistsErrorItemWhenEventLogCannotRecord)' -count=1`; `go test ./internal/server -count=1`; `go test ./...`; `npm run runtime:go:product-regression -- --json` | fixed |
| Go runtime partial assistant salvage | `packages/runtime-go/internal/app/turn/assistant_delta.go:1` materializes assistant text/reasoning deltas, `packages/runtime-go/internal/app/turn/failure.go:40` includes that salvage before the terminal error item on failed turns, and `packages/runtime-go/internal/server/turn_terminal.go:57` reuses the same app-layer materializer for non-discarded interrupts. Interrupts preserve partial assistant output when `discard` is false at `packages/runtime-go/internal/server/turn_routes.go:75`; terminal CAS plus appended terminal items remain at `packages/runtime-go/internal/server/durable_turns.go:204` and `packages/runtime-go/internal/app/turn/terminal_update.go:91`. | Reasonix stream recovery/grace handling under `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/agent.go:35` preserves visible partial progress before finalization. | OpenCode interrupted assistant finalize at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/message-v2.ts:605` is the reference for durable assistant metadata rather than live-only deltas. | Not applicable. | P0 before fix: assistant deltas were replayable from `events.jsonl` but not materialized into `thread.json`, so a failed/reloaded turn could show the terminal error while losing visible partial assistant output. | Equivalent rewrite plus app-layer extraction. | Fixed by materializing assistant deltas on failed turns and non-discarded interrupts, then emitting terminal `item_completed` events for replay convergence; failure-shaping now lives in app code, leaving server as a store/event coordinator. | `packages/runtime-go/internal/app/turn/assistant_delta_test.go:1`; `packages/runtime-go/internal/app/turn/failure_test.go:1`; `packages/runtime-go/internal/server/durable_store_test.go:1009`; `go test ./internal/app/turn ./internal/server -run 'Test(MaterializeAssistantDeltaItems|BuildFailureRecord|RuntimeTurnFailureMaterializesAssistantDeltas)' -count=1`; `go test ./...` | fixed |
| Go interrupted-stream recovery depth | Analytix defines the bounded recovery contract in app loop: `packages/runtime-go/internal/app/loop/tool_guards.go:13` sets `MaxInterruptedStreamRecoveries = 3`, `packages/runtime-go/internal/app/loop/recovery.go:39` rejects explicit cancellation and classifies transient interrupted streams, `packages/runtime-go/internal/app/loop/recovery.go:61` builds the tail prompt, `packages/runtime-go/internal/app/loop/events.go:93` shapes replay-visible `interrupted_stream` diagnostics, and `packages/runtime-go/internal/server/agent_loop.go:236` applies the bound while `packages/runtime-go/internal/app/loop/event_recorder.go:45` records the app-shaped recovery event and `packages/runtime-go/internal/server/agent_loop.go:248` keeps recovery attempts from consuming `maxModelSteps`. | Reasonix uses `maxStreamRecoveries = 3` at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/agent.go:35`, emits retry events with the same max at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/agent.go:912`, and decrements the loop step so recovery retries do not consume the step budget at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/agent.go:913`. | OpenCode interrupted assistant finalize remains the durable-state reference; this row is about streamed provider recovery depth. | Not applicable. | P1 before fix: analytix had the same recovery prompt and step-budget behavior as Reasonix but only one post-output interrupted-stream recovery, making transient network/provider stream cuts easier to surface as terminal provider errors. | Absorb Reasonix bounded recovery depth exactly, without unbounded retry or provider fallback. | Fixed by raising the Go runtime interrupted-stream recovery depth to three attempts, moving recovery classification/prompt/event shaping into `internal/app/loop`, and expanding the post-output stream test so two visible partial stream cuts recover under `maxModelSteps:1`, preserving partial output and recording attempts `1/3` and `2/3`. | `packages/runtime-go/internal/app/loop/events_test.go:1`; `packages/runtime-go/internal/app/loop/recovery_test.go:1`; `packages/runtime-go/runtime_server_test.go:4820`; `go test . -run TestRuntimeServerPostOutputProviderInterruptionRecoversWithTailPrompt -count=1`; `go test . -run 'TestRuntimeServer(PostOutputProviderInterruptionRecoversWithTailPrompt|PartialToolCallInterruptionRecoversWithoutExecutingPartialTool)' -count=1`; `go test ./internal/app/loop -run 'Test(BuildStreamInterruptedRecoveryEvent|InterruptedStreamRecovery)' -count=1` | fixed |
| Direct/lightweight prompt tool-free provider request | Go runtime direct/lightweight routing now reaches the provider without advertising tool schemas for `你是什么大模型`, `你是什么模型`, `你好`, `这个问题怎么理解`, and `简单总结一下`. `packages/runtime-go/runtime_server_test.go:456` creates live runtime thread/turns for those prompts, captures provider request bodies, asserts there is no top-level `tools` field, still requires the analytix provider/model identity guard, and verifies SSE replay persists `cacheDiagnostics.route:"direct_answer"`, `toolCount:0`, `firstTokenLatencyMs`, and `durationMs`. The lower-level schema selector locks the same prompt set at `packages/runtime-go/internal/server/mcp_tool_schema_test.go:195`, while `packages/runtime-go/internal/server/tool_catalog.go:143` keeps the route classifier tool-free only when there is no tool scope/subagent/plan state and no agent-work cue. Go records the timing fields in provider diagnostics through `packages/runtime-go/internal/server/turn_finalize.go:11` and `packages/runtime-go/internal/adapters/outbound/provider/client/client.go:253`. The legacy TypeScript loop mirrors the lightweight classifier at `packages/runtime/src/loop-test-support/agent-loop.ts:461`, writes route/tool count/timing into cache diagnostics via `packages/runtime/src/cache/prefix-cache-diagnostics.ts:26`, covers direct/agent route diagnostics at `packages/runtime/tests/loop.test.ts:178` and `packages/runtime/tests/cache.test.ts:327`, and the renderer mapper preserves these fields in the usage snapshot at `src/renderer/src/agent/analytix-mapper.test.ts:2226`. `scripts/runtime-go-speed-cache-gate.mjs:31` now runs the direct/work-prompt Go contract, and `scripts/runtime-go-speed-cache-gate.mjs:312` exposes it as the `direct_answer_tool_free` gate metric. | Reasonix model identity/grace handling is the reference for answering lightweight prompts directly instead of routing through unrelated tools. | OpenCode provider/model identity is the reference for preserving execution identity in prompts; this row is not a task inheritance change. | Not applicable. | P1 before guard: identity or short ordinary questions could receive a broad tool catalog because over-wide cues such as `项目`, `分析`, and `问题` pushed normal language into the agent work path, hurting latency/cache and weakening the provider/model identity answer contract; the route decision and request timing were also not durable/replay-visible as cache diagnostics. | Equivalent analytix runtime/provider request guard across Go default and legacy TypeScript loops, plus durable prompt-shape/timing diagnostics. | Fixed by routing direct identity, greetings, short questions, and scoped lightweight Chinese prompts through the tool-free profile while preserving the system prompt that states `provider=<id>`, `model=<id>`, and the vendor identity guard; Go and TS both keep tools for work prompts such as `请分析项目代码并使用 docs`, and usage events now expose provider-safe `route`/`toolCount`/`firstTokenLatencyMs`/`durationMs` without raw prompt/system/tool text. The speed/cache gate now fails if the Go direct-answer/work-prompt fixture stops proving tool-free direct prompts and scoped work prompts. | `packages/runtime-go/runtime_server_test.go:456`; `packages/runtime-go/internal/provider/provider_test.go:1748`; `packages/runtime-go/internal/server/mcp_tool_schema_test.go:195`; `packages/runtime/tests/loop.test.ts:178`; `packages/runtime/tests/cache.test.ts:327`; `src/renderer/src/agent/analytix-mapper.test.ts:2226`; `scripts/runtime-go-speed-cache-gate.mjs:31`; `scripts/runtime-go-speed-cache-gate.mjs:312`; `go test . -run TestRuntimeServerDirectLightweightPromptsDoNotAdvertiseToolsToProvider -count=1`; `go test ./internal/provider -run 'TestRuntimeCacheDiagnosticsComparesPreviousPrefixShape|TestRuntimeProviderConfigSetValidateExecutionModelRejectsBoundedProviderMismatch' -count=1`; `go test ./internal/server -run 'TestRuntimeToolSchemasForPrompt(ScopesMCPAutoSafety|KeepsAgentToolsForWorkPrompts)|TestRuntimeToolSchemasPinMentionedComputerUseServer' -count=1`; `npm run --prefix packages/runtime test -- tests/cache.test.ts tests/loop.test.ts --run`; `npm run test -- --run src/renderer/src/agent/analytix-mapper.test.ts`; `npm run runtime:go:speed-cache-gate` | fixed |
| Provider/model inheritance | `packages/runtime/src/contracts/model-execution-ref.ts:4` now removes the soft fallback source and requires `providerId` on every `ModelExecutionRef`; `src/renderer/src/agent/analytix-contract.ts:50` mirrors the provider-bound execution tuple for renderer/runtime JSON. `packages/runtime/src/delegation-test-support/delegation-runtime.ts:276` rejects child runs before record creation when the resolved provider is missing, `packages/runtime/src/delegation-test-support/delegation-runtime.ts:771` records missing inherited/default source as `runtime-default` rather than `fallback`, `packages/runtime/src/delegation-test-support/child-agent-executor.ts:93` requires a provider-bound `modelExecution`, and `packages/runtime/src/delegation-test-support/child-agent-executor.ts:96` throws `provider_not_found` when the direct executor boundary is bypassed without an inherited provider. | Reasonix subagent runtime inherits provider unless explicitly overridden and treats incompatible continuation as hard failure. | OpenCode `Model.Ref` requires provider/model binding and task inheritance passes the tuple through. | Not applicable. | P0 before fix: child/subagent paths could silently drift to default provider/model if inheritance failed, a direct executor was called outside `DelegationRuntime`, or a shared execution ref represented default fallback without a concrete provider. | Equivalent hard-fail on runtime path and direct executor boundary. | Fixed the main TS subagent runtime path and direct `ChildAgentExecutor` path to require a concrete provider/model execution ref before creating child state, and retired `source:"fallback"` / `fallbackReason` from the shared execution-ref contract. | `packages/runtime/tests/contracts.test.ts:334`; `packages/runtime/tests/delegation-runtime.test.ts:539`; `packages/runtime/tests/child-agent-executor.test.ts:422`; `packages/runtime/tests/child-agent-executor.test.ts:432`; `npm run --prefix packages/runtime test -- tests/contracts.test.ts tests/loop.test.ts tests/child-agent-executor.test.ts tests/delegation-runtime.test.ts tests/thread-summary-route.test.ts --run` | fixed |
| Go explicit provider hard-fail | `packages/runtime-go/internal/server/turn_start.go:121` rejects non-empty `providerId` values that are not present in the runtime provider catalog/default, and `packages/runtime-go/internal/app/model/provider_config.go:150` centralizes the `HasProvider` check while the provider facade at `packages/runtime-go/internal/provider/provider.go:119` preserves the legacy no-catalog default behavior. | Reasonix treats provider/session identity mismatch as an execution error instead of silently swapping providers. | OpenCode `Model.Ref` keeps provider/model as a required pair and fails when the provider cannot be resolved. | Not applicable. | P0 before fix: a Go runtime turn with an explicit missing provider could route to the configured/default provider, hiding model/provider drift in subagent or resume paths. | Equivalent hard-fail. | Added provider catalog membership validation before `TurnConfig`, returning visible `provider_not_found` and preventing fallback provider calls. | `packages/runtime-go/runtime_server_test.go:5113`; `packages/runtime-go/internal/provider/provider_test.go:2039`; `packages/runtime-go/internal/app/model/provider_config_test.go:1`; `go test . -run 'TestRuntimeServer(DefaultContractCoversRendererBaselineEndpoints|RejectsMissingExplicitProviderWithoutDefaultFallback)' -count=1`; `go test ./internal/provider ./internal/app/model -run 'TestRuntimeProviderConfig|TestRuntimeProviderConfigSet' -count=1`; `go test ./...` | fixed |
| Route-aware heavy tool scoping | Go runtime now classifies provider turns as `direct_answer`, `light_agent`, `tool_agent`, or `subagent_agent` before building provider-visible schemas at `packages/runtime-go/internal/app/toolcatalog/routing.go:69`, uses that route to withhold subagent/job tools unless the prompt or explicit tool scope asks for them at `packages/runtime-go/internal/app/toolcatalog/materialize.go:54`, withholds `run_skill` unless the prompt/tool scope names a skill at `packages/runtime-go/internal/app/toolcatalog/materialize.go:58`, and recalculates cache diagnostics from the advertised tool set at `packages/runtime-go/internal/server/agent_loop.go:265` / `packages/runtime-go/internal/app/toolcatalog/diagnostics.go:169`. The legacy TypeScript loop mirrors the provider-visible filter at `packages/runtime/src/loop-test-support/agent-loop.ts:509`, `packages/runtime/src/loop-test-support/agent-loop.ts:521`, and `packages/runtime/src/loop-test-support/agent-loop.ts:1491`, then turns the actual advertised set into the active execution allow-list at `packages/runtime/src/loop-test-support/agent-loop.ts:713`, `packages/runtime/src/loop-test-support/agent-loop.ts:1497`, `packages/runtime/src/loop-test-support/agent-loop.ts:1994`, and `packages/runtime/src/loop-test-support/agent-loop.ts:2112`; intentional route changes are separated from real tool-catalog drift at `packages/runtime/src/loop-test-support/agent-loop.ts:3073`. | Reasonix `LazyToolset` registers cached placeholder tools and swaps real plugin tools on demand at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/plugin/lazy.go:336`, while its subagent boundary excludes recursive agent/skill/job tools at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/task.go:60`; analytix absorbs the route-scoped surface and keeps its own MCP/search/runtime config. | OpenCode builds tools from the current provider/model policy at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/tools.ts:89` and keeps skills as progressively loaded task-specific instructions at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/system.ts:102`; analytix keeps the same principle while preserving its runtime tool host and ModelExecutionRef boundary. | CodexDesktop plugin manifests expose skills/interface metadata such as `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/src/mac-arm64/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json:19`; analytix absorbs metadata/runtime-boundary lessons but not auth bypass. | P1 before fix: after direct-answer gating, ordinary code/MCP work prompts could still advertise or execute child-agent/job and skill tools by default, bloating prompt/cache shape, letting forged hidden tool calls start subagents, and producing noisy catalog-drift diagnostics when normal prompts moved between direct/tool routes. | Equivalent route-scoped rewrite, not a product UI import. | Fixed Go and TS provider-visible tool selection so ordinary work prompts keep builtin investigation and scoped MCP tools, but hide `delegate_task` / `task` / `parallel_tasks` / job controls and skill loaders until the prompt/tool scope explicitly names subagent/delegate/child/parallel/background or skill intent. Explicit subagent and skill prompts still expose the original tools, preserving durable child sessions and skill execution. TS execution now clamps `ToolHostContext.allowedToolNames` to the advertised set so forged hidden `delegate_task` calls become paired error `tool_result` items instead of child execution, and route-aware catalog snapshots no longer misreport direct-to-work prompt changes as drift. | `packages/runtime-go/internal/server/mcp_tool_schema_test.go:256`; `packages/runtime-go/runtime_server_test.go:520`; `packages/runtime/tests/loop.test.ts:259`; `packages/runtime/tests/loop.test.ts:350`; `packages/runtime/tests/loop.test.ts:418`; `go test ./internal/server -run 'TestRuntimeToolSchemasForPrompt(KeepsAgentToolsForWorkPrompts|ScopesMCPAutoSafety|PinMentionedComputerUseServer)' -count=1`; `go test . -run 'TestRuntimeServer(DirectLightweightPromptsDoNotAdvertiseToolsToProvider|WorkPromptDoesNotAdvertiseSubagentOrSkillToolsWithoutCue|ExecutesDelegateTaskWithDurableChildRun|ParallelTasksCreateDurableChildRuns)' -count=1`; `npm run --prefix packages/runtime test -- tests/loop.test.ts tests/cache.test.ts --run`; `npm run --prefix packages/runtime test -- tests/loop.test.ts --run -t "forged hidden delegation|intentional prompt route changes|scopes delegation and skill tools"` | fixed |
| Provider/model bounded-catalog preflight | Go runtime now rejects an explicit external model request before contacting the provider when the selected provider has a bounded `models` / `modelProfiles` catalog and the model does not match that provider. `packages/runtime-go/internal/server/turn_start.go:129` calls `ValidateExecutionModel` before building the execution config, `packages/runtime-go/internal/app/model/provider_config.go:164` implements the bounded-catalog check, and `packages/runtime-go/runtime_server_test.go:513` proves `providerId:"deepseek"` plus `model:"mimo-v2-pro"` returns `providerError.kind:"invalid_model"` with zero provider HTTP requests. The provider facade converts the app/model execution error to a redacted provider error at `packages/runtime-go/internal/provider/provider.go:123`. The legacy TypeScript multi-provider client performs the same preflight at `packages/runtime/src/model-test-support/model/multi-provider-model-client.ts:116` / `packages/runtime/src/model-test-support/model/multi-provider-model-client.ts:169`, with regression coverage at `packages/runtime/src/adapters/model/multi-provider-model-client.test.ts:299`. | Reasonix treats provider/session identity mismatch as an execution error instead of sending a mismatched model to the provider. | OpenCode `Model.Ref` keeps provider/model as an explicit tuple at `/Users/sun/Projects/_upstreams/opencode/packages/schema/src/model.ts:16`, and task execution inherits the tuple rather than resolving model id independently. | Not applicable; CodexDesktop plugin resource layout does not define provider/model execution semantics. | P0 before fix: DeepSeek could receive a non-DeepSeek model id such as `mimo-v2-pro`, producing confusing provider diagnostics, slower failures, and possible provider/model cache namespace drift. | Equivalent OpenCode/Reasonix-style provider-turn boundary for bounded providers, while leaving custom/unbounded providers usable. | Fixed by adding a non-network preflight that rejects mismatched model ids only when the selected provider exposes a bounded catalog. Internal child-run execution keeps the existing exact source identity path, and custom/unbounded provider configs are not blocked, preserving multi-provider extensibility. | `packages/runtime-go/internal/provider/provider_test.go:106`; `packages/runtime-go/internal/app/model/provider_config_test.go:1`; `packages/runtime-go/runtime_server_test.go:513`; `packages/runtime/src/adapters/model/multi-provider-model-client.test.ts:299`; `go test ./internal/provider ./internal/app/model -run TestRuntimeProviderConfigSetValidateExecutionModelRejectsBoundedProviderMismatch -count=1`; `go test . -run 'TestRuntimeServer(DirectLightweightPromptsDoNotAdvertiseToolsToProvider|RejectsProviderModelMismatchBeforeProviderRequest)' -count=1`; `npm run --prefix packages/runtime test -- src/adapters/model/multi-provider-model-client.test.ts --run` | fixed |
| Provider capability semantic probe | `src/main/provider-connection.ts:28` / `src/main/provider-connection.ts:29` now define color/shape aliases for deterministic image probes, `src/main/provider-connection.ts:561` no longer tells the model the expected `red square` answer in the probe prompt, and `src/main/provider-connection.ts:626` matches semantic image responses through the alias sets. | Not a Reasonix runtime-loop mechanism. | OpenCode provider/model gating is the reference for hard capability evidence, but the probe wording remains analytix-specific. | Not applicable. | P1 before fix: providers that correctly described the tiny red square as a red rectangle could be marked `semantic_failed`, causing false-negative vision/tool-result-image capability state and downstream provider selection confusion. | Equivalent provider-probe hardening. | Fixed by accepting semantically equivalent square/rectangle wording while removing prompt leakage of the expected answer, preserving the requirement that the model must inspect the pixels. | `src/main/provider-connection.test.ts:224`; `npm run test -- src/main/provider-connection.test.ts --run`; `npm run typecheck` | fixed |
| Subagent continue/fork session identity | TS durable child references are validated in `packages/runtime/src/tool-test-support/tool/task-job-tool-provider.ts:452`, `packages/runtime/src/delegation-test-support/delegation-runtime.ts:276`, and `packages/runtime/tests/task-job-orchestration-contract.test.ts:715`. Go runtime has a native child-run store at `packages/runtime-go/internal/jobs/lineage.go:171`, locks source refs at `packages/runtime-go/internal/jobs/lineage.go:142`, cleans stale running child jobs at `packages/runtime-go/internal/jobs/lineage.go:370`, resolves provider/model/endpoint/variant before child creation at `packages/runtime-go/internal/server/subagent_jobs.go:170`, persists `ModelExecution` into job records at `packages/runtime-go/internal/jobs/lineage.go:220`, validates source provider/model/endpoint/variant drift at `packages/runtime-go/internal/server/subagent_jobs.go:236`, reuses the source child thread for `continue_from` and forks it for `fork_from` at `packages/runtime-go/internal/server/subagent_jobs.go:432`, passes the recorded endpoint into the app-layer child turn request at `packages/runtime-go/internal/app/subagent/turn_completion.go:51`, and replays child execution metadata through SSE at `packages/runtime-go/internal/app/subagent/output.go:60`. Go tests cover durable transcript continue/fork, source model inheritance when the parent changes, concurrent continue locking, ancestor fork, tool-scope drift rejection, stale interrupted child cleanup, cross-parent rejection, profile provider/model execution refs, source execution-drift rejection, and app-layer child turn completion at `packages/runtime-go/runtime_server_test.go:8067`, `packages/runtime-go/runtime_server_test.go:8472`, `packages/runtime-go/runtime_server_test.go:8570`, `packages/runtime-go/runtime_server_test.go:8650`, `packages/runtime-go/runtime_server_test.go:8724`, `packages/runtime-go/runtime_server_test.go:8808`, `packages/runtime-go/runtime_server_test.go:8881`, `packages/runtime-go/runtime_server_test.go:8967`, `packages/runtime-go/internal/server/subagent_source_test.go:29`, and `packages/runtime-go/internal/app/subagent/turn_completion_test.go:13`. | Reasonix `SubagentStore` / `PrepareContinue` / `RunSubAgentWithSession` are the comparison baseline. Analytix implements the same safety properties with `jobs.Manager` plus side child threads instead of importing the Reasonix session file layout. | OpenCode task provider inheritance is absorbed as explicit provider/model hard-fail and source child identity inheritance, including a durable `providerId`/`modelId`/`variant` execution ref. | Not applicable. | P0 if implementation falls back to prompt transcript injection, loses source provider/model/endpoint/variant, or permits cross-parent/stale child continuation. | Equivalent native implementation plus TS parity; no need to copy Reasonix storage layout. | Marked closed by source/test evidence: TS direct executor and delegation paths hard-fail missing/fallback provider; Go child runs are durable job records backed by child threads, not plain prompt injection. `continue_from` reuses the child thread, `fork_from` forks it, source status/thread/workspace/provider/model/endpoint/variant/effort/tool-scope/schema/persona are validated, profile execution refs persist to `/runtime/tools`, thread summary, and SSE replay, and stale running child jobs are marked interrupted on startup. | `packages/runtime-go/runtime_server_test.go:8067`; `packages/runtime-go/internal/server/subagent_source_test.go:29`; `packages/runtime-go/internal/app/subagent/turn_completion_test.go:13`; `packages/runtime-go/runtime_server_test.go:8472`; `packages/runtime-go/runtime_server_test.go:8570`; `packages/runtime-go/runtime_server_test.go:8650`; `packages/runtime-go/runtime_server_test.go:8724`; `packages/runtime-go/runtime_server_test.go:8808`; `packages/runtime-go/runtime_server_test.go:8881`; `packages/runtime-go/runtime_server_test.go:8967`; `packages/runtime/tests/task-job-orchestration-contract.test.ts:715`; `go test ./internal/app/subagent ./internal/server ./internal/architecture`; `go test . -run 'TestRuntimeServer(SubagentProfileCarriesProviderModelExecutionRef|SubagentContinueInheritsSourceIdentityWhenParentModelChanges|SubagentInheritsLatestParentThreadModel|ExplicitTurnModelBecomesThreadCurrentModel)' -count=1`; `go test ./internal/server -run 'TestRuntimeServerRejects(SubagentContinueSourceExecutionDrift|NonSubagentContinueSource)' -count=1`; `go test ./internal/server ./internal/provider ./internal/jobs ./cmd/runtime-server`; `go test ./...`; `npm run test -- packages/runtime/tests/child-agent-executor.test.ts packages/runtime/tests/delegation-runtime.test.ts packages/runtime/tests/task-job-orchestration-contract.test.ts --run` | fixed |
| Plugin manifest / MCP / skill runtime boundary | `src/main/services/hub-agent-marketplace-service.ts:917` keeps cached manifest discovery inside Analytix-owned marketplace roots; `src/main/analytix-process.ts:688` / `src/main/analytix-process.ts:707` / `src/main/analytix-process.ts:713` now resolve installed plugin MCP manifest files, cwd, commands, and relative args through plugin-dir containment; `src/main/services/skill-service.ts:158` / `src/main/services/skill-service.ts:358` only scan `~/.analytix/plugins/cache` for plugin skill roots; `packages/runtime/src/skills/skill-runtime.ts:368` / `packages/runtime/src/skills/skill-runtime.ts:381` contain `skill.json` entry/assets inside the skill package root. Apps/resources are marketplace-visible metadata/connector hints via `src/main/services/hub-agent-marketplace-service.ts:1105` and `src/renderer/src/components/PluginMarketplaceView.tsx:903`, but are not runtime-registered MCP resources. | Reasonix MCP lifecycle remains a runtime-tooling reference. | Not applicable. | CodexDesktop bundled `.codex-plugin/plugin.json`, `.mcp.json`, skills/apps manifests are positive references; auth bypass remains rejected. | P1 before fix: installed plugin MCP paths, skill entry/assets, or system Codex plugin cache fallback could cross the Analytix runtime boundary. Apps/resource execution remains explicitly not absorbed in this slice. | Equivalent containment plus explicit non-absorption of unsafe/broader app-resource registration. | Fixed plugin MCP and skill containment; removed default system `~/.codex/plugins/cache` discovery from Hub/skill paths. CodexDesktop auth bypass is rejected. Runtime apps/resources registration is `acceptable_by_contract`: marketplace keeps the metadata visible and marks connector requirement, while runtime config sync ignores apps/resources and clears stale plugin-managed MCP entries instead of executing connector/resource side effects. | `src/main/analytix-process.test.ts:1315`; `src/main/analytix-process.test.ts:1370`; `src/main/services/hub-agent-marketplace-service.test.ts:113`; `src/main/services/hub-agent-marketplace-service.test.ts:166`; `src/main/services/skill-service.test.ts:256`; `packages/runtime/tests/skill-runtime.test.ts:90`; `npm run test -- src/main/services/hub-agent-marketplace-service.test.ts src/main/analytix-process.test.ts --run`; `npm run test -- src/main/analytix-process.test.ts src/main/services/hub-agent-marketplace-service.test.ts src/main/services/skill-service.test.ts packages/runtime/tests/skill-runtime.test.ts --run` | fixed |
| MCP tool catalog / schema cache stability | Go MCP input schemas are canonicalized before cache/store use through the outbound protocol adapter at `packages/runtime-go/internal/adapters/outbound/mcp/protocol/protocol.go:264` and the compatibility wrapper at `packages/runtime-go/internal/mcp/manager.go:1092`, provider-visible MCP tools read their cached input schema through `packages/runtime-go/internal/server/tool_catalog.go:36`, runtime tool schema identity hashing canonicalizes tool parameters in the app-layer catalog service at `packages/runtime-go/internal/app/toolcatalog/service.go:33`, and stale cached MCP catalog/schema misses become explicit `mcp_cached_schema_stale` failures at `packages/runtime-go/internal/mcp/manager.go:455`. The read-only approval contract uses configured `ToolReadOnlyHint` and preserves paired blocked results for unknown/mutating tools at `packages/runtime-go/internal/server/tool_catalog.go:130` / `packages/runtime-go/internal/app/toolcatalog/diagnostics.go:158`, so dynamic plugin/MCP tool catalog drift cannot silently change cache prefix, tool schema pairing, or approval behavior. | Reasonix lazy plugin/tool catalog stability is a reference for treating tool schemas as runtime identity, but analytix keeps its Go MCP manager/cache. | OpenCode durable tool pairing remains the comparison for not feeding invalid tool histories. | CodexDesktop plugin resource layout is a packaging reference only; no auth bypass is absorbed. | P1: dynamic plugin/MCP tool catalog or schema drift could invalidate prompt-cache assumptions, break provider tool pairing, or let stale/missing MCP tools appear callable after reconnect. | Equivalent cache/schema guard inside analytix runtime. | Closed by canonical MCP schema caching, runtime tool schema hash canonicalization, explicit stale-schema diagnostics, readOnlyHint-gated approval behavior, and speed/cache gate metrics for `tool_schema_stable` and `mcp_schema_stable`. | `packages/runtime-go/internal/server/mcp_tool_schema_test.go:13`; `packages/runtime-go/internal/adapters/outbound/mcp/protocol/protocol_test.go:1`; `packages/runtime-go/internal/mcp/manager_test.go:1312`; `scripts/runtime-go-speed-cache-gate.mjs:184`; `go test ./internal/server -run 'TestRuntimeTool(SchemasUseMCPInputSchema|SchemaHashCanonicalizesSetLikeSchemaFields)' -count=1`; `go test ./internal/mcp ./internal/adapters/outbound/mcp/protocol -run 'TestProductionManager.*Schema|TestCanonical|TestParseToolsCanonicalizesInputSchema' -count=1`; `npm run runtime:go:speed-cache-gate`; `node scripts/runtime-closure-matrix-audit.mjs --strict-local --json` | fixed |
| MCP prompt-pinned tool scoping | `packages/runtime-go/internal/app/toolcatalog/routing.go:369` prepends prompt-pinned MCP tools before search results, `packages/runtime-go/internal/app/toolcatalog/routing.go:382` raises the local limit so all pinned tools survive even when `TopKDefault` is lower, and `packages/runtime-go/internal/app/toolcatalog/routing.go:325` resolves explicit tool names plus mentioned MCP server aliases such as Analytix Computer Use. `packages/runtime-go/internal/server/mcp_tool_schema_test.go:209` covers natural server mentions and explicit tool-name mentions, while `packages/runtime-go/internal/server/mcp_tool_schema_test.go:195` keeps direct identity prompts tool-free and `packages/runtime-go/internal/server/mcp_tool_schema_test.go:254` keeps agent tools available for work prompts. | Reasonix tool routing remains a reference for respecting explicit user/tool intent while still narrowing irrelevant tools. | OpenCode durable tool pairing is the comparison for keeping provider history valid after tool selection; this row focuses on pre-call catalog scoping. | CodexDesktop plugin resource layout is only a packaging reference; no auth bypass is absorbed. | P1 before fix: MCP search could cap away an explicitly mentioned MCP server/tool when `TopKDefault` was small, pushing the model toward unrelated tools or no tool even though the user named the tool/server. | Equivalent analytix runtime scoping guard. | Fixed by pinning explicitly mentioned MCP tools/servers before search ranking, expanding the limit only enough to keep pinned tools, keeping unrelated MCP servers scoped out, and retaining a tool-free profile for direct identity prompts. | `packages/runtime-go/internal/server/mcp_tool_schema_test.go:209`; `go test ./internal/server -run TestRuntimeToolSchemasPinMentionedComputerUseServer -count=1`; `go test ./internal/server -run 'TestRuntimeToolSchemasForPrompt(ScopesMCPAutoSafety|KeepsAgentToolsForWorkPrompts)|TestRuntimeToolSchemasPinMentionedComputerUseServer' -count=1` | fixed |
| `approvalPolicy=never` read-only MCP execution | `packages/runtime-go/internal/server/tool_catalog.go:130` now delegates approval-never classification to an app-layer helper instead of treating every `mcp__*` call as mutating. `packages/runtime-go/internal/app/toolcatalog/diagnostics.go:158` allows only MCP tools whose runtime catalog exposes `ToolReadOnlyHint(toolName)==true`; unknown or unannotated MCP tools still return `approval_policy_blocked`. The frontdoor A evidence in `thr_durable_75` / `turn_102` showed `mcp__analytix_funds__get_current_case` and `mcp__analytix_funds__count_case_rows` were blocked solely because `approvalPolicy` was `never`, even though direct `count_case_rows(fc_transaction)` returned the oracle count through the normalized analysis table. | Reasonix approval/cancel controller is the reference for real policy enforcement without fake approval gates; analytix keeps the MCP annotation contract. | OpenCode tool/task inheritance is not the main source for approval policy, but its durable tool-result pairing is matched by returning a paired blocked result for mutating MCP. | CodexDesktop plugin auth bypass is explicitly not absorbed. | P0 for frontdoor/runtime closure: a read-only MCP call under `approvalPolicy=never` produced a visible refusal and no durable data result, while a too-broad fix could accidentally execute mutating plugin/MCP actions. | Equivalent safe narrowing. | Fixed by retaining conservative default mutating classification for MCP, then overriding it only for explicit `readOnlyHint=true` catalog entries. This lets read-only plugin data tools run without an approval gate under `never`, while mutating/unknown MCP tools still become paired `approval_policy_blocked` tool results and do not execute. | `packages/runtime-go/runtime_server_test.go:8264`; `go test ./... -run 'TestRuntimeServerApprovalNever(BlocksMutatingToolExecution|AllowsReadOnlyMCPAndBlocksMutatingMCP)$'` | fixed |
| User input continuation tool-result privacy | Go user-input resolution builds a submitted-answer output at `packages/runtime-go/internal/app/turn/gate_requests.go:126` and now keeps a dual channel in `packages/runtime-go/internal/server/vision_bridge.go:13`: provider continuation receives the answer-bearing tool result, while `runtimePersistableToolOutput` delegates to app-layer `PersistableToolOutput` and continues removing `answers` before durable item/event replay at `packages/runtime-go/internal/server/tool_settlement.go:228` / `packages/runtime-go/internal/app/toolcatalog/diagnostics.go:180`. | Reasonix ACP/controller continuation is the reference for resumable gate lifecycle and cancellation; analytix keeps its own GUI user-input schema and replay privacy contract. | OpenCode durable tool-result pairing is the reference for feeding valid tool history back to the model without losing interrupted/finalized state. | Not applicable; CodexDesktop plugin resource layout does not define GUI user-input continuation. | P1 before fix: user input submission could resume the provider loop with only `answerCount/status`, so the model saw a paired tool result but not the user's actual answer; the opposite broad fix could leak submitted answers into `events.jsonl` or replay. | Equivalent continuation/history repair with analytix privacy boundary. | Fixed by preserving submitted answers only in the in-memory provider `tool` message produced for continuation, while persisted `tool_result` items and `user_input_resolved` SSE replay continue to omit answer values. Restarted pending input continuation keeps the same behavior. | `packages/runtime-go/runtime_server_test.go:14258`; `packages/runtime-go/runtime_server_test.go:14377`; `packages/runtime-go/internal/app/toolcatalog/diagnostics_test.go:100`; `go test . -run 'TestRuntimeServerUserInput(OnlyAppearsWhenModelCallsTool|ResolutionContinuesAfterRestart)' -count=1`; `go test ./internal/app/toolcatalog`; `npm run runtime:go:speed-cache-gate` | fixed |
| MCP stdio closed-pipe retry | Fresh installed `0.16.8` frontdoor canary `thr_durable_102` / `turn_128` showed repeated `mcp_call_failed` tool results for `mcp__analytix_funds__count_case_rows` with `error:"write |1: file already closed"`. `packages/runtime-go/internal/mcp/manager.go:615` already retried broken pipes, EOF, 5xx, and closed network connections through `reconnectToolForRetry`, but did not classify Go/macOS closed file/pipe wording as a transient transport failure. | Reasonix controller/reconnect behavior is a reference for treating transport breakage as recoverable infrastructure, not as a user-facing model fact. | OpenCode durable tool result pairing is preserved: if reconnect still fails, analytix returns a paired `mcp_call_failed` result; the fix only adds one safe retry for transport wording. | CodexDesktop plugin auth bypass remains rejected; this stays within analytix MCP manager. | P0/P1: a stale/dead stdio MCP pipe can make every plugin data tool return empty failed results, after which the model falls back to old files, memory, or visible process/error language. | Equivalent transport-retry hardening. | Fixed by classifying `closed pipe`, `file already closed`, and `use of closed file` as retryable MCP transport failures. JSON-RPC tool/business errors still do not reconnect. Fresh installed/config-patched `0.16.9` live frontdoor A-G oracle passed after runtime restart/config remount, proving the previous stale-pipe blocker no longer appears in accepted frontdoor evidence. | `packages/runtime-go/internal/mcp/manager_test.go:805`; `go test ./internal/mcp -run 'Test(ShouldRetryMCPCallTreatsClosedPipeAsTransportFailure|ProductionManagerRetriesTransientCallFailureAfterReconnect|ProductionManagerDoesNotRetryJSONRPCToolErrors)' -count=1`; `go test ./internal/mcp -count=1`; `node plugins/analytix-fund-analysis/scripts/frontdoor-p0-oracle.mjs --json --case-project-root /Users/sun/Projects/江苏航案件分析 --case-id 38dc50241996 --threads-root output/analytix-fund-analysis/frontdoor-canary-runs/2026-07-01T04-35-00-0800-0.16.9-frontdoor-live-config-patched-threads-root/threads --require-installed-version 0.16.9 --fail-on-gaps` passed at `output/analytix-fund-analysis/frontdoor-p0-oracle/2026-06-30T20-41-37-063Z/frontdoor-p0-oracle.json` | fixed |
| Case fund-analysis frontdoor tool source scoping | `packages/runtime-go/internal/server/agent_loop.go:263` now limits server work to workspace binding IO and MCP catalog lookup, then delegates prompt detection, analytix-funds tool-scope filtering, unavailable-tool sentinel policy, provider identity prompt shaping, final-answer recovery prompts, and internal-language sanitization to `packages/runtime-go/internal/app/loop/prompts.go:17`, `packages/runtime-go/internal/app/loop/prompts.go:38`, `packages/runtime-go/internal/app/loop/prompts.go:69`, `packages/runtime-go/internal/app/loop/prompts.go:136`, and `packages/runtime-go/internal/app/loop/prompts.go:188`. `packages/runtime-go/internal/server/tool_catalog.go:67` passes each MCP server real tool description to the provider instead of the generic configured-MCP text. `packages/runtime-go/internal/mcp/manager.go:812` and `packages/runtime-go/internal/mcp/manager_test_double.go:89` expose `ToolDescription` from production and replay managers. `plugins/analytix-fund-analysis/mcp/frontdoor-runtime.mjs` propagates Analytix runtime context through internal frontdoor `safeSkill` calls via `frontdoorSafeSkill`, so nested skill execution stays bound to the same case/runtime context. | Reasonix frontdoor/session policy is a reference for making the runtime enforce the intended execution lane instead of relying only on prompt style. | OpenCode durable tool pairing remains relevant; this slice is about narrowing tool exposure before the provider chooses a path. | CodexDesktop plugin manifest descriptions are a positive reference; auth bypass remains rejected. | P0/P1 for fund-analysis frontdoor closure: even when the right plugin tools are installed, a case-analysis prompt with broad default tools can lead the model to answer from old reports, workspace files, memory, shell/search, unrelated MCP servers, or contextless nested skill calls instead of current-case MCP evidence; generic MCP descriptions also weaken tool selection. | Product-specific runtime guard with narrow activation. | Fixed by activating only in bound Analytix case-project workspaces and only for prompts that look like case/fund-analysis fact requests. When active and no explicit request tool scope is already present, the provider receives only analytix-funds MCP tools plus a system instruction requiring current-case MCP evidence, preserving requested Top-N and prohibiting internal final-answer leakage. The pure prompt/scope/sanitization policy is now app-layer tested; non-case workspaces, non-case prompts, and explicit caller scopes are not overridden. The fresh `0.16.9` A-G live oracle proves the runtime/source-lane guard works end to end with installed plugin context propagation. | `packages/runtime-go/internal/app/loop/prompts_test.go:1`; `packages/runtime-go/runtime_server_test.go:10688`; `packages/runtime-go/internal/mcp/manager_test.go:1537`; `go test ./internal/app/loop -run TestCaseFund -count=1`; `go test ./internal/mcp -run TestProductionManagerToolDescriptionReturnsConfiguredDescription -count=1`; `go test ./internal/server -run TestRuntimeServerCaseFundPromptScopesProviderToolsToAnalytixFunds -count=1`; `node plugins/analytix-fund-analysis/scripts/user-visible-language-smoke.mjs`; full `0.16.9` frontdoor oracle passed at `output/analytix-fund-analysis/frontdoor-p0-oracle/2026-06-30T20-41-37-063Z/frontdoor-p0-oracle.json` | fixed |
| Fund-analysis oracle/runtime cache gate | `plugins/analytix-fund-analysis/scripts/mcp-return-oracle.mjs:90` now resolves the repo-local oracle case project when no explicit env root is set, so the default gate runs the real DuckDB oracle instead of skipping for a missing case path. `plugins/analytix-fund-analysis/scripts/functional-eval.mjs:238` now calls the oracle with `--require-case-db --fail-on-gaps`, and `plugins/analytix-fund-analysis/scripts/functional-eval.mjs:247` now calls the investigation scenario audit with `--require-case-db` so release evidence cannot hide a missing local DuckDB path as a skip. `plugins/analytix-fund-analysis/scripts/investigation-scenario-audit.mjs:450` now resolves the same repo-local oracle case project fallback as the oracle, and the rapid-in/out scenario uses `analysis_txn_detail_idx` plus `analysis_txn_daily_agg` structural metrics instead of requiring a nonexistent precomputed `analysis_rule_pattern_idx`. Source and installed fund-analysis manifest evidence now report `0.16.9`, and `plugins/analytix-fund-analysis/scripts/sync-runtime-cache.mjs` patches the Analytix-owned runtime cache plus `~/.analytix/data/config.json` MCP args/cwd/skill roots so restarted runtimes no longer mount stale plugin resources. `plugins/analytix-fund-analysis/scripts/frontdoor-p0-oracle.mjs` now records installed manifest mtime, thread evidence mtime, and durable thread workspace; auto-discovery only selects frontdoor evidence whose `thread.json.workspace` resolves to `--case-project-root`, while excluded candidates are written to the evidence payload. It scans only `user_message` text for frontdoor task matching, loads at most the newest matching thread per task, and normalizes replay with a completed-item key set instead of repeatedly scanning every item for every delta. It also scans both legacy direct plugin cache roots and Analytix-owned generated marketplace roots under `.cache/analytix-hub-plugins/marketplaces`, so local runtime remounts such as `0.16.9-local-*` are accepted as installed-version evidence. It also normalizes durable messages plus replay events so completed assistant text wins over running delta residue, while delta-only turns remain available as partial fallback, and C-task Top-N checks can read a durable `top_n_contract` from tool results. `plugins/analytix-fund-analysis/mcp/duckdb-workbench-runtime.mjs` now normalizes unfiltered `count_case_rows(table_name=fc_transaction_norm)` totals to `analysis_txn_detail_idx` and records the requested table plus normalization reason. It also emits `top_n_contract` in `run_case_sql` payload data, `metric_scope`, and `evidence_card` when `LIMIT N` or `ROW_NUMBER()/rank <= N` expresses a requested Top-N. `plugins/analytix-fund-analysis/mcp/user-facing-language.mjs` now redacts `案件ID/案件编号/case_id + long hex` values to `当前案件`, translates `Candidate Edge` / `Confirmed Edge` / `候选边已确认` into public-security evidence language, and flags both shapes as user-visible leakage. The Go runtime approval fix at `packages/runtime-go/internal/server/tool_catalog.go:130` / `packages/runtime-go/internal/app/toolcatalog/diagnostics.go:158` closes the latest A frontdoor execution blocker where read-only MCP tools were denied under `approvalPolicy=never`. `plugins/analytix-fund-analysis/mcp/tool-call-runtime.mjs:941` now prevents `CaseSourceError` from falling through to local DuckDB fallback, preserving explicit invalid-case blockers, and `plugins/analytix-fund-analysis/mcp/tool-call-runtime.mjs:943` adds a diagnostic/test-only `ANALYTIX_FUNDS_DISABLE_LOCAL_DUCKDB_FALLBACK=1` switch so functional tests can verify the raw `CONTROLLED_CASE_WORKBENCH_UNAVAILABLE` capability-gap contract without disabling the production fallback path. | Not applicable to Reasonix core runtime, except as a durable tool/plugin execution closure check. | Not applicable. | CodexDesktop plugin resource layout is the packaging reference; auth bypass remains rejected. | P1/P0 for release closure: oracle skip, stale runtime cache/config, generated marketplace installs being invisible to the frontdoor gate, stale frontdoor thread evidence, frontdoor evidence from the wrong workspace/case project, full auto oracle scanning tool-output history instead of user prompts, replay normalization growing quadratically with delta count, a model-selected cleaned bottom table for ordinary transaction-detail total counts, a model using `run_case_sql` for Top-N without replayable limit evidence, user-facing final text exposing internal case identifiers / graph edge state labels, runtime approval policy blocking read-only MCP data tools, explicit invalid `case_id` silently downgrading to active-case/local fallback, functional gates hiding raw controlled-workbench capability gaps because fallback succeeds, or release functional evidence accepting skipped non-backend DuckDB coverage would hide or fabricate plugin runtime regressions. | Equivalent product-specific gate hardening. | Fixed default oracle/scenario input resolution, made `functional-eval` use oracle and scenario coverage as hard case-DB gates, hardened the frontdoor P0 oracle against stale/wrong-workspace evidence, generated-marketplace install false negatives, and unbounded full-history auto discovery, fixed replay normalization so completed assistant answers are not replaced by single-token delta leftovers, closed the A-count table-selection root cause by forcing unfiltered transaction-detail total counts to the analysis index, closed the A latest runtime approval root cause by letting explicit read-only MCP tools run under `approvalPolicy=never` while keeping mutating/unknown MCP blocked, closed the D case-source guard root cause by letting `INVALID_CASE_SOURCE` return a blocker instead of attempting local fallback, closed the C Top-N runtime evidence root cause by adding durable `run_case_sql.top_n_contract` plus oracle consumption, added deterministic user-facing language guards for long case-id values plus Candidate/Confirmed Edge state labels, added a fallback-disable diagnostic contract for raw workbench capability gaps, and remounted the Analytix-owned runtime cache/config after the source patch. Fresh `0.16.9` frontdoor A-G evidence now passes; release publish alignment remains blocked by dirty/tag state, not by runtime closure evidence. | `plugins/analytix-fund-analysis/scripts/frontdoor-p0-oracle.mjs:1`; `plugins/analytix-fund-analysis/scripts/runtime-cache-contract.mjs:1`; `plugins/analytix-fund-analysis/scripts/count-case-rows-contract-smoke.mjs:78`; `node plugins/analytix-fund-analysis/scripts/doctor.mjs` passed after remount with local runtime cache drift as PASS and installed `0.16.9-local-17321d89385d`; `node plugins/analytix-fund-analysis/scripts/functional-eval.mjs` passed at `output/analytix-fund-analysis/functional-closure/2026-06-30T23-57-05-454Z/functional-eval.json` with 37 passed / 0 skipped / 0 failed; `node plugins/analytix-fund-analysis/scripts/investigation-scenario-audit.mjs --json --require-case-db --fail-on-structural-gaps` passed with 11 supported / 0 structural gaps / 1 case-data gap; `node plugins/analytix-fund-analysis/scripts/mcp-return-oracle.mjs --require-case-db --fail-on-gaps` passed at `output/analytix-fund-analysis/mcp-return-oracle/2026-06-30T23-53-21-443Z/mcp-return-oracle.json`; `node plugins/analytix-fund-analysis/scripts/frontdoor-p0-oracle.mjs --self-test-workspace-binding --json`; `node plugins/analytix-fund-analysis/scripts/frontdoor-p0-oracle.mjs --self-test-replay-normalization --json`; `node plugins/analytix-fund-analysis/scripts/frontdoor-p0-oracle.mjs --self-test-installed-version-scan --json`; `node plugins/analytix-fund-analysis/scripts/count-case-rows-contract-smoke.mjs --json`; `node plugins/analytix-fund-analysis/scripts/user-visible-language-smoke.mjs`; `node plugins/analytix-fund-analysis/scripts/pair-amount-contract-smoke.mjs`; `node plugins/analytix-fund-analysis/scripts/sync-runtime-cache.mjs --self-test-runtime-config --json`; `go test ./... -run 'TestRuntimeServerApprovalNever(BlocksMutatingToolExecution|AllowsReadOnlyMCPAndBlocksMutatingMCP)$'`; full live oracle `node plugins/analytix-fund-analysis/scripts/frontdoor-p0-oracle.mjs --json --case-project-root /Users/sun/Projects/江苏航案件分析 --case-id 38dc50241996 --threads-root output/analytix-fund-analysis/frontdoor-canary-runs/2026-07-01T04-35-00-0800-0.16.9-frontdoor-live-config-patched-threads-root/threads --require-installed-version 0.16.9 --fail-on-gaps` passed at `output/analytix-fund-analysis/frontdoor-p0-oracle/2026-06-30T20-41-37-063Z/frontdoor-p0-oracle.json`; `sync-runtime-cache.mjs --apply --confirm-local-remount --json` patched Analytix-owned runtime caches, generated marketplace/install markers, and quarantined stale marketplace cache `0.16.9-local-dadf246516b3`. | fixed |
| Fund-analysis exact count final-answer contract | Fresh installed `0.16.8` canary thread `thr_durable_91` / `turn_117` called `count_case_rows` and received the exact row count `2645472`, but the assistant final answer rounded it to `约 264.5 万条`; `frontdoor-p0-oracle.mjs --thread-id thr_durable_91 --require-installed-version 0.16.8` failed `A:answer_count_mismatch_expected_2645472`. Source now emits `exact_count_contract` from `plugins/analytix-fund-analysis/mcp/agent-payload-compiler.mjs:250` for `count_case_rows`, `plugins/analytix-fund-analysis/mcp/agent-output-compiler.mjs:1493` renders it as `精确计数答复约束`, and `plugins/analytix-fund-analysis/scripts/count-case-rows-contract-smoke.mjs:143` locks the visible exact-count contract. Real-case direct compiler output now says `交易明细表精确记录数为 2,645,472 条（原始整数 2645472）` and forbids `约/大约/左右/万条` approximations. | Reasonix is not a plugin answer-style reference here; the relevant closure principle is durable exact facts must survive into the final answer. | OpenCode durable message metadata is a reference for preserving exact tool facts across assistant finalization, not for Chinese answer prose. | Not applicable except plugin packaging must carry the updated files into installed cache before fresh frontdoor evidence is accepted. | P1: exact count facts can be durable and replay-visible but still become approximate in the final answer, causing visible release oracle failure. | Product-specific hardening. | Fixed at source compiler layer without changing runtime tool schemas or provider behavior. The local Analytix-owned runtime cache was remounted with `sync-runtime-cache.mjs --apply --confirm-local-remount`, and installed MCP modules emit the exact-count contract. Fresh `0.16.9` frontdoor A-G oracle now passes, including A exact-count acceptance. | `plugins/analytix-fund-analysis/scripts/count-case-rows-contract-smoke.mjs:78`; `node --check plugins/analytix-fund-analysis/mcp/agent-payload-compiler.mjs`; `node --check plugins/analytix-fund-analysis/mcp/agent-output-compiler.mjs`; `node --check plugins/analytix-fund-analysis/scripts/count-case-rows-contract-smoke.mjs`; `node plugins/analytix-fund-analysis/scripts/count-case-rows-contract-smoke.mjs --json`; installed real-case direct `count_case_rows(fc_transaction_norm)` compiler sample returned the `2,645,472` exact-count contract; full `0.16.9` frontdoor oracle passed at `output/analytix-fund-analysis/frontdoor-p0-oracle/2026-06-30T20-41-37-063Z/frontdoor-p0-oracle.json` | fixed |
| Turn lifecycle terminal CAS | Go terminal CAS is enforced by `FinishTurnIfActive*`; `packages/runtime-go/internal/app/turn/gate_cancellation.go:1` now owns pending approval/user_input cancellation event shaping, `packages/runtime-go/internal/server/turn_cancel.go:47` drains pending gates on interrupt/terminal continuation, and restart replay ignores pending gates after terminal events at `packages/runtime-go/internal/server/runtime_restore.go:73`. Review turns fail visibly if durable review item or replay event recording fails through `packages/runtime-go/internal/server/thread_routes.go:73` and `packages/runtime-go/internal/adapters/inbound/httpapi/review.go:44`. TS `TurnService.interruptTurn` and `TurnService.finishTurn` check `isTerminalTurnStatus` at `packages/runtime/src/services-test-support/turn-service.ts:154` and `packages/runtime/src/services-test-support/turn-service.ts:370`. | Reasonix controller/finalization at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/control/controller.go:1268` is a reference for terminal state not being overwritten by late cancel/finalization. | OpenCode interrupted assistant finalize at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/message-v2.ts:605` is a reference for terminal metadata durability. | Not applicable. | P0 before fix: TS late interrupt/finish could overwrite completed/failed turns, Go pending approval/user_input continuations could survive an aborted terminal turn, and a review turn could return accepted while its review item was only live/response-visible if durable append or replay recording failed. | Equivalent CAS/continuation/durable-review guard plus app-layer event shaping. | Fixed Go pending gate drain and restore-time terminal cleanup, moved pending gate cancellation event construction into `internal/app/turn`, fixed TS service-level terminal CAS for interrupt/finish, and fixed Go review route to return `review_failed` instead of accepting a turn whose durable review item or replay event was not recorded. Existing fork/resume tests verify history pairing and turn-id rewriting after continuation paths. | `packages/runtime-go/internal/app/turn/gate_cancellation_test.go:1`; `packages/runtime-go/runtime_server_test.go:3586`; `packages/runtime-go/runtime_server_test.go:13146`; `packages/runtime-go/runtime_server_test.go:13311`; `packages/runtime-go/runtime_server_test.go:13465`; `packages/runtime-go/runtime_server_test.go:13765`; `packages/runtime/tests/loop.test.ts:4738`; `go test ./internal/app/turn ./internal/server -run 'Test(PendingGateCancellationEvents|RuntimeServer(ReviewPersistsReviewItemAndReplay|InterruptCancelsPending(UserInput|Approval)Continuation))' -count=1`; `go test . -run 'TestRuntimeServer(ForkAndResumePreserveToolPairingHistory|ForkTurnIDTruncatesAndRewritesHistory|SecondTurnIncludesPriorHistory)' -count=1`; `npm run test -- packages/runtime/tests/loop.test.ts --run -t "late interrupt"` | fixed |
| Interrupt/cancel execution | Foreground provider cancel is covered by `packages/runtime-go/runtime_server_test.go:6946`; foreground bash cancel/timeout result shaping now lives at `packages/runtime-go/internal/app/terminal/bash.go:20`, background shell terminal settlement at `packages/runtime-go/internal/app/terminal/background_bash.go:31`, and process-group execution at `packages/runtime-go/internal/adapters/outbound/process/shell_runner.go:19`; Go pending gates are drained on interrupt by the server adapter and settled through app/control at `packages/runtime-go/internal/server/turn_routes.go:57` / `packages/runtime-go/internal/app/control/interrupt.go:127`; approval/user_input handlers reject terminal continuations at `packages/runtime-go/internal/server/gates_routes.go:47` / `packages/runtime-go/internal/server/gates_routes.go:115`; Go tool loop writes cancellation placeholders before executing later tools at `packages/runtime-go/internal/server/tools_execution.go:194`, read-only batches do the same at `packages/runtime-go/internal/server/tools_execution.go:181`, and `runtimeToolCancelledOutput` delegates to app-layer `CancelledOutput` at `packages/runtime-go/internal/server/tool_settlement.go:228` / `packages/runtime-go/internal/app/toolcatalog/diagnostics.go:198`. Background shell jobs are registered for shutdown/kill through the thin server callback at `packages/runtime-go/internal/server/terminal.go:48` and app-layer bash tool runner at `packages/runtime-go/internal/app/terminal/tool_runner.go:62`, context-cancel/timeout job states now count as terminal in both the runtime view at `packages/runtime-go/internal/app/subagent/job_view.go:294` and the durable job manager at `packages/runtime-go/internal/jobs/lineage.go:508`, and shutdown cancellation is covered by `packages/runtime-go/internal/server/runtime_shutdown_test.go:56`. Subagent background child-run kill/cancel and stale cleanup are covered by `packages/runtime-go/runtime_server_test.go:9731` and the subagent identity row. | Reasonix cancel controller is a reference for actual execution cancellation and gate cleanup. | OpenCode durable interrupted finalize is a reference for interrupted assistant/tool history. | Not applicable. | P0 before fix: an aborted turn could still accept pending approval/user_input continuation, a later `write_file` in the same provider tool batch could execute after a prior long tool was cancelled, and background shell shutdown/timeout could persist `canceled` or `timeout` as non-terminal task-job states, leaving wait/diagnostics/reload paths looking active or stalled. | Equivalent cancellation hardening. | Fixed gate continuation cleanup, terminal continuation rejection, same-step tool cancellation placeholders, task-job terminal recognition, and app-layer timeout/cancel result shaping so provider history pairing and background job diagnostics converge after interrupt, kill, timeout, shutdown, or restart cleanup. | `packages/runtime-go/runtime_server_test.go:7021`; `packages/runtime-go/runtime_server_test.go:13308`; `packages/runtime-go/runtime_server_test.go:13608`; `packages/runtime-go/internal/server/runtime_shutdown_test.go:56`; `packages/runtime-go/runtime_server_test.go:9731`; `packages/runtime-go/internal/app/toolcatalog/diagnostics_test.go:113`; `packages/runtime-go/internal/app/terminal/bash_test.go:28`; `packages/runtime-go/internal/app/terminal/background_bash_test.go:46`; `packages/runtime-go/internal/app/terminal/background_bash_test.go:107`; `packages/runtime-go/internal/app/terminal/bash_test.go:38`; `packages/runtime-go/internal/app/terminal/tool_runner_test.go:10`; `packages/runtime-go/internal/app/terminal/tool_runner_test.go:25`; `packages/runtime-go/internal/adapters/outbound/process/shell_runner_test.go:13`; `go test ./... -run 'TestRuntimeServerInterruptStopsLaterToolsInSameProviderStep|TestRuntimeServerInterruptCancelsPending(UserInput|Approval)Continuation'`; `go test ./internal/server -run 'TestRuntimeServerShutdown(CancelsActiveExecutionAndDisconnectsMCP|CancelsBackgroundShellAndMarksJobTerminal)' -count=1`; `go test . -run 'TestRuntimeServer(BashRunInBackgroundExposesOutputAndKill|BackgroundTaskJobKillCancelsChildRun|BackgroundTaskJobsWaitAndOutput|ModelCanInspectBackgroundTaskJobs|BashTimeoutKillsProcessGroupGrandchild)' -count=1`; `go test ./internal/app/toolcatalog ./internal/app/terminal ./internal/adapters/outbound/process` | fixed |
| Interrupt discard history isolation | Go interrupt now writes `discard` metadata in the same durable terminal update as `aborted` via `packages/runtime-go/internal/server/durable_turns.go:204`, `packages/runtime-go/internal/app/turn/terminal_update.go:44`, and `packages/runtime-go/internal/server/turn_routes.go:77`. Provider-history construction skips `status:"aborted"` turns whose persisted `discard` is true in the app-layer projector at `packages/runtime-go/internal/app/model/provider_history.go:25`, while non-discarded interrupts still materialize partial assistant deltas as covered in the partial-salvage row. | Reasonix cancel/discard handling is the reference for not refeeding cancelled turn context after the user requested discard. | OpenCode interrupted assistant finalize is the reference for differentiating salvage from discarded interrupted state. | Not applicable. | P1 before fix: `discard:true` was only present in the `turn_aborted` replay event, so the next provider request could still include the aborted user prompt from `thread.json` history. | Equivalent discard semantics on durable history, not only live event stream. | Fixed by atomically persisting `discard` with terminal abort and filtering discarded aborted turns from provider history. | `packages/runtime-go/runtime_server_test.go:7021`; `go test . -run TestRuntimeServerInterruptDiscardExcludesAbortedTurnFromNextProviderHistory -count=1`; `go test . -run 'TestRuntimeServer(InterruptCancelsActiveAsyncTurnAndPreservesAbortedStatus|InterruptDiscardExcludesAbortedTurnFromNextProviderHistory|InterruptStopsLaterToolsInSameProviderStep|InterruptedToolCallHistoryIsRepairedForProvider|ForkAndResumePreserveToolPairingHistory|ForkTurnIDTruncatesAndRewritesHistory)' -count=1` | fixed |
| Durable store crash/restart recovery | `packages/runtime-go/internal/server/runtime_restore.go:78` now preserves pending approval/user-input turn keys during restore; `packages/runtime-go/internal/server/runtime_restore.go:104` aborts stale `running/queued` main turns without pending gates; `packages/runtime-go/internal/server/runtime_restore.go:128` appends `runtime_restarted` error items and `turn_aborted` replay events. | Reasonix startup marks stale running work interrupted while preserving resumable gates. | Durable message metadata is a reference. | Not applicable. | P0 before fix: crash/restart could leave main turns and threads permanently running, blocking rewind/compact and UI settle. | Equivalent rewrite. | Fixed for Go main turns by restart-time terminal CAS plus visible runtime restart error; approval/user-input continuation is excluded from cleanup. | `packages/runtime-go/internal/server/durable_store_test.go:855`; `go test ./internal/server -run 'TestRuntime(RestoreAbortsStaleRunningMainTurn|TurnFailureMaterializesAssistantDeltas|TurnFailurePersistsErrorItemWhenEventLogCannotRecord)' -count=1`; `go test ./internal/server -count=1` | fixed |
| Tools/MCP pairing and cancel placeholders | TS tool history repair is covered by `packages/runtime/src/domain/model-history-repair.ts:14`. Go MCP propagates turn context through `packages/runtime-go/internal/server/tool_settlement.go:228`, `packages/runtime-go/internal/mcp/manager.go:412`, and `packages/runtime-go/internal/mcp/manager.go:463`; HTTP transport owns context-bound requests at `packages/runtime-go/internal/adapters/outbound/mcp/http/client.go:126`; stdio transport owns context-bound calls and process close at `packages/runtime-go/internal/adapters/outbound/mcp/stdio/process.go:173` and `packages/runtime-go/internal/adapters/outbound/mcp/stdio/process.go:181`; cancellation returns `mcp_call_cancelled` at `packages/runtime-go/internal/mcp/manager.go:476`. Go non-MCP tool batches now persist `tool_cancelled`/`tool_timeout` placeholders for unexecuted calls at `packages/runtime-go/internal/server/tools_execution.go:194`, `packages/runtime-go/internal/server/tools_execution.go:200`, `packages/runtime-go/internal/server/tool_settlement.go:228`, and `packages/runtime-go/internal/app/toolcatalog/diagnostics.go:198`. | Reasonix MCP/tool loop cancellation is the reference. | Provider history repair is a reference. | Plugin resource layout only; no auth bypass. | P1 before fix: interrupt could mark a turn aborted while an MCP HTTP/stdio call continued until timeout/completion, or leave same-step tool calls unpaired after cancellation. | Equivalent cancel propagation and placeholder repair. | Fixed Go MCP HTTP and stdio calls to use context-aware requests, avoid retrying canceled calls, close stdio processes on context cancellation, and return visible cancelled tool results. Fixed same-provider-step tool cancellation placeholders for later unexecuted tools. | `packages/runtime-go/internal/mcp/manager_test.go:227`; `packages/runtime-go/internal/adapters/outbound/mcp/http/client_test.go:1`; `packages/runtime-go/internal/adapters/outbound/mcp/stdio/process_test.go:1`; `packages/runtime-go/runtime_server_test.go:7021`; `packages/runtime-go/internal/app/toolcatalog/diagnostics_test.go:113`; `go test ./internal/mcp ./internal/adapters/outbound/mcp/... -count=1`; `go test ./... -run 'TestRuntimeServerInterruptStopsLaterToolsInSameProviderStep|TestRuntimeServerInterruptCancelsPending(UserInput|Approval)Continuation'`; `go test ./internal/app/toolcatalog` | fixed |
| Renderer timeline / pinned summary settle | Renderer terminal handling is covered. `src/renderer/src/store/chat-store-thread-actions.ts:551` now subscribes from `max(detail.latestSeq, store.lastSeq)` after loading thread detail, so historical replay deltas no longer reopen idle turns; `src/renderer/src/store/chat-store-thread-actions.ts:335` / `src/renderer/src/store/chat-store-thread-actions.ts:360` preserve newer live state when `recoverActiveTurn` receives a stale thread detail response, and `src/renderer/src/store/chat-store-thread-actions.ts:382` subscribes recovery from the current high-water seq. `src/renderer/src/store/chat-store-thread-actions.test.ts:624` covers the replay reverse-race, and `src/renderer/src/store/chat-store-thread-actions.test.ts:668` covers the live terminal-error recovery race. | Reasonix desktop status is reference only, not UI product source. | Not applicable. | CodexDesktop plugin detail/connector layout is a reference for plugin UI only. | P1 before fix: detail-first, replay-later ordering could duplicate assistant content or re-open busy state; recovery detail could also overwrite a newer live terminal error and put the composer back into busy/recovering state. | Equivalent renderer hardening. | Fixed `subscribeThreadEventsLive` to fetch detail first, set blocks/lastSeq/busy, then start SSE recovery from the detail high-water seq. Fixed `recoverActiveTurn` to keep newer live blocks/error/busy/currentTurn state when `lastSeq` advances during recovery, return the actual current busy state, and subscribe from `max(detail.latestSeq, store.lastSeq)`. | `src/renderer/src/store/chat-store-thread-actions.test.ts:624`; `src/renderer/src/store/chat-store-thread-actions.test.ts:668`; `npm run test -- src/renderer/src/store/chat-store-thread-actions.test.ts src/renderer/src/store/chat-store-runtime.test.ts --run` | fixed |
| Renderer measurement / trace pressure guard | `src/renderer/src/thread/virtualizer/resize-observer-batcher.ts:29` batches row measurement updates behind one scheduled frame, `src/renderer/src/thread/virtualizer/analytix-thread-virtualizer.ts:28` keeps long timelines windowed with bounded overscan instead of rendering every row, and `src/renderer/src/thread/tracing/thread-performance-trace.ts:16` sanitizes trace payloads before any persisted diagnostics call. `src/renderer/src/thread/thread-long-thread-smoke.test.ts:164` covers a long-thread virtualizer/trace sample and proves string prompt/output data is stripped. | Reasonix stream/readiness telemetry is a reference for low-noise runtime signals, but renderer measurement/trace is analytix UI-owned. | Not applicable. | Not applicable. | P1: high-frequency ResizeObserver measurement, virtualizer profiler, or thread trace metadata could flood renderer/main IPC or disk and could persist prompt/output strings if not bounded and sanitized. | Equivalent UI-pressure guard; no upstream UI code imported. | Closed by existing renderer virtualizer primitives plus explicit matrix/audit coverage: measurements are frame-batched, long thread rendering is windowed, and trace metadata drops strings before persistence. The named hazard audit now fails if this pressure guard disappears from the P0/P1 matrix. | `src/renderer/src/thread/virtualizer/analytix-thread-virtualizer.test.ts:74`; `src/renderer/src/thread/thread-long-thread-smoke.test.ts:164`; `src/renderer/src/thread/tracing/thread-performance-trace.test.ts:21`; `npm run test -- src/renderer/src/thread/virtualizer/analytix-thread-virtualizer.test.ts src/renderer/src/thread/thread-long-thread-smoke.test.ts src/renderer/src/thread/tracing/thread-performance-trace.test.ts --run`; `node scripts/runtime-closure-matrix-audit.mjs --strict-local --json` | fixed |
| Renderer subagent lifecycle labels | Pinned summary detail now prefixes a lifecycle label from `src/renderer/src/components/summary/SubagentSummaryRows.tsx:180`, and `subagentLifecycleLabel` maps raw child states such as `failed`, `killed`, `interrupted`, `canceled`, and `timeout` at `src/renderer/src/components/summary/SubagentSummaryRows.tsx:215`. The right inspector chrome and metadata reuse that helper at `src/renderer/src/components/summary/SubagentInspectorPanel.tsx:315` and `src/renderer/src/components/summary/SubagentInspectorPanel.tsx:478`. | Reasonix background/subagent lifecycle status is the reference for keeping failed/interrupted child work visibly terminal. | OpenCode interrupted assistant finalize is a reference for preserving interrupted state instead of flattening it into generic completion. | Not applicable. | P1 before fix: the right pinned summary and inspector could compress failed, killed, or interrupted child runs into a generic `已结束`, hiding the terminal cause even when runtime projection was durable. | Analytix UI-level equivalent; no product identity or runtime boundary change. | Fixed by sharing one lifecycle-label helper between pinned summary rows and the right inspector, while retaining provider/model/endpoint metadata in the inspector. | `src/renderer/src/components/summary/ThreadSummaryPanel.test.ts:420`; `src/renderer/src/components/summary/SubagentInspectorPanel.test.ts:100`; `npm run test -- src/renderer/src/components/summary/ThreadSummaryPanel.test.ts src/renderer/src/components/summary/SubagentInspectorPanel.test.ts --run` | fixed |
| Packaged runtime/process boundary | TS child process supervision remains at `src/main/analytix-process.ts:360`. Go sidecar exits are now observed by `src/main/runtime/analytix-adapter.ts:1222`, report unexpected ready runtime-server exits through `src/main/runtime/analytix-adapter.ts:1211`, and are wired into the main supervisor at `src/main/index.ts:1467`; intentional stops are marked at `src/main/runtime/analytix-adapter.ts:1247` / `src/main/runtime/analytix-adapter.ts:1446`. Go runtime-server SIGTERM/SIGINT shutdown now uses a bounded context at `packages/runtime-go/cmd/runtime-server/main.go:93`, calls handler lifecycle shutdown via `packages/runtime-go/cmd/runtime-server/main.go:154`, and the handler cancels active turns/background jobs plus disconnects MCP at `packages/runtime-go/internal/server/turn_cancel.go:40`. | Reasonix serve lifecycle and bounded shutdown are references. | Not applicable. | CodexDesktop packaging/resource copy is a positive reference. | P0/P1 before fix: Go runtime crash could leave SSE/busy recovery dependent on renderer watchdog instead of supervised restart; SIGTERM/SIGINT also used unbounded HTTP shutdown without cancelling active provider/tool/background/MCP execution. | Equivalent supervisor hook plus bounded graceful shutdown. | Fixed Go runtime-server sidecar exit observation so unexpected post-ready exits feed the same main-process supervisor path as the TS runtime, while intentional stops and non-runtime-server probes do not restart. Fixed runtime-server signal shutdown to cancel active runtime execution and MCP connections before bounded HTTP shutdown. | `src/main/runtime/analytix-adapter.test.ts:731`; `src/main/runtime/analytix-adapter.test.ts:745`; `packages/runtime-go/internal/server/runtime_shutdown_test.go:11`; `packages/runtime-go/cmd/runtime-server/main_test.go:215`; `npm run test -- src/main/runtime/analytix-adapter.test.ts src/main/runtime-sse-ipc.test.ts --run`; `go test ./internal/server -run TestRuntimeServerShutdownCancelsActiveExecutionAndDisconnectsMCP -count=1`; `go test ./cmd/runtime-server -run 'TestShutdownRuntimeServerNotifiesLifecycleHandlerWithDeadline|TestRuntimeServerShutdownTimeoutUsesEnvOverride' -count=1` | fixed |
| Packaged file-link dependency materialization | `packages/runtime/package.json` depends on `analytix-computer-use` through a local `file:` package. A real `npm run dist:mac:arm64` failed in `scripts/after-pack.cjs` because electron-builder did not dereference that runtime-local symlink into `app.asar.unpacked/packages/runtime/node_modules/analytix-computer-use`. `scripts/after-pack.cjs:200` now finds the local package root, `scripts/after-pack.cjs:224` copies it with `dereference:true` after runtime `npm prune`, and `scripts/after-pack.cjs:396` runs that restore before the existing runtime/native helper validation. | Reasonix does not ship this desktop helper package; no absorption needed. | Not applicable. | CodexDesktop resource-copy discipline is the packaging reference; auth/gate bypass remains rejected. | P1: regenerated macOS packages could fail after-pack or ship a runtime without the Analytix Computer Use MCP/helper package, breaking packaged tool/plugin availability while dev and old packaged artifacts still pass. | Equivalent packaging resource materialization inside the analytix boundary. | Fixed by restoring the local file-linked package into the packaged runtime after prune, then keeping the existing package manifest, CLI, native helper, product identity, and no-source assertions. | `src/main/packaging-config.test.ts:305`; `npm run test -- src/main/packaging-config.test.ts --run`; `npm run dist:mac:arm64`; `npm run runtime:go:packaged-gui-smoke -- --json --actual --timeout-ms 60000`; `npm run runtime:go:packaged-soak -- --json --actual --no-write --timeout-ms 90000`; `npm run test`; `npm run typecheck`; `git diff --check` | fixed |
| Runtime info / bridge contract governance | `packages/runtime-go/internal/app/runtimeinfo/contract_capabilities.go:109` adds disabled `visionBridge` to Go runtime capability responses, including `semanticProbeStatus:"unknown"` to match the TS `RuntimeCapabilityManifest`; `packages/runtime/src/conformance/fixtures/go-g1-shadow-contract.json:210` preserves the shadow contract. `packages/runtime-go/runtime_server_test.go:141` starts a live Go runtime with `capabilities.visionBridge`, proves `/v1/runtime/info` reports the configured available bridge provider/model/probe/screenshot-limit state, and asserts the bridge API key/base URL are not leaked. `src/renderer/src/lib/browser-analytix-bridge.ts:484` implements browser-preview `probeModelCapabilities`, and `scripts/scan-product-sovereignty.cjs:2118` plus G5 fixtures/schema at `packages/runtime/src/conformance/runtime-parity-fixtures.ts:2654` / `packages/runtime/src/conformance/fixtures/go-g5-full-loop-contract.json:1087` keep the named runtime API governed. | Not a Reasonix protocol import; this is analytix contract hygiene. | Provider capability probing follows OpenCode-style provider/model identity checks but remains analytix-owned. | CodexDesktop plugin/connector governance is a reference for allow-listed bridge/API surface. | P1 before fix: runtime-info schema drift, bridge allow-list drift, or a `capabilities.visionBridge` config that main wrote but Go did not read could fail reload/packaged validation, leak provider credentials, or push renderer code outside governed API lists. The latest product-regression run exposed a concrete drift where TS shadow runtime-info emitted `visionBridge.semanticProbeStatus:"unknown"` but the Go fixture/response did not consistently include that required field. | Equivalent contract synchronization. | Added the missing disabled capability field and semantic probe status, browser-preview failure stub, product sovereignty allow-list entry, conformance fixture/schema updates, and a live Go runtime-info config/redaction regression for `capabilities.visionBridge`; this response shape now lives in the app runtime-info contract and is exposed through the inbound HTTP adapter. | `packages/runtime/tests/go-runtime-conformance.test.ts:88`; `packages/runtime-go/internal/adapters/inbound/httpapi/response_test.go:60`; `packages/runtime-go/runtime_server_test.go:141`; `src/renderer/src/lib/browser-analytix-bridge.test.ts:99`; `npx vitest run packages/runtime/tests/go-runtime-conformance.test.ts --run`; `go test . -run TestRuntimeServerRuntimeInfoReadsVisionBridgeCapabilityConfig -count=1`; `go test ./internal/adapters/inbound/httpapi ./internal/server -run TestRuntimeVisionBridge -count=1`; `go test ./internal/server -run TestRuntime -count=1`; `npm run test -- src/renderer/src/lib/browser-analytix-bridge.test.ts --run`; `npm run scan:product-sovereignty`; `npm run runtime:go:product-regression -- --json` | fixed |
| Thread summary terminal projection | Right-summary task-job projection now uses app-layer read-model assembly at `packages/runtime-go/internal/app/threadsummary/service.go:144`, `packages/runtime-go/internal/app/threadsummary/service.go:152`, `packages/runtime-go/internal/app/threadsummary/service.go:200`, and `packages/runtime-go/internal/app/threadsummary/service.go:224`; the server facade at `packages/runtime-go/internal/server/thread_summary.go:61` and `packages/runtime-go/internal/server/thread_summary.go:68` only maps HTTP and injects durable store/job dependencies. The app-layer projector remains at `packages/runtime-go/internal/app/threadsummary/projection.go:229`; subagent projection is in `packages/runtime-go/internal/app/threadsummary/subagents.go:45`, `packages/runtime-go/internal/app/threadsummary/subagents.go:165`, and `packages/runtime-go/internal/app/threadsummary/subagents.go:269`; command task/source/output projection is in `packages/runtime-go/internal/app/threadsummary/commands.go:29`, `packages/runtime-go/internal/app/threadsummary/commands.go:124`, and `packages/runtime-go/internal/app/threadsummary/commands.go:170`. Raw job status now passes through `packages/runtime-go/internal/app/threadsummary/projection.go:58`. `packages/runtime-go/internal/server/thread_summary_test.go:266` locks `canceled` and `timeout` background jobs as summary `status:"terminal"` with `canKill:false`, and `packages/runtime-go/internal/app/threadsummary/service_test.go:10`, `packages/runtime-go/internal/app/threadsummary/projection_test.go:10`, `packages/runtime-go/internal/app/threadsummary/commands_test.go:8`, plus `packages/runtime-go/internal/app/threadsummary/subagents_test.go:10` lock the app service and pure projections. `packages/runtime-go/internal/server/thread_summary_test.go:263` also waits for restarted summary task jobs to reach terminal state via `packages/runtime-go/internal/server/thread_summary_test.go:655`, covering the async background lifecycle before test cleanup. | Reasonix background/subagent cleanup is the reference for terminal job settle and visible desktop status. | Not applicable. | Not applicable. | P1 before guard: the runtime could correctly mark background shell `canceled`/`timeout` terminal while the right summary/pinned task projection lacked a dedicated regression for those terminal states, risking future drift back to active/stalled UI. Production-tag validation could also race background job writes against temp-dir cleanup. | Equivalent validation and projection hardening. | Added a right-summary regression for `canceled`/`timeout` task jobs, moved read-model assembly plus subagent/task/status/display/output and command/source/output projection into `internal/app/threadsummary`, and retained the polling helper that waits for restarted jobs to stop being active/non-killable before test cleanup. | `packages/runtime-go/internal/server/thread_summary_test.go:266`; `packages/runtime-go/internal/app/threadsummary/service_test.go:1`; `packages/runtime-go/internal/app/threadsummary/projection_test.go:1`; `packages/runtime-go/internal/app/threadsummary/commands_test.go:1`; `packages/runtime-go/internal/app/threadsummary/subagents_test.go:1`; `go test ./internal/server -run 'TestThreadSummary(ProjectsCanceledAndTimeoutTaskJobsAsTerminal|RoutesProjectGoRuntimeState)' -count=1`; `go test ./internal/app/threadsummary -count=1`; `go test -tags production ./internal/server -run TestThreadSummaryRoutesProjectGoRuntimeState -count=20`; `go test -tags production ./internal/server -count=1`; `npm run runtime:go:product-regression -- --json` | fixed |
| Secret redaction / sandbox / approval boundary | Provider auth failures are structured and replay-redacted at `packages/runtime-go/runtime_server_test.go:5036`; provider HTTP diagnostics redact `api key` / endpoint body secrets at `packages/runtime-go/internal/provider/provider_test.go:68` and `packages/runtime-go/internal/provider/provider_test.go:106`; Go runtime retry/error text now redacts bare `sk-*`, `Bearer ...`, spaced `api key: ...`, and `token=...` through the app-loop recovery sanitizer at `packages/runtime-go/internal/app/loop/recovery.go:72`; MCP HTTP/list/call diagnostics redact configured headers, query tokens, and echoed upstream secrets at `packages/runtime-go/internal/mcp/manager_test.go:296` and `packages/runtime-go/internal/mcp/manager_test.go:353`; shared renderer/main text redaction covers recursive secret keys and inline bearer/token text at `src/shared/secret-redaction.test.ts:4`; protected read-dir sandbox behavior is covered around `packages/runtime-go/runtime_server_test.go:8238`; approval/user-input pending gates are drained on abort at `packages/runtime-go/internal/server/turn_routes.go:57` and settled through app/control at `packages/runtime-go/internal/app/control/interrupt.go:127`, and terminal continuations reject at `packages/runtime-go/internal/server/gates_routes.go:47` / `packages/runtime-go/internal/server/gates_routes.go:115`. | Reasonix sandbox/tool policy is a reference for consistent runtime-boundary enforcement; analytix keeps its own protected-root and gate contracts. | OpenCode provider auth failure durability/redaction is a reference for not leaking model/provider credentials into durable messages. | CodexDesktop plugin layout is useful, but `patch-plugin-auth.js` auth bypass remains an explicit anti-pattern and is not absorbed. | P0 before fix: runtime retry/error text could leave `Authorization: Bearer ...` or spaced `api key: ...` credentials in visible turn failure messages even when provider/MCP layers were already redacted. | Equivalent redaction hardening plus explicit rejection of bypass patterns. | Fixed Go runtime retry/error sanitizer for bearer and spaced API-key forms. Existing provider, MCP, shared redaction, protected read-dir, and approval/user-input continuation guards remain the product contract evidence. No bypass or deprecated bridge/provider path was introduced. | `packages/runtime-go/internal/app/loop/recovery_test.go:1`; `packages/runtime-go/internal/server/runtime_redaction_test.go:9`; `packages/runtime-go/runtime_server_test.go:5036`; `packages/runtime-go/internal/provider/provider_test.go:68`; `packages/runtime-go/internal/mcp/manager_test.go:296`; `src/shared/secret-redaction.test.ts:4`; `go test ./internal/server -run TestSanitizeProviderRetryMessageRedactsBearerAndSpacedAPIKey -count=1` | fixed |

| OpenCode provider/model default-fallback closure | Go runtime now validates any explicit, inherited thread, internal usage, or internal child-run model before provider default fallback at `packages/runtime-go/internal/server/turn_start.go:129` / `packages/runtime-go/internal/server/turn_start.go:135`; subagent child tasks validate the resolved child model before creating the child turn at `packages/runtime-go/internal/server/subagent_jobs.go:434`; durable thread creation/reload no longer invents `deepseek-chat` when a thread has no model at `packages/runtime-go/internal/app/thread/lifecycle.go:26`, `packages/runtime-go/internal/app/thread/lifecycle.go:40`, and `packages/runtime-go/internal/server/durable_store_test.go:834`. Go child-run `modelExecution` refs now hard-require concrete `providerId` and `modelId`, preserve `runtime-default` as an auditable source, and fail before durable child-run creation if execution identity is incomplete at `packages/runtime-go/internal/server/subagent_jobs.go:382` / `packages/runtime-go/internal/server/subagent_jobs.go:382`. Shared/test-support model execution refs no longer allow a soft `fallback` source or missing provider id: the source enum and required `providerId` are locked at `packages/runtime/src/contracts/model-execution-ref.ts:4`, renderer JSON mirrors the hard provider-bound tuple at `src/renderer/src/agent/analytix-contract.ts:50`, loop test-support labels default-runtime selection as `runtime-default` only when a concrete provider id is available at `packages/runtime/src/loop-test-support/agent-loop.ts:1516`, and delegation records accept `runtime-default` rather than `fallback` at `packages/runtime/src/delegation-test-support/delegation-runtime.ts:58`. | Reasonix subagent task execution preserves chosen provider/session identity at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/task.go:516` and treats incompatible continuation as failure. | OpenCode binds execution identity as `{providerID, modelID, variant}` through `Model.Ref` at `/Users/sun/Projects/_upstreams/opencode/packages/schema/src/model.ts:16`, stores interrupted/durable assistant metadata at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/message-v2.ts:605`, and inherits task child model tuples at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/tool/task.ts:160`. | Not applicable; CodexDesktop plugin resource layout does not define model execution semantics. | P0 before fix: a stale thread model, durable-store invented default model, explicit child/internal model outside a bounded provider catalog, or a Go/shared `ModelExecutionRef` carrying `source:"fallback"` / no `providerId` could silently fall through to the selected provider default and make a subagent/continue path run the wrong model. | Absorb OpenCode hard-fail semantics while preserving analytix custom/unbounded provider extensibility. | Fixed by validating inherited and internal execution models before `TurnConfigForExecution`, rejecting invalid subagent child model refs before child turn creation, removing the durable-store `deepseek-chat` default so absence of a model remains absence rather than a hidden execution ref, hard-failing incomplete Go child-run model execution refs, and retiring shared fallback execution refs in favor of provider-bound `{providerId, modelId, variant?}` tuples with `runtime-default` as an auditable concrete source. | `packages/runtime-go/runtime_server_test.go:680`; `packages/runtime-go/runtime_server_test.go:853`; `packages/runtime-go/internal/server/model_execution_ref_test.go:8`; `packages/runtime-go/internal/server/durable_store_test.go:586`; `packages/runtime-go/internal/provider/provider_test.go:954`; `packages/runtime/tests/contracts.test.ts:334`; `packages/runtime/tests/child-agent-executor.test.ts:433`; `packages/runtime/tests/delegation-runtime.test.ts:540`; `packages/runtime/tests/loop.test.ts:1034`; `go test . -run 'Test(RuntimeModelExecutionRefRequiresConcreteProviderModel|RuntimeServer(RejectsProviderModelMismatchBeforeProviderRequest|RejectsStaleThreadModelBeforeDefaultFallback|SubagentRejectsUnconfiguredChildModelBeforeChildTurn|SkillsParseReasonixFrontmatterAndLazyPackageExtras|SubagentContinueInheritsSourceIdentityWhenParentModelChanges|ExplicitTurnModelBecomesThreadCurrentModel|SubagentInheritsLatestParentThreadModel))' -count=1`; `go test ./internal/server -run 'TestDurableEventStoreDoesNotInventDefaultThreadModel|TestRuntimeRestoreAbortsStaleRunningMainTurn' -count=1`; `go test ./internal/provider -run 'TestRuntimeProvider(ConfigSetValidateExecutionModelRejectsBoundedProviderMismatch|ExecutionConfigRequiresValidationForBoundedCatalogs)' -count=1`; `npm run --prefix packages/runtime test -- tests/contracts.test.ts tests/loop.test.ts tests/child-agent-executor.test.ts tests/delegation-runtime.test.ts --run` | fixed |
| MCP workspace trust boundary | Go MCP `ServerSpec` now carries `trustScope` and `trustedWorkspaceRoots` at `packages/runtime-go/internal/mcp/manager.go:64`; `LoadMCPJSONDocument` parses GUI/runtime config trust fields at `packages/runtime-go/internal/mcp/manager.go:946`, skips workspace-scoped servers that lack a trusted root or whose current workspace is outside that root at `packages/runtime-go/internal/mcp/manager.go:988`, normalizes trust scope at `packages/runtime-go/internal/mcp/manager.go:1045`, and reports redacted trust diagnostics without raw path disclosure at `packages/runtime-go/internal/mcp/manager.go:727`. | Reasonix MCP lazy/status handling is a resilience reference, but analytix keeps its own workspace trust contract instead of importing Reasonix desktop state. | OpenCode tool/session policy at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/tools.ts:89` is a boundary reference; provider/model identity remains separate from MCP workspace trust. | CodexDesktop manifests expose plugin resources under `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/src/mac-arm64/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json:19`, while `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/scripts/patch-plugin-auth.js:3` remains the rejected bypass anti-pattern. | P1 before fix: main/preload config preserved MCP workspace trust fields, but the Go MCP loader ignored them, so a workspace-scoped plugin/server could be loaded outside its trusted roots. | Absorb CodexDesktop manifest/resource boundary discipline and keep analytix trust policy; reject auth bypass. | Fixed by making Go MCP config parsing enforce workspace-scoped trusted roots before creating server specs, preserving user-scoped servers, surfacing trust diagnostics, and including trust data in MCP fingerprints so cached schemas cannot mask trust-boundary changes. | `packages/runtime-go/internal/mcp/manager_test.go:106`; `go test ./internal/mcp -run 'TestLoadMCPJSONDocument(AppliesWorkspaceTrustScope|ReadsGUIAndRuntimeConfigShapes)' -count=1` | fixed |
| Hub marketplace fallback/package integrity boundary | The Hub marketplace service now keeps only the canonical HTTPS Hub default at `src/main/services/hub-agent-marketplace-service.ts:27`, requires an explicit `ANALYTIX_AGENT_PLUGIN_MARKETPLACE_FALLBACK_BASE_URL` before fallback routing at `src/main/services/hub-agent-marketplace-service.ts:281`, no longer defaults fallback TLS verification off at `src/main/services/hub-agent-marketplace-service.ts:297`, and still checks downloaded plugin package SHA-256 before extraction at `src/main/services/hub-agent-marketplace-service.ts:465`. | Reasonix lazy plugin loading at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/plugin/lazy.go:336` is a startup/cache resilience reference; analytix keeps its own Hub marketplace transport and package checksum boundary. | OpenCode tool/session policy at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/session/tools.ts:89` is a runtime boundary reference rather than a marketplace transport design. | CodexDesktop bundled marketplace layout at `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/src/mac-arm64/plugins/openai-bundled/.agents/plugins/marketplace.json:6` and plugin manifests at `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/src/mac-arm64/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json:19` are positive resource-layout references; `/Users/sun/Projects/_upstreams/CodexDesktop-Rebuild/scripts/patch-plugin-auth.js:3` remains the rejected auth-bypass anti-pattern. | P1 before fix: Hub sync carried an implicit IP fallback with fallback TLS verification disabled by default, so a catalog outage could silently switch transport trust posture before package checksum validation. | Absorb CodexDesktop manifest/resource layout discipline while keeping explicit analytix Hub trust configuration; reject hidden insecure fallback and auth bypass. | Fixed by removing the shipped default IP fallback and the implicit `insecureTls:'1'` fallback override. Explicit fallback remains available for operator-controlled deployments, and package downloads still rewrite through the same explicit fallback while enforcing SHA-256 before extraction. | `src/main/services/hub-agent-marketplace-service.test.ts:118`; `src/main/services/hub-agent-marketplace-service.test.ts:404`; `npm run test -- --run src/main/services/hub-agent-marketplace-service.test.ts` | fixed |
| Go execution model override preservation | `packages/runtime-go/internal/server/turn_start.go:135` uses provider `TurnConfigForExecution` whenever a runtime request carries an explicit model or internal subagent child-run metadata, so execution no longer rewrites a child/user model override back to the configured default. `packages/runtime-go/internal/app/model/provider_config.go:111` adds the execution config entry point, `packages/runtime-go/internal/app/model/provider_config.go:200` keeps normal provider diagnostics on the existing bounded-catalog path, and `packages/runtime-go/internal/app/model/provider_config.go:307` preserves unlisted explicit models only for the execution path. The provider facade keeps the old entry point at `packages/runtime-go/internal/provider/provider.go:115`. | Reasonix subagent task execution preserves the chosen child model/session identity at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/task.go:516` rather than silently swapping to a parent default. | OpenCode binds provider/model as an explicit `Model.Ref` at `/Users/sun/Projects/_upstreams/opencode/packages/schema/src/model.ts:16` and task child execution inherits the tuple at `/Users/sun/Projects/_upstreams/opencode/packages/opencode/src/tool/task.ts:160`. | Not applicable; CodexDesktop plugin resource layout does not define analytix model execution semantics. | P0 before fix: Go runtime execution could receive a skill/frontmatter child model, source child model, or explicit second-turn model, then the provider config bounded-model fallback rewrote it to the default model. That hid subagent model drift and prevented cache diagnostics from reporting model namespace churn. | Equivalent OpenCode/Reasonix-style execution identity preservation while keeping analytix provider diagnostics bounded. | Added a separate provider execution config path for model calls, wired `startRuntimeTurn` to use it for explicit or internal child-run models, and kept ordinary `TurnConfig` fallback for diagnostics/config display so configured model lists and provider probes are not broadened accidentally. | `packages/runtime-go/internal/provider/provider_test.go:920`; `packages/runtime-go/internal/app/model/provider_config_test.go:1`; `packages/runtime-go/runtime_server_test.go:2169`; `packages/runtime-go/runtime_server_test.go:4140`; `packages/runtime-go/runtime_server_test.go:8975`; `go test . -run 'TestRuntimeServer(SkillsParseReasonixFrontmatterAndLazyPackageExtras|CacheDiagnosticsTrackPrefixShapeChanges|SubagentContinueInheritsSourceIdentityWhenParentModelChanges)$' -count=1`; `go test ./internal/provider ./internal/app/model -run 'TestRuntimeProvider(ConfigRejectsCrossProviderModelDrift|ExecutionConfigPreservesExplicitUnlistedModel)|TestRuntimeProviderConfigSet' -count=1`; `go test ./...` | fixed |
| Composer model picker raw runtime model preservation | `src/main/upstream-models.ts:49` reads the raw `settings.runtime.model`, `src/main/upstream-models.ts:51` prefers that raw value as the composer default when it is a usable text model, `src/main/upstream-models.ts:68` includes both raw runtime and normalized runtime models in fallback IDs, and `src/main/upstream-models.ts:185` keeps the raw reader trim-only. `src/main/upstream-models.test.ts:84` locks `settings-model` in configured IDs and `src/main/upstream-models.test.ts:111` locks `local-only-model` when upstream query falls back to configured data. | Reasonix preserves selected model/session identity during execution at `/Users/sun/Projects/_upstreams/DeepSeek-Reasonix/internal/agent/task.go:516`; this row is the analytix main-process/UI picker equivalent, not a loop import. | OpenCode binds model identity explicitly through `Model.Ref` at `/Users/sun/Projects/_upstreams/opencode/packages/schema/src/model.ts:16`; analytix keeps raw composer-visible runtime model identity even when execution normalization has provider defaults. | Not applicable; CodexDesktop plugin resource layout does not define model picker or provider fallback semantics. | P1 before fix: the composer/provider model list could hide a locally configured runtime model and promote the first provider-catalog model as default, making picker state and provider diagnostics drift away from the user's explicit model. | Equivalent provider/model identity preservation at the desktop model-list boundary. | Fixed by separating raw runtime model preservation from normalized runtime execution settings: UI/fallback model discovery keeps explicit raw models visible while provider execution and probes continue to use the normalized settings path. | `src/main/upstream-models.test.ts:84`; `src/main/upstream-models.test.ts:111`; `npm run test -- --run src/main/upstream-models.test.ts src/shared/app-settings-provider.test.ts src/shared/openai-compat-url.test.ts src/main/provider-connection.test.ts src/main/services/write-inline-completion-service.test.ts src/main/claw-scheduled-task-detector.test.ts src/renderer/src/components/settings-section-agents.test.ts src/renderer/src/agent/analytix-runtime.test.ts`; `npm run runtime:go:product-regression -- --json` | fixed |

## Requested absorption matrix closure

| Module | Conclusion | Absorb target | Analytix closure evidence | Regression | Status |
| --- | --- | --- | --- | --- | --- |
| TODO / 完成证据 | Reasonix 明显更强，已按 analytix Go runtime 合同完整吸收 | Reasonix `todo_write -> complete_step -> evidence ledger -> final readiness -> goal FSM` | Go runtime owns `todo_write` and `complete_step`, persists the evidence ledger, enforces final readiness, and keeps the goal FSM durable in app-layer service code at `packages/runtime-go/internal/app/goal/tool_service.go:76`, `packages/runtime-go/internal/app/goal/tool_service.go:127`, `packages/runtime-go/internal/app/goal/tool_service.go:163`, and `packages/runtime-go/internal/server/durable_goals_todos.go:116`; server wrapper/replay callbacks stay at `packages/runtime-go/internal/server/goal_tools.go:20` and `packages/runtime-go/internal/server/goal_tools.go:48`. | `packages/runtime-go/internal/app/goal/tool_service_test.go:1`; `packages/runtime-go/runtime_server_test.go:10935`; `go test ./internal/app/goal`; `go test . -run TestRuntimeServerGoalTodoCompleteStepAndFinalReadiness -count=1` | fixed |
| Session todo UI | OpenCode 为 UI/store/event/dock 辅助，不能替代 Reasonix 证据闭环，已吸收为 session todo UI | OpenCode session todo store/event/dock 思路 | Renderer keeps activeThreadTodos in the store, exposes the TodoPanel dock and above-composer session todo status, and syncs todo store events back to thread snapshots at `src/renderer/src/components/todo/TodoPanel.tsx:29`, `src/renderer/src/store/chat-store-maintenance-actions.ts:446`, and `src/renderer/src/components/chat/above-composer/above-composer.test.ts:1`. | `src/renderer/src/components/todo/TodoPanel.test.ts:67`; `src/renderer/src/store/chat-store-maintenance-actions.test.ts:460`; `npx vitest run src/renderer/src/components/todo/TodoPanel.test.ts src/renderer/src/store/chat-store-maintenance-actions.test.ts src/renderer/src/components/chat/above-composer/above-composer.test.ts` | fixed |
| Subagent continuation/fork | Reasonix 更强，已做等价 native child side thread continuation/fork | Reasonix `SubagentStore / PrepareFresh / PrepareContinue / RunSubAgentWithSession / hash identity guard` | Go runtime resolves `continue_from` and `fork_from` against durable child-run records, copies ancestor continue_from into the current parent, and validates hash identity guard fields such as provider/model/endpoint/tool schema/system prompt through app-layer execution/source guards at `packages/runtime-go/internal/app/subagent/execution.go:47`, `packages/runtime-go/internal/app/subagent/profile.go:93`, and the thin server orchestration call at `packages/runtime-go/internal/server/subagent_jobs.go:193`; child turn completion now runs through `packages/runtime-go/internal/app/subagent/turn_completion.go:43`. | `packages/runtime-go/runtime_server_test.go:9874`; `packages/runtime-go/internal/app/subagent/profile_test.go:66`; `packages/runtime-go/internal/app/subagent/execution_test.go:11`; `packages/runtime-go/internal/app/subagent/turn_completion_test.go:13`; `go test . -run 'TestRuntimeServer(SubagentContinueAndForkUseDurableTranscript|CopiesAncestorSubagentContinueIntoCurrentParent|RejectsSubagentContinueSourceExecutionDrift)' -count=1`; `go test ./internal/app/subagent -run 'TestValidateSourceRejectsIdentityDriftAndAcceptsMatchingSource|TestResolveExecution|TestCompleteTask' -count=1` | fixed |
| Subagent profile / model inheritance | OpenCode 补强，已用 ModelExecutionRef 和 profile 权限继承落地 | OpenCode `Agent.Info`、`mode: primary/subagent/all`、permission/model inheritance | Go subagent profiles carry provider, model, endpoint, variant, effort, toolPolicy, tools, mode primary/subagent/all behavior through tool scoping, app-layer execution resolution, durable ModelExecutionRef lineage, and app-layer child turn completion at `packages/runtime-go/internal/app/subagent/profile.go:44`, `packages/runtime-go/internal/app/subagent/execution.go:47`, `packages/runtime-go/internal/server/subagent_jobs.go:215`, and `packages/runtime-go/internal/app/subagent/turn_completion.go:43`; pure profile config/default/diagnostic/application/model-source/tool-scope helpers now live at `packages/runtime-go/internal/app/subagent/config.go:1`, `packages/runtime-go/internal/app/subagent/profile.go:44`, `packages/runtime-go/internal/app/subagent/profile.go:142`, and `packages/runtime-go/internal/app/subagent/profile.go:180`. | `packages/runtime-go/runtime_server_test.go:8201`; `packages/runtime-go/runtime_server_test.go:8332`; `packages/runtime-go/internal/app/subagent/config_test.go:10`; `packages/runtime-go/internal/app/subagent/profile_test.go:10`; `packages/runtime-go/internal/app/subagent/execution_test.go:11`; `go test . -run 'TestRuntimeServer(SubagentProfileCarriesProviderModelExecutionRef|SubagentLifecycleCountsChildToolInvocations|SubagentInheritProfileDoesNotSilentlyRunBash)' -count=1`; `go test ./internal/app/subagent -run 'TestMergeProfileSettings|TestValidateProfileSettings|TestApplyProfile|TestProfileHelpers|TestResolveExecution' -count=1` | fixed |
| parallel_tasks | Reasonix 更强，已吸收依赖校验、wave 执行、partial aggregate 和结果 handoff | Reasonix dependency validation, wave execution, partial aggregate, result handoff | `parallel_tasks` validates dependencies, executes each ready wave, aggregates partial child results, hands dependency output to later waves, and returns parent handoff state through durable child runs via the app runner at `packages/runtime-go/internal/app/subagent/parallel_runner.go:1`, with server execution reduced to the child-task callback at `packages/runtime-go/internal/server/subagent_jobs.go:58`. | `packages/runtime-go/runtime_server_test.go:9375`; `packages/runtime-go/runtime_server_test.go:9448`; `packages/runtime-go/internal/app/subagent/parallel_runner_test.go:1`; `go test . -run 'TestRuntimeServerParallelTasks(HonorDependsOnWaves|RunReadyWaveConcurrently|CreateDurableChildRuns)' -count=1` | fixed |
| background jobs | Reasonix 为主，OpenCode 辅助 UX，已吸收 session-scoped wait/output/kill 和 promote/extend 诊断 | Reasonix session-scoped wait/output/kill and OpenCode promote/extend UX | Background subagent and shell jobs are session-scoped, expose wait/output/kill tools, keep output cursors, and settle killed/canceled/timeout states durably through the app service at `packages/runtime-go/internal/server/subagent_jobs.go:504`, `packages/runtime-go/internal/server/subagent_jobs.go:510`, `packages/runtime-go/internal/server/subagent_jobs.go:519`, plus right-summary task output/kill routes at `packages/runtime-go/internal/server/thread_summary.go:105` and `packages/runtime-go/internal/server/thread_summary.go:134`; pure background job context, output, diagnostics, terminal projection, cancel registry, output cursor, subagent slot queue, wait/output/kill request parsing, parent-thread allow checks, response envelopes, service wait/output/kill orchestration for runtime tools and right-summary task routes, durable job repository calls now live at `packages/runtime-go/internal/app/subagent/job_view.go:17`, `packages/runtime-go/internal/app/subagent/job_view.go:134`, `packages/runtime-go/internal/app/subagent/job_view.go:282`, `packages/runtime-go/internal/app/subagent/runtime_state.go:1`, `packages/runtime-go/internal/app/subagent/task_job_tools.go:1`, and `packages/runtime-go/internal/app/subagent/task_job_service.go:27`, `packages/runtime-go/internal/app/subagent/task_job_service.go:62`, and `packages/runtime-go/internal/app/subagent/task_job_service.go:69`; shell background/restart child-run start requests and background completion runner/job orchestration now live at `packages/runtime-go/internal/app/terminal/background_bash.go:32`, `packages/runtime-go/internal/app/terminal/background_bash.go:44`, and `packages/runtime-go/internal/app/terminal/background_bash.go:181`. | `packages/runtime-go/runtime_server_test.go:10192`; `packages/runtime-go/runtime_server_test.go:10422`; `packages/runtime-go/runtime_server_test.go:10411`; `packages/runtime-go/internal/app/subagent/job_view_test.go:10`; `packages/runtime-go/internal/app/subagent/runtime_state_test.go:1`; `packages/runtime-go/internal/app/subagent/task_job_service_test.go:10`; `packages/runtime-go/internal/app/terminal/background_bash_test.go:46`; `packages/runtime-go/internal/app/terminal/background_bash_test.go:107`; `packages/runtime-go/internal/app/terminal/bash_test.go:38`; `packages/runtime-go/internal/server/thread_summary_test.go:1`; `go test . -run 'TestRuntimeServer(BackgroundTaskJobsWaitAndOutput|BashRunInBackgroundExposesOutputAndKill|ModelCanInspectBackgroundTaskJobs|BackgroundTaskJobKillCancelsChildRun)' -count=1`; `go test ./internal/app/subagent -run 'TestTaskJob|TestBackgroundJob|TestRuntimeState' -count=1`; `go test ./internal/server -run 'TestThreadSummary(RoutesProjectGoRuntimeState|ProjectsCanceledAndTimeoutTaskJobsAsTerminal)' -count=1` | fixed |
| Provider wire / OpenAI-compatible | Reasonix 更强项已等价吸收并扩展到多 provider | Reasonix stream reconnect, usage parsing, history repair, schema canonicalize | Go runtime uses bounded interrupted-stream reconnect, canonical tool schema hashing, provider history repair, provider error redaction, and OpenAI-compatible usage parsing across chat completions, responses, and messages at `packages/runtime-go/internal/app/loop/recovery.go:39`, `packages/runtime-go/internal/app/toolcatalog/service.go:33`, `packages/runtime-go/internal/app/model/provider_history.go:13`, `packages/runtime-go/internal/app/model/provider_history.go:70`, `packages/runtime-go/internal/app/model/tool_pairing.go:44`, `packages/runtime-go/internal/app/model/tool_pairing.go:148`, `packages/runtime-go/internal/adapters/outbound/provider/client/client.go:181`, `packages/runtime-go/internal/adapters/outbound/provider/client/client.go:318`, `packages/runtime-go/internal/adapters/outbound/provider/client/client.go:424`, `packages/runtime-go/internal/adapters/outbound/provider/stream/stream.go:28`, `packages/runtime-go/internal/adapters/outbound/provider/stream/stream.go:381`, `packages/runtime-go/internal/adapters/outbound/provider/stream/stream.go:668`, and `packages/runtime-go/internal/adapters/outbound/provider/stream/stream.go:824`. | `packages/runtime-go/runtime_server_test.go:14893`; `packages/runtime-go/internal/app/model/tool_pairing_test.go:1`; `packages/runtime-go/internal/provider/provider_test.go:1875`; `packages/runtime-go/internal/adapters/outbound/provider/client/client_test.go:1`; `packages/runtime-go/internal/adapters/outbound/provider/stream/stream_test.go:1`; `go test . -run 'TestRuntimeServer(InterruptedToolCallHistoryIsRepairedForProvider|ProviderRequestShapesKeepDeepSeekFieldsScoped)' -count=1`; `go test ./internal/app/model ./internal/provider ./internal/adapters/outbound/provider/client ./internal/adapters/outbound/provider/stream -run 'TestSanitizeToolPairing|TestHTTPProviderClient|TestParse.*Usage|TestProviderUsage|TestParseOpenAIChatSSEToolCallAndNativeCacheUsage|TestParseAnthropicMessagesSSEUsageAndToolCall' -count=1` | fixed |
| Model resolver / variant | OpenCode 更强，已以 ModelExecutionRef 等价吸收并禁止 default model 派生回退 | OpenCode `Model.Ref { providerID, id, variant }` and no default model fallback | Runtime execution validates provider/model before provider fallback, preserves explicit model/variant via ModelExecutionRef, and thread creation no longer invents default provider/model at `packages/runtime-go/internal/app/model/provider_config.go:111`, `packages/runtime-go/internal/app/model/provider_config.go:164`, `packages/runtime-go/internal/app/subagent/execution.go:47`, `packages/runtime-go/internal/server/turn_start.go:129`, and `packages/runtime-go/internal/server/subagent_jobs.go:215`. | `packages/runtime-go/runtime_server_test.go:680`; `packages/runtime-go/runtime_server_test.go:8201`; `packages/runtime-go/internal/provider/provider_test.go:106`; `packages/runtime-go/internal/app/model/provider_config_test.go:1`; `packages/runtime-go/internal/app/subagent/execution_test.go:11`; `go test . -run 'TestRuntimeServer(ThreadCreateDoesNotInventDefaultModel|RejectsProviderModelMismatchBeforeProviderRequest|SubagentProfileCarriesProviderModelExecutionRef)' -count=1`; `go test ./internal/provider ./internal/app/model -run TestRuntimeProviderConfigSetValidateExecutionModelRejectsBoundedProviderMismatch -count=1`; `go test ./internal/app/subagent -run TestResolveExecution -count=1` | fixed |
| Tool registry / settlement | OpenCode 更强，已吸收 materialize、identity、stale tool call 和 durable settlement | OpenCode materialize, identity, stale tool call, durable settlement | Tool calls are materialized to durable `tool_call` and `tool_result` items, stale/partial interrupted tool calls are repaired before provider history, blocked/denied tools still produce paired tool results, user-input answer payloads are redacted before durable replay, cancelled/timeout placeholders are shaped consistently, and runtime tool policy/progress classification now has app-layer coverage at `packages/runtime-go/internal/server/tools_execution.go:20`, `packages/runtime-go/internal/server/tool_settlement.go:61`, `packages/runtime-go/internal/server/tool_settlement.go:228`, `packages/runtime-go/internal/app/model/provider_history.go:70`, `packages/runtime-go/internal/app/model/tool_pairing.go:52`, `packages/runtime-go/internal/app/model/tool_pairing.go:210`, `packages/runtime-go/internal/app/toolcatalog/diagnostics.go:1`; builtin tool schema materialization now lives at `packages/runtime-go/internal/app/toolcatalog/builtin_tools.go:1`, with plan tool schema materialization at `packages/runtime-go/internal/app/toolcatalog/plan_tools.go:1`, subagent/job/skill schema materialization at `packages/runtime-go/internal/app/toolcatalog/subagent_tools.go:1`, and user-input gate schemas at `packages/runtime-go/internal/app/toolcatalog/user_input.go:1`. | `packages/runtime-go/runtime_server_test.go:14893`; `packages/runtime-go/runtime_server_test.go:8924`; `packages/runtime-go/internal/app/model/tool_pairing_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/diagnostics_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/builtin_tools_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/plan_tools_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/subagent_tools_test.go:1`; `packages/runtime-go/internal/app/toolcatalog/user_input_test.go:1`; `go test . -run 'TestRuntimeServer(InterruptedToolCallHistoryIsRepairedForProvider|ApprovalPolicyNeverReturnsToolResultForBlockedTools|MCPReadOnlyApprovalPolicyNeverAllowsReadOnlyAndBlocksMutating)' -count=1`; `go test ./internal/app/model ./internal/app/toolcatalog -run 'TestSanitizeToolPairing|TestRequiresApprovalPolicyAndSandbox|TestBlockedByApprovalNever|TestGenericToolProgressAndStatus|TestPersistableToolOutputRedactsUserInputAnswers|TestCancelledOutputDistinguishesTimeout' -count=1` | fixed |
| Cache/usage | Reasonix + OpenCode 均已吸收，保留真实 cache fields 和 read/write/non-cached breakdown | Reasonix real cache fields plus OpenCode read/write/non-cached breakdown | Provider usage parses cacheHit/cacheMiss fields for DeepSeek/OpenAI-compatible/Anthropic style streams, app/model owns stable prefix-shape capture for system/tool/history/source diagnostics, app/usage owns durable record normalization plus runtime/thread/day/model/source aggregation, and app/turn emits cache diagnostics/cost breakdown without prompt text at `packages/runtime-go/internal/app/model/prefix_shape.go:1`, `packages/runtime-go/internal/app/usage/records.go:1`, `packages/runtime-go/internal/adapters/inbound/httpapi/usage.go:1`, `packages/runtime-go/internal/adapters/outbound/provider/stream/stream.go:381`, `packages/runtime-go/internal/adapters/outbound/provider/stream/stream.go:668`, `packages/runtime-go/internal/adapters/outbound/provider/stream/stream.go:824`, `packages/runtime-go/internal/adapters/outbound/provider/usage/usage.go:9`, `packages/runtime-go/internal/adapters/outbound/provider/usage/usage.go:30`, `packages/runtime-go/internal/adapters/outbound/provider/usage/usage.go:72`, and `packages/runtime-go/internal/app/turn/finalize_after_loop.go:43`. | `packages/runtime-go/internal/app/model/prefix_shape_test.go:1`; `packages/runtime-go/internal/app/usage/records_test.go:1`; `packages/runtime-go/internal/adapters/inbound/httpapi/usage_test.go:1`; `packages/runtime-go/internal/provider/provider_test.go:1875`; `packages/runtime-go/internal/adapters/outbound/provider/stream/stream_test.go:1`; `packages/runtime-go/runtime_server_test.go:4580`; `go test ./internal/app/model ./internal/app/usage ./internal/adapters/inbound/httpapi ./internal/provider ./internal/adapters/outbound/provider/stream -run 'TestCapturePrefixShape|TestResponsesAggregateCacheSourcesAndProviders|TestUsageHandlers|TestParse.*Cache|TestProviderUsage|TestApplyPricing|TestParseOpenAIChatSSEToolCallAndNativeCacheUsage|TestParseAnthropicMessagesSSEUsageAndToolCall' -count=1`; `go test . -run TestRuntimeServerCacheDiagnosticsTrackPrefixShapeChanges -count=1` | fixed |
| Vision / unsupported multimodal | OpenCode + Reasonix 合同已吸收，text-only 不进无限 base64，unsupported media 明确错误或走 vision bridge observation | OpenCode and Reasonix explicit unsupported-media errors and vision bridge observation | Attachment routing sends image payload only to vision-capable models, converts text-only attachments into bounded text fallback, rejects unsupported media or oversized base64 before provider injection, and uses redacted vision bridge observations for screenshot tools at `packages/runtime-go/internal/app/turn/attachments.go:61`, `packages/runtime-go/internal/app/turn/attachments.go:121`, `packages/runtime-go/internal/adapters/outbound/filestore/attachments.go:96`, and `packages/runtime-go/internal/app/runtimeinfo/vision_bridge.go:64`. | `packages/runtime-go/runtime_server_test.go:3701`; `packages/runtime-go/internal/server/vision_bridge_test.go:43`; `packages/runtime-go/internal/app/turn/attachments_test.go:1`; `go test . -run TestRuntimeServerAttachmentPayloadReachesProviderAsImageOrTextFallback -count=1`; `go test ./internal/server -run TestRuntimeVisionBridge -count=1`; `go test ./internal/app/turn -run TestAttachmentResolver -count=1` | fixed |
| 插件/skills/MCP/native | CodexDesktop-Rebuild + OpenCode 均已吸收适配，保留 analytix permission/trust boundary 和 plugin graph | CodexDesktop resource layout, manifest, packaging, connector boundary plus OpenCode catalog/permission/plugin graph | Hub/plugin code ingests manifest apps/resources/mcpServers/skills without auth bypass, Go MCP enforces trustScope and catalog fingerprints, MCP HTTP/stdio/protocol/cache/redaction boundaries now sit in outbound adapters, and renderer marketplace exposes catalog/permission/plugin graph diagnostics at `src/main/services/hub-agent-marketplace-service.ts:828`, `packages/runtime-go/internal/mcp/manager.go:936`, `packages/runtime-go/internal/mcp/manager.go:988`, `packages/runtime-go/internal/adapters/outbound/mcp/http/client.go:1`, `packages/runtime-go/internal/adapters/outbound/mcp/stdio/process.go:1`, `packages/runtime-go/internal/adapters/outbound/mcp/protocol/protocol.go:1`, `packages/runtime-go/internal/adapters/outbound/mcp/cache/cache.go:1`, `packages/runtime-go/internal/adapters/outbound/mcp/redaction/redaction.go:1`, and `src/renderer/src/components/plugin-marketplace-runtime.ts:38`. | `src/main/services/hub-agent-marketplace-service.test.ts:210`; `packages/runtime-go/internal/mcp/manager_test.go:106`; `packages/runtime-go/internal/adapters/outbound/mcp/http/client_test.go:1`; `packages/runtime-go/internal/adapters/outbound/mcp/stdio/process_test.go:1`; `packages/runtime-go/internal/adapters/outbound/mcp/protocol/protocol_test.go:1`; `packages/runtime-go/internal/adapters/outbound/mcp/cache/cache_test.go:1`; `packages/runtime-go/internal/adapters/outbound/mcp/redaction/redaction_test.go:1`; `packages/runtime/tests/skill-runtime.test.ts:197`; `packages/runtime/tests/mcp-tool-provider.test.ts:1071`; `npx vitest run src/main/services/hub-agent-marketplace-service.test.ts src/renderer/src/components/PluginMarketplaceView.test.ts packages/runtime/tests/skill-runtime.test.ts packages/runtime/tests/mcp-tool-provider.test.ts`; `go test ./internal/mcp ./internal/adapters/outbound/mcp/... -run 'TestLoadMCPJSONDocumentAppliesWorkspaceTrustScope|TestNewJSONRPCRequest|TestCommandEnvironment|TestCache|TestRedacted|TestDiagnostic|TestParseSSEJSONRPCResponse' -count=1` | fixed |

## Current slice closure

- Current runtime-go kernel split slice moves provider stable prefix-shape/cache
  prefix capture into `packages/runtime-go/internal/app/model/prefix_shape.go`,
  provider config/model resolution into
  `packages/runtime-go/internal/app/model/provider_config.go`,
  provider history tool-call/tool-result repair into
  `packages/runtime-go/internal/app/model/tool_pairing.go`, runtime tool
  contract diagnostics plus approval/blocked-tool policy, sub-agent/job tool
  classification, generic tool-progress settlement, persistable tool-output
  redaction, and cancelled/timeout tool-result shaping into
  `packages/runtime-go/internal/app/toolcatalog/diagnostics.go`, sub-agent
  request parsing, parallel-task DAG validation, wave execution, dependency handoff, aggregate result shaping, and child
  display-name shaping into
  `packages/runtime-go/internal/app/subagent/requests.go`, sub-agent profile
  application, model-source/profile-source helpers, continuation/fork source
  hash identity validation, approval/max-step helpers, and tool-scope filtering
  into `packages/runtime-go/internal/app/subagent/profile.go`, background
  task-job context selection, diagnostics, terminal status, output cursor/filter
  views, and bounded replay text shaping into
  `packages/runtime-go/internal/app/subagent/job_view.go`, thread-summary
  subagent/task/status/display/output plus command/source projection into
  `packages/runtime-go/internal/app/threadsummary/projection.go` and
  `packages/runtime-go/internal/app/threadsummary/commands.go` /
  `packages/runtime-go/internal/app/threadsummary/subagents.go`, prompt-route
  classification, MCP prompt pinning, goal/tool cue detection, and GUI
  user-input question normalization into
  `packages/runtime-go/internal/app/toolcatalog/routing.go` and
  `packages/runtime-go/internal/app/toolcatalog/user_input.go`, foreground bash request validation/result shaping and background bash durable start/result orchestration
  into `packages/runtime-go/internal/app/terminal/bash_tool_request.go`,
  `packages/runtime-go/internal/app/terminal/bash.go`,
  `packages/runtime-go/internal/app/terminal/background_bash.go`, and
  `packages/runtime-go/internal/app/terminal/buffer.go`, foreground shell
  process execution into
  `packages/runtime-go/internal/adapters/outbound/process/shell_runner.go`,
  read-file text view, UTF-8/UTF-16 codec normalization, exact text
  edit/delete-range planning, notebook edit normalization, code-index
  request parsing/symbol extraction/filtering/formatting, and glob/grep
  pattern normalization/matching into
  `packages/runtime-go/internal/app/filetools/text.go`,
  `packages/runtime-go/internal/app/filetools/edit.go`, and
  `packages/runtime-go/internal/app/filetools/notebook.go` /
  `packages/runtime-go/internal/app/filetools/code_index.go` /
  `packages/runtime-go/internal/app/filetools/search.go`, provider HTTP
  execution/reconnect/error redaction into
  `packages/runtime-go/internal/adapters/outbound/provider/client/client.go`,
  provider stream parsing/tool-call assembly/cache telemetry into
  `packages/runtime-go/internal/adapters/outbound/provider/stream/stream.go`,
  attachment model-input resolution/authorization/text-fallback settlement into
  `packages/runtime-go/internal/app/turn/attachments.go`,
  workspace status projection into
  `packages/runtime-go/internal/app/session/workspace_status.go`,
  runtime default-model/attachment/memory/MCP search/catalog/subagent/
  computer-use/web/vision capability-state projection plus sandbox, MCP search,
  web, and vision bridge config defaulting/parsing into
  `packages/runtime-go/internal/app/runtimeinfo/capabilities.go`,
  bounded terminal output buffering into
  `packages/runtime-go/internal/app/terminal/buffer.go`,
  usage record normalization/windowed runtime-thread-day-model aggregation into
  `packages/runtime-go/internal/app/usage/records.go`,
  attachment, memory, usage, and workspace-status HTTP response contracts into
  `packages/runtime-go/internal/adapters/inbound/httpapi/attachments.go` and
  `packages/runtime-go/internal/adapters/inbound/httpapi/memory.go` and
  `packages/runtime-go/internal/adapters/inbound/httpapi/usage.go` and
  `packages/runtime-go/internal/adapters/inbound/httpapi/workspace_status.go`,
  persistent attachment upload/content/diagnostics into
  `packages/runtime-go/internal/adapters/outbound/filestore/attachments.go`,
  persistent memory CRUD/diagnostics into
  `packages/runtime-go/internal/adapters/outbound/filestore/memory.go`,
  JSONL usage-index append/load/atomic rewrite helpers, JSON map-file
  read/write, text-file read/decode, directory listing/find/glob walking,
  code-index file discovery/parsing IO, gitignore matcher/repo-root walking,
  and bounded grep file scanning/context-window collection into
  `packages/runtime-go/internal/adapters/outbound/filestore/jsonl.go` and
  `packages/runtime-go/internal/adapters/outbound/filestore/json_map.go` and
  `packages/runtime-go/internal/adapters/outbound/filestore/text.go` and
  `packages/runtime-go/internal/adapters/outbound/filestore/search.go` and
  `packages/runtime-go/internal/adapters/outbound/filestore/code_index.go` and
  `packages/runtime-go/internal/adapters/outbound/filestore/gitignore.go` and
  `packages/runtime-go/internal/adapters/outbound/filestore/grep.go`,
  web fetch request parsing, URL policy, SSRF/IP guards, proxy-aware transport,
  bounded HTTP execution, URL redaction, source identity, telemetry, and HTML/plain-text extraction into
  `packages/runtime-go/internal/adapters/outbound/webfetch/client.go`,
  workspace status git/filesystem probing into
  `packages/runtime-go/internal/adapters/outbound/process/workspace_status.go`,
  and moves MCP HTTP request/client setup, stdio process startup, shared
  JSON-RPC/catalog protocol decoding, schema-cache persistence, and
  diagnostics/catalog redaction out of the transitional MCP manager into
  `packages/runtime-go/internal/adapters/outbound/mcp/http/client.go`,
  `packages/runtime-go/internal/adapters/outbound/mcp/stdio/process.go`,
  `packages/runtime-go/internal/adapters/outbound/mcp/protocol/protocol.go`,
  `packages/runtime-go/internal/adapters/outbound/mcp/cache/cache.go`, and
  `packages/runtime-go/internal/adapters/outbound/mcp/redaction/redaction.go`.
  Compatibility wrappers keep existing MCP manager tests and runtime behavior
  unchanged while adapter-owned tests lock HTTP request headers/session ids,
  stdio command environment, JSON-RPC response/SSE chunk parsing, catalog
  materialization, cache fingerprint/load/save, canonical schema
  materialization, safe cache paths, and secret redaction.
- Current Go-only retirement slice adds `scripts/runtime-go-ts-retirement-scan.mjs`
  as a formal validation entrypoint and deletes unused TypeScript runtime
  barrels and single-tool alias shims: `packages/runtime/src/adapters/index.ts`,
  `packages/runtime/src/adapters/tool/index.ts`,
  `packages/runtime/src/adapters/tool/bash.ts`,
  `packages/runtime/src/adapters/tool/edit.ts`,
  `packages/runtime/src/adapters/tool/find.ts`,
  `packages/runtime/src/adapters/tool/grep.ts`,
  `packages/runtime/src/adapters/tool/ls.ts`,
  `packages/runtime/src/adapters/tool/read.ts`,
  `packages/runtime/src/adapters/tool/write.ts`,
  `packages/runtime/src/delegation/index.ts`,
  `packages/runtime/src/loop/append-only-session-log.ts`,
  `packages/runtime/src/loop/model-request-estimator.ts`,
  `packages/runtime/src/loop/tool-result-image.ts`,
  `packages/runtime/src/loop/context-estimator.ts`,
  `packages/runtime/src/loop/index.ts`,
  `packages/runtime/src/review/review-prompt.ts`,
  `packages/runtime/src/review/git-review-target.ts`,
  `packages/runtime/src/review/review-output.ts`,
  `packages/runtime/src/adapters/model/tool-argument-repair.ts`,
  `packages/runtime/src/adapters/model/vision-bridge.ts`,
  `packages/runtime/src/server/index.ts`, and
  `packages/runtime/src/services/index.ts`. Review parsing/target helpers moved
  to `packages/runtime/src/shared/review-output.ts` and
  `packages/runtime/src/shared/review-target.ts`; shell safety classification
  moved to `packages/runtime/src/shared/shell-safety.ts`. Tool-argument repair
  was folded into the remaining legacy compat model-client oracle, while generic
  context estimation and tool-result image helpers moved to
  `packages/runtime/src/shared/context-estimator.ts` and
  `packages/runtime/src/shared/tool-result-image.ts`; vision bridge observation
  support moved to `packages/runtime/src/shared/vision-bridge.ts`. The same slice folded the retired
  TS model-client helper leaves into the remaining legacy client test oracle and
  deleted `packages/runtime/src/adapters/model/deepseek-pricing.ts`,
  `packages/runtime/src/adapters/model/minimax-pricing.ts`,
  `packages/runtime/src/adapters/model/model-error-probe.ts`, and
  `packages/runtime/src/adapters/model/proxy-fetch.ts`. The scan now proves
  `typeScriptCodePathDeleted:true` and `typeScriptRuntimeSourceDeleted:true`
  with zero production imports, package exports, build-config entries,
  packaged `dist` modules, or forbidden source-root modules pointing at retired
  TypeScript agent runtime paths. The hook engine no longer imports retired
  tool runtime utilities for spawn-tree termination; that helper now lives in
  `packages/runtime/src/shared/spawn-tree.ts`. The remaining TS HTTP/SSE,
  model-client, tool-host, delegation, loop, and service oracles were moved out
  of executable runtime roots to `packages/runtime/src/*-test-support/`, so
  they are test/conformance-only and no longer form a production fallback server
  or agent loop. The retired TS composition root
  `packages/runtime/src/server/runtime-factory.ts` is now deleted; its remaining
  usage carryover test helper moved to `packages/runtime/src/shared/usage-carryover.ts`.
  The retired TS Node serve wrapper `packages/runtime/src/server/node-http-server.ts`
  is also deleted, leaving `analytix serve` to launch only the Go runtime-server.
  Residual source implementation count is now 0. The packaged-contract dry-run
  still locks the failure-disclosure branch for mocked rollback evidence, while
  the real `runtime:go:ts-retirement-scan` is the final source-retirement gate.
- Current provider/model closure also persists explicit turn `model`,
  `reasoningEffort`, and `endpointFormat` back to the thread patch before
  `AppendTurnToThread`, so the next main or child turn inherits the actual
  current execution identity instead of an older thread model. Ancestor
  `continue_from` is now allowed only when the source parent is an ancestor of
  the current parent and is copied into the current parent as a continuation
  thread, preserving Reasonix-style native session continuation without mutating
  an ancestor child session. Regressions:
  `TestRuntimeServerExplicitTurnModelBecomesThreadCurrentModel`,
  `TestRuntimeServerSubagentInheritsLatestParentThreadModel`, and
  `TestRuntimeServerCopiesAncestorSubagentContinueIntoCurrentParent`.
- Current subagent execution-identity closure now persists profile/source
  provider, model, endpoint format, variant, and a redacted `ModelExecution`
  ref on Go child runs, exposes the same data through `/v1/runtime/tools`,
  thread summary, and SSE replay, and rejects source continuation/fork when
  execution identity drifts. Regressions:
  `TestRuntimeServerSubagentProfileCarriesProviderModelExecutionRef` and
  `TestRuntimeServerRejectsSubagentContinueSourceExecutionDrift`.
- Current OpenCode default-fallback closure now validates inherited thread
  models, internal child-run models, and subagent child task models before
  provider default fallback, and durable store no longer invents
  `deepseek-chat` for model-less threads. Regressions:
  `TestRuntimeServerRejectsStaleThreadModelBeforeDefaultFallback`,
  `TestRuntimeServerSubagentRejectsUnconfiguredChildModelBeforeChildTurn`,
  `TestDurableEventStoreDoesNotInventDefaultThreadModel`, and
  `TestRuntimeProviderExecutionConfigRequiresValidationForBoundedCatalogs`.
- Current CodexDesktop plugin-boundary closure now carries MCP `trustScope` and
  `trustedWorkspaceRoots` through the Go MCP loader, skips workspace-scoped
  servers outside trusted roots, includes trust data in MCP fingerprints, and
  exposes only redacted trust diagnostics. Regression:
  `TestLoadMCPJSONDocumentAppliesWorkspaceTrustScope`.
- Current Hub marketplace boundary closure removes the shipped implicit
  insecure IP fallback: fallback transport now requires an explicit
  `ANALYTIX_AGENT_PLUGIN_MARKETPLACE_FALLBACK_BASE_URL`, fallback TLS is no
  longer disabled by default, and package downloads still enforce SHA-256 before
  extraction. Regressions:
  `does not ship a default insecure Hub IP fallback` and
  `uses an explicit Hub fallback without bypassing package checksums`.
- The same slice also closes a rollback-evidence reporting blind spot:
  `scripts/runtime-go-rollback-retirement-evidence.mjs` now continues through
  `legacy-child-retired`, `analytix-serve-go-launcher`, `packaged-go-boundary`,
  and the `typescript-codepath-scan` even when an earlier retirement check
  fails; `scripts/runtime-go-rollback-retirement-report.mjs` likewise still
  runs packaged QA after rollback evidence fails. A single broken test can no
  longer hide later Go-only boundary regressions.
  `src/main/runtime-go-packaged-contract-report.test.ts` locks dry-run coverage
  plus the real early-failure/no-fail-fast behavior for both scripts.
- The reporting-closure hardening also covers the broad release gates:
  `scripts/runtime-go-product-regression.mjs` now records later product
  regression checks after an early child command fails, and
  `scripts/runtime-go-speed-cache-gate.mjs` records later speed/cache checks
  after an early child command fails. The regression suite fakes early failures
  and proves later checks such as product sovereignty, TS provider-cache, and
  Go MCP schema/cache are still present in the final JSON report.
- Refreshed upstream refs after safe fast-forward/fetch: Reasonix
  `origin/main-v2` is now `2ade3cd50d94e81ffb5f77d14b7de74082d06664`,
  OpenCode `origin/dev` is `6697cf3fd81d44fc8c3f72d32edb0e2549d24003`,
  and CodexDesktop-Rebuild `origin/master` is
  `0cb6bc8fba872d391666d6eed1dcd73b533fb2d3`. Latest Reasonix desktop
  titlebar/session-save/topic/registry changes remain non-absorbed product
  surface, and its bash cancel wait cleanup is already covered by analytix
  shell/background cancel evidence; latest OpenCode app/session UI deltas do
  not touch the mandatory provider/model/task inheritance files, and earlier
  OpenAI reasoning-variant catalog inference remains acceptable_by_contract
  because analytix only applies reasoning protocol from explicit preset/user
  model profiles, not implicit upstream catalog names, and keeps provider
  history repair/redaction in its own runtime contracts.
  `scripts/runtime-closure-matrix-audit.mjs --strict-local --json` now makes the
  matrix ref freshness check repeatable without mutating upstream worktrees, and
  checks all 90 Reasonix/OpenCode/CodexDesktop comparison cells so vague
  upstream text such as `same as above` cannot satisfy a P0/P1 row.
- Current provider/model slice additionally closed a product-regression failure
  where the desktop composer model picker lost raw `runtime.model` values after
  provider-settings normalization. `src/main/upstream-models.ts` now preserves
  the raw explicit runtime model for fallback/default model discovery while
  keeping normalized provider settings on the execution/probe path.
- Current direct-answer/cache slice expands the previous identity-only guard to
  ordinary lightweight prompts (`你是什么模型`, `你好`, `这个问题怎么理解`,
  `简单总结一下`). Go and TS now stop treating broad everyday words such as
  `项目`, `分析`, and `问题` as sufficient agent-work cues by themselves, so
  short ordinary prompts can avoid provider-visible MCP/skill/subagent schemas
  while explicit work prompts still keep their tools. The same slice now stores
  provider-safe prompt route/tool-count diagnostics in usage events and SSE
  replay (`direct_answer`, `light_agent`, `tool_agent`, `subagent_agent`) plus
  `firstTokenLatencyMs`/`durationMs` timing without adding raw prompt/system/tool
  text to cache diagnostics.
- Current bounded-provider slice closes the DeepSeek `mimo-v2-pro` class of
  model drift: external Go runtime turns and the legacy TypeScript multi-provider
  client now reject a model that is not present in the selected bounded
  provider catalog before issuing any provider request, returning structured
  `invalid_model` diagnostics instead of waiting for a provider 400.
- Fixed P0 slice: TS and Go `/events` replay/live setup failures now produce
  structured terminal runtime error events instead of bare `event:error` frames,
  JSON 400 setup responses, or renderer-only EventSource failures.
- Fixed P0 slice: Go runtime failure recording now persists the failed turn and
  visible error item before attempting event-log writes, and event recording
  errors are returned/logged instead of silently ignored.
- Fixed P0 slice: Go runtime failed turns and non-discarded interrupts now
  materialize streamed assistant deltas into durable terminal assistant items,
  so partial output is not only live/SSE-visible.
- Fixed P1 slice: Go interrupted-stream recovery depth now matches Reasonix at
  three bounded attempts, records `interrupted_stream` recovery diagnostics, and
  preserves the contract that recovery retries do not consume `maxModelSteps`.
- Fixed P0 slice: Go runtime restore now aborts stale non-gated main turns after
  restart, writes a visible `runtime_restarted` error item, and leaves
  approval/user-input continuation turns resumable.
- Fixed P0 slice: TS `DelegationRuntime` now rejects subagent runs that would
  inherit no provider before creating child records, shared
  `ModelExecutionRef` no longer permits a fallback execution source, and direct
  `ChildAgentExecutor` calls now require an inherited provider-bound execution
  ref.
- Fixed P0 slice: Go runtime now rejects explicit missing providers before
  `TurnConfig`, preserving default behavior only for legacy no-catalog runtime
  setups.
- Fixed P0 slice: Go runtime execution now preserves explicit model overrides
  for user turns, skill subagents, and source child-run continuations through
  `TurnConfigForExecution`, while ordinary provider diagnostics keep their
  bounded-catalog fallback. This closes the latest `go test ./...` failures
  where skill child runs and source child continuations silently fell back to
  the parent/default model and cache diagnostics lost the `model` namespace
  change reason.
- Fixed P0/P1 subagent parity slice: TS and Go child-run continuation/fork now
  have source-backed durable identity evidence. Go uses child threads plus
  `jobs.Manager` records as its Reasonix-equivalent store, with tests for
  source model inheritance, ancestor fork, concurrent continue locks,
  cross-parent rejection, stale interrupted children, tool-scope drift, profile
  provider/model execution refs, and source execution drift rejection.
- Fixed P1 slice: Go MCP HTTP and stdio tool calls now receive the active turn
  context, return a visible `mcp_call_cancelled` placeholder, and stop retrying
  canceled calls.
- Fixed P0/P1 MCP reconnect slice: Go MCP manager now treats
  `write |1: file already closed`, `use of closed file`, and `closed pipe` as
  retryable transport failures. Fresh installed `0.16.8` frontdoor A-G canary
  on the old runtime proved the risk; fresh installed/config-patched `0.16.9`
  frontdoor A-G oracle now passes after runtime remount/restart, so the
  stale-pipe blocker is closed by both source tests and live evidence.
- Fixed P0/P1 frontdoor source-lane slice: Go runtime now recognizes bound
  Analytix case-project workspaces plus case/fund-analysis prompts, scopes the
  provider tool catalog to `analytix_funds` MCP tools when the caller has not
  already supplied a tool scope, and passes MCP server tool descriptions through
  to the provider so the model sees precise current-case tool affordances.
- Fixed P0 slice: Go interrupt now drains pending approval/user_input
  continuations, records durable resolved/cancelled gate events, and rejects
  late gate continuation against terminal turns.
- Fixed P1 slice: Go `interrupt` with `discard:true` now persists the discard
  flag with the aborted turn and excludes that aborted turn from later provider
  history, so discarded prompts/partial output cannot leak into the next model
  request.
- Fixed P0 slice: Go same-provider-step cancellation now writes
  `tool_cancelled` placeholders for later unexecuted tool calls, so a cancelled
  foreground tool cannot be followed by a write tool in the same batch and
  provider history pairing remains valid.
- Fixed P0 slice: TS `TurnService` now guards `interruptTurn` and `finishTurn`
  with service-level terminal CAS, so late interrupt/finish calls do not
  overwrite completed/failed/aborted turns or emit duplicate terminal events.
- Fixed P1 review-turn durability slice: Go `/review` now returns
  `review_failed` unless the review item is persisted to `thread.json` and its
  `item_created` replay event is recorded, and the regression covers thread
  reload plus SSE replay visibility.
- Fixed P1 slice: plugin/MCP/skill boundaries now reject installed plugin MCP
  path escapes, stop default discovery from system `~/.codex/plugins/cache`,
  and reject `skill.json` entry/assets that leave the skill package root.
- Fixed P1 plugin apps/resources boundary: Hub marketplace keeps
  `.codex-plugin/plugin.json` `apps/resources` metadata visible and marks
  connector requirement, while runtime config sync does not register those
  entries as MCP servers and clears stale plugin-managed server ids.
- Fixed P1 slice: renderer live thread subscription now loads thread detail
  first and subscribes from the latest durable sequence, preventing stale replay
  deltas from re-opening idle threads.
- Fixed P1 renderer recovery race: `recoverActiveTurn` now preserves newer live
  terminal error state when a stale detail response returns after the SSE sink
  has already advanced `lastSeq`, and it resumes SSE from the current high-water
  sequence instead of the stale detail seq.
- Fixed P0/P1 slice: Go runtime-server sidecar exits now report unexpected
  post-ready crashes into the main supervisor path, while intentional stops and
  non-runtime-server probes are ignored.
- Fixed P1 process-boundary slice: Go runtime-server SIGTERM/SIGINT shutdown is
  now bounded and calls handler lifecycle shutdown, which cancels active turns,
  background jobs, and MCP connections before HTTP shutdown.
- Fixed P1 packaged dependency materialization slice: `after-pack` now restores
  the local file-linked `analytix-computer-use` package into
  `app.asar.unpacked/packages/runtime/node_modules` after runtime `npm prune`,
  then keeps the existing manifest/CLI/native helper/product-identity
  assertions. This closed a real `npm run dist:mac:arm64` failure where dev and
  older packaged artifacts passed but freshly generated packages lacked the
  runtime-local helper package.
- Fixed P1 background job terminal slice: background shell `canceled` and
  `timeout` states now count as terminal in the durable job manager and runtime
  task-job diagnostics, so shutdown/timeout cannot leave a job looking active or
  stalled after the subprocess is killed.
- Fixed P1 slice: Go `/v1/runtime/info` now includes disabled `visionBridge`
  capability state with `semanticProbeStatus:"unknown"`, browser preview
  implements a failure-mode `probeModelCapabilities` bridge, and the G1/G5
  conformance fixtures plus product-sovereignty scan allow-list now agree on
  that governed API surface. This closed the latest
  `runtime:go:product-regression` failure in `go-runtime-conformance`.
- Fixed P1 validation slice: thread-summary restart tests now wait for the
  background restart job to reach terminal state before cleanup, eliminating a
  production-tag cleanup race.
- Fixed P1 right-summary projection guard: canceled and timed-out background
  task jobs now have an explicit summary regression proving they render as
  terminal, non-killable task-job rows with output snippets.
- Fixed P0 security slice: Go runtime retry/error text now redacts
  `Authorization: Bearer ...`, spaced `api key: ...`, `token=...`, and bare
  `sk-*` secrets before they can appear in visible turn failure responses or
  durable/replay error events.
- Fixed fund-analysis closure slice: default `mcp-return-oracle.mjs` now runs
  against the repo-local oracle case project and real DuckDB case when no env
  root is provided, `functional-eval.mjs` invokes it with `--require-case-db`,
  controlled Workbench capability-gap tests can disable local DuckDB fallback
  with `ANALYTIX_FUNDS_DISABLE_LOCAL_DUCKDB_FALLBACK=1`, and `doctor.mjs`
  reports runtime cache drift as passed for installed `0.16.9`.
  `sync-runtime-cache.mjs` now remounts the Analytix-owned runtime cache and
  patches `~/.analytix/data/config.json` MCP args/cwd/skill roots so a restarted
  runtime does not keep pointing at stale plugin resources. `frontdoor-p0-oracle.mjs`
  rejects thread evidence older than the required installed plugin manifest,
  scans Analytix-owned generated marketplace plugin roots as installed-version
  evidence, only matches frontdoor tasks against `user_message` text, loads at
  most the latest matching thread per task, and excludes auto-discovered thread
  evidence whose durable workspace does not match the requested case project
  root. It also includes self-tests for workspace binding, replay normalization,
  and installed-version scanning; `phase4-diagnostics.mjs --release-guard`
  now runs those oracle self-tests directly, and completed durable assistant
  messages now win over running delta residue in oracle evidence assembly.
- Fixed frontdoor A-count runtime root causes: `count_case_rows` now normalizes
  an unfiltered `fc_transaction_norm` total-count request to
  `analysis_txn_detail_idx`, retains `requested_table_name`, and records the
  normalization reason. The smoke and real case direct call both return the
  oracle count `2645472`. The later failed frontdoor A evidence was re-audited
  to `approvalPolicy=never` blocking read-only MCP tools; Go runtime now allows
  MCP tools with explicit `readOnlyHint=true` while still blocking
  mutating/unknown MCP tools with paired `approval_policy_blocked` results. A
  fresh installed `0.16.8` canary then proved the tools execute, but
  `thr_durable_91` rounded the final answer to `约 264.5 万条`; source now emits
  a rendered `精确计数答复约束` for `count_case_rows` so final answers must use
  `2,645,472` / `2645472` exactly and must not write approximations. The fresh
  installed/config-patched `0.16.9` frontdoor oracle now passes A exactly.
- Fixed frontdoor C Top-N runtime evidence root cause: `run_case_sql` now emits a
  durable `top_n_contract` in payload data, `metric_scope`, and `evidence_card`
  when the SQL/purpose expresses `LIMIT N` or `ROW_NUMBER()/rank <= N`, and
  `frontdoor-p0-oracle.mjs` accepts either tool args or tool-result
  `top_n_contract` as the requested Top-N contract. The smoke covers both
  `LIMIT 2` and `ROW_NUMBER() ... rn <= 2`, and the fresh `0.16.9` frontdoor
  oracle now passes C with durable Top-N evidence.
- Fixed frontdoor E controlled-workbench evidence slice in current runtime:
  direct real-case `run_case_sql` for the 2024 monthly Top-10 holder metric now
  returns `sql_policy`, `evidence_card`, `validation_state`, `metric_scope`, a
  stable `source_hash`, and `top_n_contract`. The fresh `0.16.9` frontdoor
  oracle now passes E with report/artifact evidence.
- Fixed frontdoor D case-source guard slice: semantic tool `CaseSourceError`
  now bypasses local DuckDB fallback and returns a durable/user-readable
  `case_source_blocker`, so an explicit invalid `case_id` cannot be downgraded
  to active-case lookup, local-file search, or guessed-case recovery. The Pair
  Amount smoke now passes the invalid-case blocker guard, and the fresh
  `0.16.9` frontdoor oracle now passes D visible amount/scope evidence.
- Fixed F/G deterministic language guard slice: `translateUserVisibleText` now
  redacts visible `案件ID` / `案件编号` / `case_id` plus long hex identifiers to
  `当前案件`, translates Candidate/Confirmed Edge state labels into
  public-security evidence language, and flags `DuckDB`, generic
  `analysis_*` / `fc_*_norm`, `query_id`, `source_hash`, `metric_scope`,
  `raw_rows_exposed`, and related Workbench implementation fields as
  user-visible leakage. `agent-payload-compiler.mjs` now renders ordinary
  Workbench compact facts as business labels, carries a visible final-answer
  redaction guard, and keeps SQL-safe identifiers only on the dedicated
  explain/diagnose path so the next tool call can be repaired without putting
  table/field names into ordinary answer support text. The plugin marketplace
  manifest no longer says `DuckDB` in user-facing descriptions. This closes the
  shared source/smoke language guard, and the fresh `0.16.9` frontdoor oracle
  now passes F/G visible answer checks.
- Fixed frontdoor oracle discarded-evidence guard: `frontdoor-p0-oracle.mjs`
  now reads the latest durable turn status/discard flag from `thread.json` and
  excludes discarded aborted candidates from automatic task selection. The
  workspace-binding self-test now proves a newer discarded thread does not hide
  an older valid thread for the same task.
- Frontdoor A-G live proof is now closed for installed/config-patched `0.16.9`:
  `node plugins/analytix-fund-analysis/scripts/frontdoor-p0-oracle.mjs --json
  --case-project-root /Users/sun/Projects/江苏航案件分析 --case-id
  38dc50241996 --threads-root
  output/analytix-fund-analysis/frontdoor-canary-runs/2026-07-01T04-35-00-0800-0.16.9-frontdoor-live-config-patched-threads-root/threads
  --require-installed-version 0.16.9 --fail-on-gaps` passed at
  `output/analytix-fund-analysis/frontdoor-p0-oracle/2026-06-30T20-41-37-063Z/frontdoor-p0-oracle.json`
  with A-G all `ok` and no blockers.
- Fixed fund-analysis release-readiness evidence slice: version contract gaps
  are closed for `README.md`, `eval-coverage-contract.mjs`, and
  `runtime-cache-contract.mjs`; `mcp-surface-snapshot.mjs --fail-on-violation`
  refreshed `output/analytix-fund-analysis/evidence/mcp-surface-snapshot.json`
  for plugin `0.16.10` with 33 tools, 20 resources, 5 prompts, no default-tool
  schema/report-heavy/resource/prompt violations, and
  `backend_active_case_check:"passed"` against the live desktop data root
  active case `38dc50241996`; `release-slice-audit.mjs` now checks the same
  native packaged runtime candidate paths that `src/main/data-analysis` uses,
  including packaged-smoke `Resources/runtime`, and proves
  `dist/codex-packaged-smoke/mac-arm64/analytix.app/Contents/Resources/runtime/analytix-analysis-compute`
  has the required `stats-query-worker`, `query-stats-tree`,
  `query-stats-date-range`, and `query-stats-txn-rows` command surface.
- Remaining release blocker: source/runtime closure is now proven for the
  current `0.16.10` plugin runtime cache and packaged-smoke native runtime,
  but publish/release-slice alignment is still blocked by tag/dirty worktree
  state. Latest `release-slice-audit.mjs` reports HEAD `0d66123a4`,
  local `analytix-fund-analysis-v0.16.10` release tag missing, 86 dirty tracked paths
  after ignoring 845 generated evidence paths, 51 related release-slice dirty
  paths, and 35 unrelated dirty paths. Machine-readable disclosure:
  `tracked_dirty_count:86`; `generated_evidence_dirty_count:845`;
  `release_slice_dirty_count:51`; `unrelated_dirty_count:35`.
  Generated evidence count is a non-blocking, ignored-evidence snapshot that
  can increase when evidence-producing checks are rerun; the release gate
  requires it to be disclosed, not pinned as a product contract.
  The release slice now correctly
  classifies Go MCP manager closed-pipe transport fixes,
  `frontdoor-p0-oracle.mjs`, release-readiness contract files, and native
  packaged runtime evidence checks as release-related instead of unrelated, and
  treats local generated evidence rewrites under repo-root or
  `packages/runtime-go` output directories as evidence noise while still
  protecting deletions. Remaining unrelated dirty is now machine-grouped as
  `other_plugin_manifest` (1), `packaged_runtime_boundary` (2),
  `provider_capability_probe` (11), `renderer_subagent_lifecycle` (4),
  `runtime_closure_matrix_audit` (2), `runtime_closure_matrix_doc` (1),
  `runtime_contract_conformance` (2),
  `runtime_go_product_regression_matrix` (1),
  `runtime_go_regression_tests` (2), `runtime_lint_cleanup` (3),
  `runtime_skill_plugin_mentions` (2), `runtime_sse_ipc_recovery` (2), and
  `runtime_tool_result_image` (2). `release-slice-audit.mjs` now also
  reports `goal_wide_unrelated_dirty_count:35`, `unknown_unrelated_dirty_count:0`, and
  `unrelated_dirty_release_boundary.all_unrelated_paths_classified:true`, while
  `--staging-plan --readiness-plan` carries the same goal-wide/unknown split into
  release staging and readiness JSON; the bounded summary now exposes
  `ready_for_authorized_stage:true`, `stage_path_count:51`,
  `leave_unstaged_count:35`, `readiness_plan.blocker_count:3`,
  `readiness_plan.candidate_blocker_count:0`,
  `staging_plan.goal_wide_unrelated_paths_to_keep_out_count:35` and
  `staging_plan.unknown_unrelated_paths_to_keep_out_count:0` instead of
  truncating the keep-out split away. `--fail-on-publish-blockers` now gives
  release automation a direct non-zero gate for any remaining publish blocker,
  so blocked release identity cannot be mistaken for a passing audit just
  because JSON was emitted successfully. The
  `release-slice-audit.mjs --self-test-unrelated-dirty-groups --json` guard
  locks those staging-diagnostic buckets, `release-slice-audit.mjs
  --self-test-release-tag-status --json` locks `missing` vs `stale` vs
  `at_head` release-tag diagnostics, `release-slice-audit.mjs
  --self-test-summary-json --json` now also locks the bounded
  `publish_blocker_count`, `readiness_plan` counts, the full git/push/Hub/
  marketplace authorization boundary, and staging keep-out counters, and phase4 now runs
  `release_slice_generated_evidence_ignore_self_test`,
  `release_slice_native_runtime_evidence_self_test`,
  `release_slice_summary_self_test`,
  `release_slice_tag_status_self_test`,
  `release_slice_unrelated_dirty_groups_self_test`, and
  `release_slice_publish_blocker_exit_gate`,
  `release_slice_current_readiness_summary_gate`, and
  `release_slice_verify_isolated_gate` instead of only checking source text.
  `node plugins/analytix-fund-analysis/scripts/phase4-diagnostics.mjs --release-guard --json`
  now guards those release-slice audit contract fields through the
  passing `release_slice_audit_contract` check, including the publish-blocker
  fail flag, publish-blocker count, readiness count, full authorization
  requirements, staging keep-out field names, generated-evidence ignore
  behavior, packaged native-runtime command-surface detection, release
  tag/unrelated-dirty grouping self-tests, a live exit-code check proving
  `--fail-on-publish-blockers` matches `publish_blocker_count`, and an
  isolated detached-worktree apply check proving the release slice excludes
  unrelated paths and cleans up. The same phase4 guard also runs
  `frontdoor_oracle_workspace_binding_self_test`,
  `frontdoor_oracle_replay_normalization_self_test`, and
  `frontdoor_oracle_installed_version_scan_self_test`, proving the live oracle
  keeps wrong-workspace/stale evidence out, preserves completed/partial replay
  answers, and accepts both generated marketplace and runtime-cache install
  roots as installed-version evidence. The same phase4 guard also runs
  `runtime_cache_config_remount_self_test`, proving MCP server path, cwd, and
  skill root remount to the existing Analytix-owned `0.16.9` cache, and the
  current release-slice readiness summary gate proves the live dirty worktree
  summary keeps `publish_blocker_count`, the full authorization list, and
  staging keep-out counts internally consistent. It now reports 106 passed
  checks, 0 warnings, and only 2 failures, both release-operation
  blockers (`release_git_tag_identity` and `release_git_worktree_clean`) after
  the active-case lock, MCP request-handler validation, explicit env injection,
  active-case API metadata, investigation-lab protocol, and functional-closure
  DuckDB hard-gate guard gaps were closed. Machine-readable disclosure:
  `phase4_release_guard_summary: pass=106 warn=0 fail=2`;
  `phase4_release_guard_failures: release_git_tag_identity, release_git_worktree_clean`.
  The phase4
  `release_slice_verify_isolated_gate` proves the release-slice paths
  can be applied in a temporary detached worktree with
  `isolated_dirty_total:41`, `isolated_related_dirty_count:41`,
  `isolated_unrelated_dirty_count:0`, 60 Node syntax checks,
  `doctor --skip-git --skip-runtime` passing, and temporary worktree cleanup
  complete. The full goal remains active until release commit/tag alignment and
  the 35 unrelated dirty paths are explicitly handled or waived by the user.
- Verified focused tests: `packages/runtime` event/delegation/task/child group
  passed 4 files / 40 tests; provider/subagent/thread-summary group passed 6
  files / 57 tests; renderer/plugin/preload group passed 8 files / 223 tests.
- Verified combined npm test slice: `npm run test -- ... --run` passed 13
  files / 302 tests across sidecar supervision, SSE/runtime store, child
  agents, browser bridge, renderer runtime mapping, plugin marketplace, and
  MCP tools.
- Verified product gate after the case frontdoor tool-source guard: `npm run
  runtime:go:product-regression -- --json` passed with 35 matrix features and
  all reported checks passed, including runtime contracts, provider settings,
  MCP/plugin, subagent/background, approval/user-input, renderer timeline,
  packaged smoke contracts, and product sovereignty scan.
- Verified speed/cache gate: `npm run runtime:go:speed-cache-gate` passed,
  including deterministic first-token/streaming metrics, provider settings
  resolution, TS/Go cache contracts, tool progress, subagent, background job,
  approval, attachments, usage/cost, compaction history, and MCP schema/cache
  checks.
- Verified TypeScript gates: `npm run typecheck` and `npm run build:runtime`
  passed.
- Verified production build and lint: `npm run build` passed, and
  `npm run lint` now exits 0 after removing 21 lint errors in regex/Boolean cleanup
  paths; the remaining output is 41 existing React hook warnings.
- Verified full test suite: `npm run test` passed 330 test files / 2888 tests
  on the current slice after interrupted-stream recovery depth, renderer
  subagent lifecycle-label closure, packaged file-link regression, release
  audit publish-blocker gating, and the lint-only regex cleanup.
- Verified Go runtime failure-record guard: `go test ./internal/server -run
  TestRuntimeTurnFailurePersistsErrorItemWhenEventLogCannotRecord -count=1`,
  `go test ./internal/server -count=1`, and `go test ./...` passed in
  `packages/runtime-go`.
- Verified final focused guard: `npx vitest run
  packages/runtime/tests/events-route.test.ts` passed, and `git diff --check`
  passed.
- Verified latest focused guards: `go test ./... -run
  'TestRuntimeServerInterruptStopsLaterToolsInSameProviderStep|TestRuntimeServerInterruptCancelsPending(UserInput|Approval)Continuation'`
  passed in `packages/runtime-go`; `npm run test --
  packages/runtime/tests/loop.test.ts --run -t "late interrupt"` passed; `npm
  run test -- src/main/services/skill-service.test.ts
  packages/runtime/tests/skill-runtime.test.ts --run` passed; `npm run test --
  src/main/analytix-process.test.ts
  src/main/services/hub-agent-marketplace-service.test.ts --run` passed.
- Verified latest security/subagent guards: `go test ./internal/server -run
  TestSanitizeProviderRetryMessageRedactsBearerAndSpacedAPIKey -count=1`,
  `go test ./...`, `npm run test -- src/shared/secret-redaction.test.ts
  --run`, `go test . -run
  'TestRuntimeServer(SubagentContinueAndForkUseDurableTranscript|SubagentContinueInheritsSourceIdentityWhenParentModelChanges|RejectsConcurrentSubagentContinueFromSameReference|AllowsSubagentForkFromAncestorParentThread|RejectsSubagentContinueWhenToolScopeDrifts|StartupInterruptsStaleRunningSubagentRuns|RejectsCrossParentSubagentContinue)'
  -count=1`, `npm run test --
  packages/runtime/tests/child-agent-executor.test.ts
  packages/runtime/tests/delegation-runtime.test.ts
  packages/runtime/tests/task-job-orchestration-contract.test.ts --run`, and
  `git diff --check` passed.
- Verified latest interrupt-discard guard: `go test . -run
  TestRuntimeServerInterruptDiscardExcludesAbortedTurnFromNextProviderHistory
  -count=1` and the focused interrupt/history group covering active cancel,
  discard history filtering, same-step tool cancellation, repaired interrupted
  tool history, fork/resume tool pairing, and fork turn-id rewriting passed.
- Verified latest process-boundary guards: `go test ./internal/server -run
  TestRuntimeServerShutdownCancelsActiveExecutionAndDisconnectsMCP -count=1`
  and `go test ./cmd/runtime-server -run
  'TestShutdownRuntimeServerNotifiesLifecycleHandlerWithDeadline|TestRuntimeServerShutdownTimeoutUsesEnvOverride'
  -count=1` passed.
- Verified latest background job terminal guards: `go test ./internal/server
  -run
  'TestRuntimeServerShutdown(CancelsActiveExecutionAndDisconnectsMCP|CancelsBackgroundShellAndMarksJobTerminal)'
  -count=1`, `go test ./internal/jobs -count=1`, and `go test . -run
  'TestRuntimeServer(BashRunInBackgroundExposesOutputAndKill|BackgroundTaskJobKillCancelsChildRun|BackgroundTaskJobsWaitAndOutput|ModelCanInspectBackgroundTaskJobs|BashTimeoutKillsProcessGroupGrandchild)'
  -count=1` passed in `packages/runtime-go`.
- Verified latest review/fork/resume guard: `go test . -run
  'TestRuntimeServer(ReviewPersistsReviewItemAndReplay|ForkAndResumePreserveToolPairingHistory|ForkTurnIDTruncatesAndRewritesHistory|SecondTurnIncludesPriorHistory)'
  -count=1` passed in `packages/runtime-go`.
- Verified latest Go/whitespace gates after review durable-replay closure:
  `go test ./...` passed in `packages/runtime-go`, and `git diff --check`
  passed.
- Verified latest Go/whitespace gates after background terminal closure:
  `go test ./...` passed in `packages/runtime-go`, and `git diff --check`
  passed.
- Verified latest renderer recovery guard: `npm run test --
  src/renderer/src/store/chat-store-thread-actions.test.ts
  src/renderer/src/store/chat-store-runtime.test.ts --run` passed 2 files / 51
  tests, and `npm run typecheck` passed.
- Verified latest right-summary terminal projection guard: `go test
  ./internal/server -run
  'TestThreadSummary(ProjectsCanceledAndTimeoutTaskJobsAsTerminal|RoutesProjectGoRuntimeState)'
  -count=1`, `go test ./internal/server -count=1`, and `git diff --check`
  passed in `packages/runtime-go`.
- Verified latest plugin apps/resources guard: `npm run test --
  src/main/analytix-process.test.ts
  src/main/services/hub-agent-marketplace-service.test.ts
  src/main/services/skill-service.test.ts
  packages/runtime/tests/skill-runtime.test.ts --run` passed 4 files / 69
  tests; `npm run typecheck`, `npm run build:runtime`, and `git diff --check`
  passed.
- Verified latest fund-analysis contract guard: `node --check
  plugins/analytix-fund-analysis/scripts/sync-runtime-cache.mjs`, `node --check
  plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs`, `node
  plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs
  --self-test-generated-evidence-ignore --json`, `node --check
  plugins/analytix-fund-analysis/mcp/frontdoor-runtime.mjs`, `node --check
  plugins/analytix-fund-analysis/scripts/user-visible-language-smoke.mjs`, and
  `node plugins/analytix-fund-analysis/scripts/user-visible-language-smoke.mjs`
  passed. The smoke now covers runtime config remount self-test and internal
  frontdoor runtime-context propagation in addition to user-visible language
  redaction; the release-slice self-test covers generated-evidence dirty-path
  classification for both repo-root and `packages/runtime-go` working
  directories, including tracked local evidence rewrites while still rejecting
  evidence deletions; `node
  plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs
  --self-test-unrelated-dirty-groups --json` covers the global runtime,
  provider, renderer, packaging, matrix-audit, and plugin-manifest diagnostic
  buckets for unrelated dirty paths. The release-slice file inventory now includes
  frontdoor oracle scripts and Go MCP manager transport files used by the
  frontdoor P0 evidence path, and the audit JSON now includes
  `unrelated_dirty_groups` for global-runtime/provider/plugin leftovers outside
  this release slice.
- Verified latest fund-analysis release-readiness evidence guard: `node --check
  plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs`, `node
  --check plugins/analytix-fund-analysis/scripts/eval-coverage-contract.mjs`,
  `node --check
  plugins/analytix-fund-analysis/scripts/runtime-cache-contract.mjs`, `node
  plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs
  --self-test-generated-evidence-ignore --json`, `node
  plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs
  --self-test-native-runtime-evidence --json`, and `node
  plugins/analytix-fund-analysis/scripts/mcp-surface-snapshot.mjs --json
  --fail-on-violation` passed. The refreshed MCP snapshot is for plugin
  `0.16.10`, reports `backend_active_case_check:"passed"` against the live
  desktop data root active case `38dc50241996`, and the release audit now
  treats packaged-smoke `Resources/runtime/analytix-analysis-compute` as valid
  native runtime evidence when its `--help` exposes all required command names.
- Verified latest fund-analysis release gates: `node
  plugins/analytix-fund-analysis/scripts/doctor.mjs` passed; `node
  plugins/analytix-fund-analysis/scripts/mcp-return-oracle.mjs
  --require-case-db --fail-on-gaps` passed at
  `output/analytix-fund-analysis/mcp-return-oracle/2026-06-30T20-42-34-424Z/mcp-return-oracle.json`
  with 21 passed / 0 failed; `node
  plugins/analytix-fund-analysis/scripts/functional-eval.mjs` passed at
  `output/analytix-fund-analysis/functional-closure/2026-06-30T23-57-05-454Z/functional-eval.json`
  with 37 passed / 0 skipped / 0 failed; `node
  plugins/analytix-fund-analysis/scripts/investigation-scenario-audit.mjs
  --json --require-case-db --fail-on-structural-gaps` passed with
  11 supported scenarios, 0 structural gaps, and 1 case-data gap.
- Re-verified the goal-required validation commands on 2026-07-01:
  `go test ./internal/server` passed in `packages/runtime-go`; `node
  plugins/analytix-fund-analysis/scripts/doctor.mjs` exited 0 with existing
  polish warnings only; `node
  plugins/analytix-fund-analysis/scripts/mcp-return-oracle.mjs
  --require-case-db --fail-on-gaps` passed with 21 passed / 0 failed at
  `output/analytix-fund-analysis/mcp-return-oracle/2026-07-01T01-18-23-481Z/mcp-return-oracle.json`;
  `node plugins/analytix-fund-analysis/scripts/functional-eval.mjs` passed
  with 37 passed / 0 skipped / 0 failed at
  `output/analytix-fund-analysis/functional-closure/2026-07-01T01-18-25-495Z/functional-eval.json`;
  `npx vitest run packages/runtime/tests/child-agent-executor.test.ts packages/runtime/tests/delegation-runtime.test.ts packages/runtime/tests/task-job-orchestration-contract.test.ts packages/runtime/src/adapters/model/multi-provider-model-client.test.ts packages/runtime/src/adapters/model/compat-model-client.endpoint-format.test.ts packages/runtime/tests/thread-summary-route.test.ts`
  passed 6 files / 57 tests; and
  `npx vitest run src/renderer/src/agent/analytix-mapper.test.ts src/renderer/src/agent/analytix-runtime.test.ts src/renderer/src/components/PluginMarketplaceView.test.ts src/main/services/hub-agent-marketplace-service.test.ts packages/runtime/tests/skill-runtime.test.ts packages/runtime/tests/mcp-tool-provider.test.ts src/main/ipc/app-ipc-schemas.test.ts src/preload/preload-runtime-request.test.ts`
  passed 8 files / 223 tests.
- Verified latest release-slice isolation gate: `node
  plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs
  --summary-json --verify-isolated --staging-plan --readiness-plan` passed with
  `isolated_dirty_total:41`, `isolated_related_dirty_count:41`,
  `isolated_unrelated_dirty_count:0`, 60 node-check files,
  `doctor_skip_git_runtime_ok:true`, 151 doctor result checks, and temporary
  worktree/root cleanup complete; the same summary reports
  `staging_plan.goal_wide_unrelated_paths_to_keep_out_count:35`,
  `staging_plan.unknown_unrelated_paths_to_keep_out_count:0`, and
  `functional_closure_evidence.ready:true`.
- Verified latest exact-count, Pair Amount, and controlled-workbench guards:
  `node plugins/analytix-fund-analysis/scripts/count-case-rows-contract-smoke.mjs
  --json`, `node plugins/analytix-fund-analysis/scripts/pair-amount-contract-smoke.mjs`,
  and a direct real-case `executeLocalDuckdbWorkbenchSkill('run_case_sql', ...)`
  against `/Users/sun/Projects/江苏航案件分析` passed. The real-case Workbench result
  returned 120 rows plus `sql_policy`, `evidence_card`, `validation_state`,
  `metric_scope.source_scope`, and `top_n_contract.requested_limit=10`.
- Verified latest approval/MCP/frontdoor source guard: `go test . -run
  'TestRuntimeServerCaseFundPromptScopesProviderToolsToAnalytixFunds|TestRuntimeServerApprovalNever(BlocksMutatingToolExecution|AllowsReadOnlyMCPAndBlocksMutatingMCP)$'
  -count=1`, `go test ./internal/mcp -run
  'Test(ShouldRetryMCPCallTreatsClosedPipeAsTransportFailure|ProductionManagerToolDescriptionReturnsConfiguredDescription|ProductionManagerRetriesTransientCallFailureAfterReconnect|ProductionManagerDoesNotRetryJSONRPCToolErrors)'
  -count=1`, `go test ./internal/server -count=1`, `go test
  ./internal/mcp -count=1`, and `go test ./...` passed in
  `packages/runtime-go`.
- Verified latest frontdoor oracle gate: the full live `0.16.9` A-G oracle
  `node plugins/analytix-fund-analysis/scripts/frontdoor-p0-oracle.mjs --json
  --case-project-root /Users/sun/Projects/江苏航案件分析 --case-id
  38dc50241996 --threads-root
  output/analytix-fund-analysis/frontdoor-canary-runs/2026-07-01T04-35-00-0800-0.16.9-frontdoor-live-config-patched-threads-root/threads
  --require-installed-version 0.16.9 --fail-on-gaps` passed at
  `output/analytix-fund-analysis/frontdoor-p0-oracle/2026-06-30T20-41-37-063Z/frontdoor-p0-oracle.json`
  with A-G all `ok` and blockers empty.
- Verified latest broad gates after the interrupted-stream recovery depth,
  renderer subagent lifecycle-label closure, and packaged file-link closure:
  `go test ./...` in
  `packages/runtime-go` passed; `npm run build:runtime` passed; `npm run
  runtime:go:product-regression -- --json` passed with 35 product matrix
  features and all checks passing, including
  `go-runtime-interrupted-stream-recovery-contract`, `p0-speed-cache-gate`,
  `p0-runtime-health-smoke`,
  packaged GUI/session contract smokes, `p0-packaging-config-contract`, MCP/UI,
  subagent/background job, approval/user_input, attachments/vision, error
  display, and product-sovereignty scan; `npm run typecheck` passed; `npm run test` passed
  330 files / 2888 tests; `git diff --check` passed; `npm run build` passed
  and rebuilt runtime plus Electron main/preload/renderer bundles successfully.
  Machine-readable product-regression disclosure:
  `runtime_go_product_regression.status:passed`;
  `runtime_go_product_regression.matrix.rowCount:35`;
  `runtime_go_product_regression.runtimeClosureMatrixAudit:passed`;
  `runtime_go_product_regression.speedCacheGate:passed`;
  `runtime_go_product_regression.packagedGuiContract:passed`;
  `runtime_go_product_regression.packagedSoakContract:passed`;
  `runtime_go_product_regression.productSovereignty:passed`.
- Re-verified the upstream-ref matrix audit hook in the actual product gate:
  the first `npm run runtime:go:product-regression -- --json` run proved
  `runtime-closure-matrix-audit` itself but failed product-sovereignty on the
  old upstream-specific override naming; after renaming the override envs to
  `ANALYTIX_UPSTREAM_REPO_*`, `npm run scan:product-sovereignty`,
  `node scripts/runtime-closure-matrix-audit.mjs --strict-local --json`, and a
  second `npm run runtime:go:product-regression -- --json` all passed.
- Strengthened the upstream-ref matrix audit hook into a matrix shape/status,
  regression, required-domain coverage, source-citation, named-hazard coverage,
  and validation-ledger gate:
  `node scripts/runtime-closure-matrix-audit.mjs --strict-local --json` now
  reports `matrix.rowCount:30`, no `unresolved`, no `missingStatus`, no
  `missingRegression`, `shape.ok:true`, `coverage.ok:true`, and
  `namedHazards.ok:true` across all 15 user-called-out latent P0/P1 risks.
  It also reports `sourceRefs.checkedCount:261` with counts `analytix:168`,
  `reasonix:12`, `opencode:5`, `codexdesktop:4`, and `regression:72`, plus
  `requiredUpstreamFiles.ok:true` for every user-mandated Reasonix/OpenCode/
  CodexDesktop source file, no missing files, no missing upstream source
  bucket, no missing per-row regression source, and no out-of-range line
  numbers. It also reports `validationLedger.ok:true`,
  proving the matrix records `go test ./...`, `npm run test`,
  `npm run typecheck`, `npm run build:runtime`,
  `go test ./internal/server`,
  `npx vitest run packages/runtime/tests/child-agent-executor.test.ts packages/runtime/tests/delegation-runtime.test.ts packages/runtime/tests/task-job-orchestration-contract.test.ts packages/runtime/src/adapters/model/multi-provider-model-client.test.ts packages/runtime/src/adapters/model/compat-model-client.endpoint-format.test.ts packages/runtime/tests/thread-summary-route.test.ts`,
  `npx vitest run src/renderer/src/agent/analytix-mapper.test.ts src/renderer/src/agent/analytix-runtime.test.ts src/renderer/src/components/PluginMarketplaceView.test.ts src/main/services/hub-agent-marketplace-service.test.ts packages/runtime/tests/skill-runtime.test.ts packages/runtime/tests/mcp-tool-provider.test.ts src/main/ipc/app-ipc-schemas.test.ts src/preload/preload-runtime-request.test.ts`,
  `npm run runtime:go:product-regression -- --json`,
  `npm run runtime:go:speed-cache-gate`, `npm run build`, `npm run lint`,
  `node plugins/analytix-fund-analysis/scripts/phase4-diagnostics.mjs --release-guard --json`,
  `node plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs --summary-json --staging-plan --readiness-plan`,
  `node plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs --self-test-unrelated-dirty-groups --json`,
  `node plugins/analytix-fund-analysis/scripts/doctor.mjs`,
  `node plugins/analytix-fund-analysis/scripts/functional-eval.mjs`,
  `node plugins/analytix-fund-analysis/scripts/mcp-return-oracle.mjs --require-case-db --fail-on-gaps`,
  `npm run scan:product-sovereignty`,
  `npm run dist:mac:arm64`,
  `npm run runtime:go:packaged-gui-smoke -- --json --actual --timeout-ms 60000`,
  `npm run runtime:go:packaged-soak -- --json --actual --no-write --timeout-ms 90000`,
  `node scripts/runtime-closure-matrix-audit.mjs --strict-local --json`, and
  `git diff --check` as required validation evidence, while `node
  scripts/runtime-closure-matrix-audit.mjs --self-test --json` covers parsing
  inline code spans containing `|`, detects a synthetic `needs_fix` row, catches
  vague regression cells, catches missing risk levels, validates all required
  closure domains, catches missing/stale source references, catches missing
  upstream source-bucket coverage, catches missing regression source references,
  catches missing validation-ledger commands, and now reports
  `releaseBlockers.ok:true` only when the known non-completion blockers are
  explicitly named: `phase4_release_guard_summary: pass=106 warn=0 fail=2`,
  `phase4_release_guard_failures: release_git_tag_identity, release_git_worktree_clean`,
  `release_git_tag_identity`, `release_git_worktree_clean`,
  missing `analytix-fund-analysis-v0.16.10`, `publish_blocker_count:5`,
  `tracked_dirty_count:86`, `generated_evidence_dirty_count:845`,
  `release_slice_dirty_count:51`, `unrelated_dirty_count:35`,
  `goal_wide_unrelated_dirty_count:35`, `unknown_unrelated_dirty_count:0`,
  `ready_for_authorized_stage:true`, `stage_path_count:51`,
  `leave_unstaged_count:35`, `readiness_plan.blocker_count:3`,
  `readiness_plan.candidate_blocker_count:0`, and
  the authorization-gated stage/commit/tag/push/Hub/marketplace operations.
  It also reports `packagedRuntime.ok:true` only when the actual packaged GUI
  smoke and packaged soak outcomes are explicitly disclosed, including app
  launch, `window.analytix`, legacy alias absence, runtime-info reachability, no
  bundled Go source, SSE replay, tool timeline continuations, and provider
  redaction. It also reports `productRegression.ok:true` only when the broad
  product-regression outcome is explicitly disclosed, including overall pass
  status, matrix row count, matrix audit, speed/cache, packaged GUI/soak
  contracts, and product-sovereignty scan.
- Verified regenerated packaged boundary: initial `npm run dist:mac:arm64`
  exposed a real after-pack P1 where the runtime-local file-linked
  `analytix-computer-use` package was missing from
  `app.asar.unpacked/packages/runtime/node_modules`. After the after-pack
  restore fix, `npm run test -- src/main/packaging-config.test.ts --run`
  passed 12 tests, `npm run dist:mac:arm64` passed, and the regenerated
  `dist/mac-arm64/analytix.app` passed `npm run
  runtime:go:packaged-gui-smoke -- --json --actual --timeout-ms 60000` with
  `actualPackagedAppLaunched:true`, `window.analytix` present, legacy
  Kun/Reasonix aliases absent, top-level runtime/provider settings intact, MiMo
  provider profile accepted, runtime restart/health/runtime-info reachable, and
  no bundled Go source in the app. The same regenerated app passed `npm run
  runtime:go:packaged-soak -- --json --actual --no-write --timeout-ms 90000`
  with actual packaged thread/turn creation, SSE replay, tool timeline, MiMo
  plan mode, attachment fallback, approval/user_input continuation, fork/resume,
  thread list/search, usage, and provider redaction covered. Machine-readable
  packaged disclosure: `packaged_gui_smoke.actualPackagedAppLaunched:true`;
  `packaged_gui_smoke.windowAnalytixPresent:true`;
  `packaged_gui_smoke.legacyAliasesAbsent:true`;
  `packaged_gui_smoke.runtimeInfoReachable:true`;
  `packaged_gui_smoke.noBundledGoSource:true`;
  `packaged_soak.threadTurnSseReplay:true`;
  `packaged_soak.toolTimelineContinuations:true`;
  `packaged_soak.providerRedaction:true`.
- Remaining non-completion blockers: runtime/source/frontdoor closure is now
  proven for installed `0.16.9` and the packaged-smoke native runtime evidence
  is ready, but publish/release-slice alignment is still blocked by
  authorization-required release operations. Current HEAD is `1cf92bb62`.
  `node plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs
  --summary-json --staging-plan --readiness-plan` reports local
  `analytix-fund-analysis-v0.16.10` release tag missing, candidate version
  ready, `dirty.total:5`, `release_slice:1`, `unrelated:4`,
  `goal_wide_unrelated:4`, `unknown_unrelated:0`,
  `all_unrelated_paths_classified:true`, `publish_blocker_count:5`,
  `stage_path_count:1`, `leave_unstaged_count:4`,
  `staging_plan.goal_wide_unrelated_paths_to_keep_out_count:4`,
  `staging_plan.unknown_unrelated_paths_to_keep_out_count:0`,
  `readiness_plan.blocker_count:3`, and
  `readiness_plan.candidate_blocker_count:0`. The remaining dirty paths are
  the release-slice audit grouping patch plus this matrix note, the
  direct-identity Go runtime regression test, and the legacy TypeScript-loop direct
  answer profile implementation/regression. The latter four are classified as
  goal-wide keep-out work rather than release-slice files.
- Fixed local runtime cache drift: `node
  plugins/analytix-fund-analysis/scripts/sync-runtime-cache.mjs --apply
  --confirm-local-remount --json` remounted the Analytix-owned runtime cache to
  `0.16.10`, patched the runtime skill catalog/install policy/config/markers,
  and quarantined stale `0.16.4` plugin cache roots under `~/.analytix/.tmp`.
  `node plugins/analytix-fund-analysis/scripts/doctor.mjs` now passes the
  local runtime install cache checks with installed `0.16.10`,
  `0.16.10-local-348183406d2c`, and top-level skill present.
- Fixed release-boundary attribution slice: after the provider/model picker fix
  and committed runtime closure work moved into HEAD, the release-slice summary
  briefly exposed four `unknown_unrelated` paths:
  `src/main/analytix-process.ts`, `src/main/services/skill-service.ts`,
  `src/main/services/skill-service.test.ts`, and `src/main/upstream-models.ts`.
  `release-slice-audit.mjs` now classifies the first three as
  `runtime_plugin_skill_boundary` and the model picker path as
  `provider_model_picker`; both are goal-wide keep-out groups, not release-slice
  files. `node plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs
  --self-test-unrelated-dirty-groups --json` now covers those paths with
  `split.goalWideCount:30`, `split.unknownCount:1` for the synthetic unknown
  path, and `splitOK:true`.
- Fixed direct-answer release-boundary attribution: the same self-test now also
  classifies `packages/runtime/src/loop-test-support/agent-loop.ts` and
  `packages/runtime/tests/loop.test.ts` as `runtime_direct_answer_profile`,
  raising the synthetic `split.goalWideCount` to `32` while keeping
  `split.unknownCount:1` for the intentionally unknown fixture.
- Current 0.16.12 fund-analysis release evidence is now refreshed from the
  active worktree. `node plugins/analytix-fund-analysis/scripts/doctor.mjs`
  exits 0 with plugin identity `analytix-fund-analysis@0.16.12`, embedded
  references in sync, runtime cache production files covered, and local runtime
  install cache drift passing with installed `0.16.12`,
  `0.16.12-local-c61ac56bf737`, and top-level skill present. `node
  plugins/analytix-fund-analysis/scripts/functional-eval.mjs` exits 0 with 37
  passed / 0 skipped / 0 failed at
  `output/analytix-fund-analysis/functional-closure/2026-07-01T04-42-08-513Z/functional-eval.json`.
  `node plugins/analytix-fund-analysis/scripts/mcp-return-oracle.mjs` exits 0
  with 22 passed / 0 failed at
  `output/analytix-fund-analysis/mcp-return-oracle/2026-07-01T04-42-49-427Z/mcp-return-oracle.json`.
  `node plugins/analytix-fund-analysis/scripts/mcp-surface-snapshot.mjs
  --fail-on-violation` exits 0 and refreshes
  `output/analytix-fund-analysis/evidence/mcp-surface-snapshot.json` for
  plugin `0.16.12` with 33 tools, 20 resources, 5 prompts,
  `default_toolbox_visible:true`, `active_case_schema_optional:true`,
  `report_heavy_hidden_by_default:true`, `production_resources_only:true`,
  `prompt_surface_visible:true`, `root_skill_readable:true`, and
  `current_case_source_check:"blocked_no_case_project"` /
  `case_source_blocker_visible:true`, proving the no-case state now follows the
  current case-project blocker contract instead of the legacy backend
  active-case API.
- Current 0.16.12 release readiness is still not publish-complete, but the
  remaining blockers are narrowed to authorization-gated release identity and
  dirty-worktree separation, not runtime closure. `node
  plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs
  --summary-json --readiness-plan --staging-plan --verify-isolated` exits 0 and
  reports `candidate_version_ready:true`,
  `readiness_plan.candidate_blocker_count:0`,
  `readiness_plan.blocker_count:3`, `publish_blocker_count:5`,
  missing `analytix-fund-analysis-v0.16.12`,
  `dirty.total:40`, `release_slice:21`, `unrelated:19`,
  `goal_wide_unrelated:8`, `unknown_unrelated:11`,
  `stage_path_count:21`, and `leave_unstaged_count:19`.
  `node plugins/analytix-fund-analysis/scripts/release-slice-audit.mjs
  --fail-on-publish-blockers` exits 1 as intended with the same publish
  blockers, so CI/release automation cannot mistake this dirty, untagged tree
  for a publishable slice. `git diff --check` remains clean after the 0.16.12
  matrix and plugin gate updates.
- Current 0.16.15 release-boundary attribution is refreshed after the
  Go-only runtime P0/P1 closure, provider/model hard-binding, goal/todo
  final-readiness, and fund-analysis release identity pass. The existing
  `analytix-fund-analysis-v0.16.14` tag is intentionally left untouched; the
  publishable local identity advances to `analytix-fund-analysis-v0.16.15`.
  The current Go runtime tree passes `go test ./...` in
  `packages/runtime-go`, covering runtime server, MCP manager, provider,
  durable-store, direct-answer/cache, SSE setup, subagent identity, and goal
  final-readiness tests together instead of only the targeted slices.
  The shared TS/runtime/renderer validation also passes `npm run typecheck`,
  `npm run build:runtime`, full `npm run test`, targeted runtime/renderer
  projection tests, and `npm run runtime:go:product-regression -- --json`.
  Current fund-analysis plugin gates pass for `0.16.15`: `doctor.mjs` exits 0,
  `functional-eval.mjs` passes 37 / 0 / 0, `mcp-return-oracle.mjs` passes
  23 / 0, and `mcp-surface-snapshot.mjs --fail-on-violation` refreshes
  `output/analytix-fund-analysis/evidence/mcp-surface-snapshot.json` to plugin
  `0.16.15` with 33 tools, 20 resources, 5 prompts, and
  `blocked_no_case_project` active-case evidence. After the release commit and
  local tag, `release-slice-audit.mjs --summary-json --staging-plan
  --readiness-plan` reports `dirty.total:0`, `release_slice:0`,
  `unrelated:0`, `goal_wide_unrelated:0`, `unknown_unrelated:0`,
  `current_release_tag_state:"at_head"`, `publish_identity_ready:true`,
  `publish_blocker_count:0`, `readiness_plan.can_publish_current_tree:true`,
  and `readiness_plan.candidate_blocker_count:0`. The self-tests still cover
  generated-evidence redaction, release-tag `missing`/`stale`/`at_head`
  diagnostics, summary JSON bounds, and the synthetic unknown dirty-path split.
- Current live Reasonix DeepSeek parity is now repeatable through
  `npm run runtime:go:reasonix-live-parity -- --json`, backed by
  `scripts/runtime-go-reasonix-live-parity.mjs` and routed through
  `scripts/runtime-go-validation-command.mjs`. The 2026-07-01 run passed:
  analytix `deepseek-v4-pro` warm live cache call was 762ms with
  cache-hit-rate `0.9868480725623583`; Reasonix CLI `deepseek-pro`
  (`deepseek-v4-pro`) warm run was 1752ms with cache-hit-rate
  `0.9909121507909795`; Reasonix provider live cache probe for
  `deepseek-v4-flash` passed with 88% warm cache and separately exposed the
  reasoning round-trip prompt-token delta of 521. The parity command redacts
  secrets, uses temporary Reasonix home/workspace state for CLI metrics, and
  keeps this as live evidence rather than a production runtime dependency.
  The broader `npm run runtime:go:live-validation -- --json
  --live-deepseek-cache-from-settings
  --live-non-deepseek-provider-from-settings --no-gate` run also passed on the
  same day with DeepSeek `deepseek-v4-pro` cache-hit-rate
  `0.9868480725623583` and a non-DeepSeek Xiaomi/MiMo probe responding in
  2409ms.
