# 2026-08-04 controlled thread checkpoint

Status: workspace convergence is complete; Analytix product delivery is not
complete. Milestone A is blocked, Milestone B is unverified, and P3 remains
deferred. Cache authority and the small cold/warm integrity probes pass, but
offline compaction is
`HOST_RESTART_REQUIRED_FOR_OFFLINE_COMPACTION`. Effective backing capacity
remains below the proven space requirement for the next cold package.

## Canonical baseline

- Branch: `main`.
- Convergence source HEAD:
  `b50978a04a8548cc5e2fa7414a9155db72797cc9`.
- Convergence source tree and formal index tree:
  `dc69e151541d6ca6235a5378308f91616c7886fe`.
- Immediately before this handover-only docs commit,
  `git status --porcelain=v1 -uall` was empty, the formal index had no staged
  paths, and the empty status SHA-256 was
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
- This file is the final repository mutation in the convergence pass. A Git
  commit cannot contain its own hash without a circular identity. Therefore,
  the exact final HEAD/tree are recorded after commit in the thread's final
  response and in the owner-only `final-state-attestation.json` beside the
  convergence manifest.
- The final residual/capacity closeout resumed from clean HEAD
  `ac0ff5d102a8de494f7abd938d5e00e22e55d435` and tree
  `66175881e6125468c2058de7d2f15054931281c3`. Its final routing commit likewise
  cannot record its own identity here; verify the current HEAD/tree fresh.

## Focused commits and fresh verification

The convergence audit found no pre-existing dirty product slice or accepted
spec delta that met all commit criteria. Consequently, this pass creates no
Agent, funds, renderer, authority, package, or spec commit. The only focused
commit is the final handover docs commit containing this file.

Fresh convergence verification:

- Cache helper fail-closed validation passed before every archive validation
  operation.
- The initial `b50978a04` tracked full-index binary patch plus exact untracked
  archive reconstructed all 1,072 dirty records with zero hash mismatches.
- Cache-only preservation validation passed for the hydration candidate,
  recovery candidate, mode-fix index, plan-pair diagnostic, BDD diagnostic,
  schedule repro files, and the `b576` diagnostic bundle.
- The owner-only preservation bundle passed archive path-safety and all 30
  payload checksum checks.
- The exact cache cleanup plan deleted 125 resolved targets; zero planned
  targets remain.
- Final cache helper, APFS, ownership-enabled, non-read-only, and writable
  checks passed.
- No product test, formal package, or packaged A/B run was started in this
  convergence pass, as required by the controlling request.

## Convergence and preservation evidence

- Convergence manifest:
  `/Volumes/AnalytixCache/development-v3/evidence/controlled-thread-convergence-20260804.CxOGk7FP/manifest/convergence-manifest.json`
- Manifest SHA-256:
  `b1583ca671ad4e8e50157e6ef0cd01accac7801060549c646796ad858f6fd493`
- Preservation archive:
  `/Volumes/AnalytixCache/development-v3/evidence/controlled-thread-convergence-20260804.CxOGk7FP/preservation/controlled-thread-preservation-bundle.tar.gz`
- Archive SHA-256:
  `972ccc841d0dd95652d04ed1dd26036b4041dc5415067f681954df52ae546a34`
- Archive restore instructions and per-file checksums are inside the archive.
  Restore only into a task-owned extraction at the recorded base commit. The
  archive is preservation evidence, not integrated source or product delivery.

No secret quarantine payload was required. The archive contains no credential,
token, live user profile, protected DuckDB, raw thread, provider body, case
narrative, or live user history.

## Canonical dirty classification

The initial worktree contained 520 tracked dirty paths and 552 untracked paths.
No path was `COMMIT_READY_A`, `COMMIT_READY_B`, or
`ACCEPTED_SPEC_DELTA`. Conservative classification was:

- `INCOMPLETE_A_B`: 778
- `SUPERSEDED`: 215
- `DEFERRED`: 46
- `USER_OR_UNKNOWN`: 21
- `DIAGNOSTIC_ONLY`: 10
- `EVIDENCE_ONLY`: 2

All initial bytes are recoverable from the verified preservation archive. The
552 untracked paths included 167 exact checkpoint-test leftovers generated by
`src/main/services/git-checkpoint-service.test.ts`; they were archived,
verified, and removed by exact path. The remaining incomplete, deferred,
diagnostic, superseded, and unknown state was likewise preserved and removed
without `reset`, `clean`, `restore`, `checkout`, `stash`, `rebase`, or amend.

## Cache reconciliation

Cache candidates were classified as follows:

- Forty alternate indexes were audited. Thirty-nine were equivalent to source
  already in committed history. The only unmatched mode-fix index deleted an
  accepted harness seam and was superseded; its minimal patch is preserved.
- The nine-path hydration/prompt plus QA/test-support candidate and the
  three-path generic retry candidate are `NOT_INTEGRATED_CACHE_ONLY`.
- The `b576` tool-step commit, plan-pair, BDD, schedule, and other harness
  candidates are diagnostic or superseded, not formal evidence.
- The r45/r46 formal reports and their artifact hashes, codesign fields, and
  failure classification are retained. Expanded packages and source copies
  were reconstructable and removed.
- Trusted Go, Cargo, npm, Electron, and sccache namespaces were retained as
  caches, not source.
- Exactly two task-owned, owner-only existing-state/protected-case clone roots
  are retained. `existing-state-clone-r1.n14elhkd` is the smaller recovery and
  consistency clone (238,356 KiB); it contains the retained isolated
  `user-data`, `runtime-data`, workspace binding, and verified DuckDB copy.
  `existing-state-clone.dp4Ret` is the larger protected B-analysis copy
  (4,489,748 KiB) containing the isolated case workspace and data-analysis
  state. The second root was omitted from the original count. Neither clone
  was opened by a product process, modified, copied into canonical source, or
  admitted to deletion. No live mutable user state was opened or modified.
- Obsolete task-owned runtime/profile snapshots were destroyed without being
  archived. The only detected `user-data`/`runtime-data` directories remain
  inside the smaller retained recovery clone; the larger protected clone is a
  separate root and must be counted independently.

The earlier capacity conclusion conflated two different layers. APFS container
free space is logical headroom inside the sparse image; allocation can grow
only while the backing `/Volumes/DataSSD` has physical space. Fresh correction
measurements were:

| Measurement | Before correction | Final | Meaning |
| --- | ---: | ---: | --- |
| Backing DataSSD application-available bytes | 6,746,931,200 | 6,746,931,200 | Physical growth ceiling for the sparse image |
| v3 sparsebundle allocated size (`du`) | 203,256,889,344 bytes (198,493,056 KiB) | 203,256,889,344 bytes (198,493,056 KiB) | Backing physical allocation |
| Inner APFS container free bytes | 46,182,543,360 | 54,803,070,976 | Logical free space, not guaranteed backing capacity |
| Cache mount application-available bytes | 6,746,927,104 | 6,746,931,200 | Effective application allocation ceiling |

`diskutil verifyVolume` passed before the detach attempt. The mount job was
suspended and all task/open-handle checks were clear, but one normal
`hdiutil detach` and sixteen normal unmount attempts were consistently
dissented by the unrelated root-owned macOS `syspolicyd` service. No force
detach, service termination, image repair, or compact operation was used.
Compaction is therefore `BLOCKED`, and the unchanged backing/image values above
are the post-attempt facts rather than a claimed physical recovery. The v3
image was left mounted at its verified UUID with ownership enabled; damaged v2
and legacy images remain detached and untouched.

Eight exact, task-owned, handle-free, reconstructable targets were removed
after an owner-only hash plan was recorded: the obsolete
`milestone-a-node-glob.ac9zCuiM`, three old package roots, the superseded
provider candidate and plan archive, and two clean diagnostic repository
copies without unique product source. Their audited allocation total was
8,238,948 KiB. This increased inner logical free space but, without compaction,
did not recover backing physical capacity. The convergence preservation
bundle, its manifests/checksums, minimal r45/r46 reports, both protected clone
roots, and trusted compiler/dependency caches remain. The two duplicate 8d7
diagnostic roots no longer remain: their byte-equivalent, one-path harness diff
was preserved once as `residual-8d7-diagnostic-harness.patch` with SHA-256
`f5b09eb4391e5f9f0fe4fc0eba8e45ba941afb8d306f1ddf3f00056003efddf9`.
It applies cleanly to a fresh archive of base
`8d79659b29b39216ffd9777d16ababc4ebd6f51a`, recreates only
`scripts/runtime-go-packaged-milestone-a.mjs`, and produces the audited result
blob `99c4fb07190645cc84adb49f467d584432bce8a7`. After that validation, both
exact owner-only roots were deleted. The patch remains `DIAGNOSTIC_ONLY`, not
integrated product source or evidence of a fix.

The final canonical cache verifier passed `verifyVolume`, fail-closed helper,
cold/warm Go probe, byte-identical SHA-256, and strict ad-hoc codesign checks.
Its receipt is `cache-verification-v1.receipt.I9G1eJNv` with SHA-256
`5887279834775d2b70e4ec8a0128eacf2ad5dea5a2fdff8b06d9e0a8eeea566c`.
The correction result is recorded owner-only in
`cache-physical-capacity-result.json` beside the convergence manifest (SHA-256
`e10a1f0b62215223e6c5206df9ffde21647ab0c81d68eaa1553ee206566388bc`).

### Residual closeout and final offline retry

The append-only `residual-closeout-8d7-20260804.json` records the two-root
identity, archive application check, exact deletion, and non-product
classification; its SHA-256 is
`b5406d8331acde4c8f61374b99bd7ddf9110c3e19517f6d92f08a6a6304cbf37`.
Deleting the two roots raised inner logical free from 54,802,886,656 to
58,572,054,528 bytes. It did not shrink the sparsebundle or reclaim backing
physical allocation.

After residual deletion, the final bounded retry ran from outside the cache,
suspended the exact mount job, passed `diskutil verifyVolume`, and found zero
ordinary visible cache handles. The one permitted normal, non-forced
`hdiutil detach` returned `resource busy`; it did not name the dissenter in
that response. The prior deterministic normal-unmount failure on the same host
identified root-owned `syspolicyd`, so no force, service mutation, or further
retry was attempted. `hdiutil compact` was not invoked. The mount job was
restored and the cache helper passed again.

| Final retry measurement | Before | After restored mount | Result |
| --- | ---: | ---: | --- |
| Backing DataSSD application-available bytes | 6,732,120,064 | 6,732,382,208 | Small statfs drift only; no compact recovery |
| v3 sparsebundle allocated size (`du`) | 203,256,889,344 bytes (198,493,056 KiB) | 203,256,889,344 bytes (198,493,056 KiB) | Unchanged |
| Inner APFS container free bytes | 58,572,042,240 | 58,571,984,896 | Logical only |
| Cache mount application-available bytes | 6,732,120,064 | 6,732,349,440 | Application view; not backing capacity proof |

The bounded result is
`HOST_RESTART_REQUIRED_FOR_OFFLINE_COMPACTION`, not `PASS`. Its append-only
record is `offline-compaction-retry-20260804.json` with SHA-256
`56c5cfbd24a7fa4e1fa04cc88265e259373a73f077c1f35be3e72039aa7089a3`.
The damaged v2/legacy images and unrelated DataSSD content were untouched.

## NOT_INTEGRATED state

`NOT_INTEGRATED` remains. It includes the 778-path incomplete canonical A/B
state, 46 deferred paths, 10 diagnostic paths, 21 user-or-unknown paths, and
the cache-only hydration, retry, and diagnostic candidates. It is recoverably
preserved but absent from the clean canonical source. None of it is claimed as
fixed, verified, committed, or delivered. The newly extracted 8d7 patch is an
append-only `DIAGNOSTIC_ONLY` preservation item within that same boundary.

## Formal Milestone A evidence

Formal packaged r45 and r46 both used source commit `b50978a04`, passed the
formal-artifact, isolated-profile, configured-provider, first-launch, and
visible normal Hub-login checks, and exposed the ordinary tool catalog. Each
run counted three successful ordinary tool executions before the ordinary turn
terminated with `tool_not_advertised`. The ordinary workflow did not reach its
expected marker, and downstream repository mutation, subagent, long-context,
compaction, restart/recovery, normal-exit, and renderer evidence checks were
skipped. Both reports therefore correctly remain `failed`, not partial passes.

The exact rejected tool name or name hash was not observed. Proven facts do not
distinguish provider emission from runtime catalog/advertisement binding or a
harness expectation defect. Cache-only prompt, retry, and harness experiments
did not run from the canonical commit through a new formal package, so r47/r48
and those temporary diagnostics do not constitute a formal PASS and do not
prove a product fix.

## Milestone A: as-built, target, gap

As-built: commit `b50978a04` can produce the formal packaged artifact, pass
codesign revalidation, launch with fresh isolation, complete visible normal Hub
login, expose the ordinary catalog, and execute some ordinary tools.

Target: the packaged universal Agent must complete the real ordinary workflow,
repository edit and test, Todo, bounded subagent, long-context compaction,
normal exit, relaunch, and exact thread/result recovery through public seams.

Gap: the first formal ordinary workflow terminates with
`tool_not_advertised` before the expected marker. This is the single current
Milestone A blocker; its exact rejected tool and originating layer are not yet
proven.

## Milestone B: as-built, target, gap

As-built: accepted funds/native/Go substrate exists in committed history. The
large dirty authority, funds, evidence, desktop, and renderer expansion did not
form a production-wired, focused-verifiable slice and is preserved as
`NOT_INTEGRATED`.

Target: a fresh packaged multi-round real DuckDB `analyze_account_flows` flow
must bind the exact case and immutable snapshot, produce a replayable redacted
analysis, EvidenceReceipt, ClaimRecord, Final Gate publication, ordinary UI,
and a separately authorized controlled full-value result.

Gap: no clean-commit packaged Milestone B public-seam run was performed in the
convergence pass. Import, cleaning, snapshot evolution, multi-round recovery,
zero-leak scanning, and controlled full-value release therefore remain
unverified. B did not substitute for A.

## OpenSpec and deferred scope

The active task ledger is 101/198. That count is task accounting only, not
product evidence. P3, full upstream absorption, formal report expansion,
second-stage work, and cross-platform release remain deferred. No task
denominator or OpenSpec change was added during convergence.

## First blocker and next thread

Do not mechanically rerun packaged Milestone A first. Once effective backing
capacity has been safely reconciled, start from clean committed source with a
fresh isolated one-shot repository and reproduce the first failing ordinary
turn against the headless production Go runtime. Capture the public HTTP
request/SSE terminal, then repeat the same seam through the supported
`analytix serve` Agent CLI launcher. Stop at the first terminal and use only
sanitized public/durable diagnostics to identify the exact rejected tool and
distinguish provider emission, Go catalog/grant binding, and launcher
propagation. Only after a focused fix is verified, committed, and validated
from that commit's archive should the next thread rebuild the package and run
visible normal Hub-login Milestone A acceptance. Do not reuse a prior profile,
package, candidate repository, or diagnostic conclusion.

Until a host restart, one successful offline compact, and a fresh backing
capacity preflight satisfy the measured package peak plus margin, do not run
package. A new thread may perform only the low-write headless Go Runtime,
HTTP/SSE, and `analytix serve` CLI diagnosis described above.

## External release conditions

- Formal publication signing/notarization and release-publication authority:
  `UNVERIFIED`; local development codesign is not release authority.
- Exact lawful case authority, independent PII projection grant, audit binding,
  and trusted controlled sink: `BLOCKED/UNVERIFIED` until supplied for a real
  authorized case.
- Windows and Linux formal package, migration, rollback, and runtime paths:
  not run in this convergence pass.

These external gaps do not convert any skipped or diagnostic row into PASS.
