interface:
  display_name: "专项资金核算"
  short_description: "当前案件只读、自定义统计范围、可复核的专项资金核算"
  default_prompt: "请按当前案件只读统计范围完成自定义核算，并说明统计范围、总额、核验意见和补证建议。自定义范围、只统计、复核后有效、重算类答案必须单列“**核验意见：**”，句内出现“已有流水支持”和“暂不能认定”，区分已有流水支持、仅作筛查范围和不能写入事实的事项。"
policy:
  allow_implicit_invocation: true
